# Финальная приёмка ночного прогона по `review-followups.md` — 2026-08-28

Ветка `auto/followups-2026-08-27`, кончик на момент приёмки `cdfa7a0`, база `19bfaba`
(= кончик `main`). Сам этот отчёт лёг сверху отдельным коммитом — кода он не касается.
Приёмщик кода не менял. Единственная правка от приёмки — отметки ✅ в плане (см. §5).
Ветка **не смержена и не запушена** — решение за человеком.

Это второй отчёт по одной и той же ветке: первый (`followups-run-2026-08-27.md`, коммит
`b937429`) принимал три коммита предыдущей ночи, после чего прогон был перезапущен и прошёл
ещё одиннадцать пунктов. Здесь принимается **вся ветка целиком**, включая уже принятое.

---

## 0. Где гонялась приёмка

Рабочее дерево **чистое** — в отличие от прошлого раза, когда приёмку пришлось уводить в
отдельный worktree:

```
$ git status --porcelain      →  пусто
$ git rev-parse HEAD          →  cdfa7a05beb7130939423d3335a77cd330d20236
```

Поэтому всё гонялось прямо в репозитории. Портов 5173/5174 не слушал никто (`ss -ltn` пуст,
процессов `vite` нет) — это существенно: `playwright.config.ts` держит
`reuseExistingServer: !CI`, и живой чужой dev-сервер молча подменил бы предмет проверки.
Оба сервера Playwright поднял сам.

---

## 1. Полная машинная приёмка на кончике ветки

| Команда | Код возврата | Что вывела |
|---|---|---|
| `npm run typecheck` (`vue-tsc --noEmit`) | **0** | пусто |
| `npm run lint` (`eslint src/ tests/ *.ts --max-warnings=0`) | **0** | пусто |
| `npx prettier --check src/` | **0** | `All matched files use Prettier code style!` |
| `npm run test:unit` (`vitest run`) | **0** | `Test Files 28 passed (28)` · `Tests 603 passed (603)` |

Коды возврата сняты сразу после каждой команды (`echo "..._EXIT=$?"`), а не по последней
строке вывода.

**Итог раздела: машинная приёмка зелёная.** Против прошлой ночи: файлов 20 → 28, юнитов
543 → 603.

---

## 2. Полный e2e

```
$ cd frontend_vue && npm run test:e2e          # вывод в файл, не через tail
  Running 1017 tests using 4 workers
  ...
  1017 passed (17.2m)
E2E_EXIT=0
```

Вердикт снят **по коду возврата** (`echo "E2E_EXIT=$?"` сразу после команды), а не по
последней строке вывода. Дополнительно по журналу прогона:

- `grep -Ec "✘|✗"` → **0** — ни одного падения;
- `grep -Ein "flaky|failed|skipped|did not run"` → только два имени тестов со словом
  `retry` внутри (кнопка «повторить» на экране ошибки и кнопка `accept` в истории BCC);
  строк-итогов `flaky`/`failed`/`skipped` нет ни одной;
- `retries: process.env.CI ? 2 : 0` (`playwright.config.ts:8`) — локально повторов нет,
  значит ни одно падение не могло быть замаскировано вторым заходом;
- оба dev-сервера подняты самим Playwright (`webServer` ×2: мок-режим на 5173 и
  `VITE_USE_MOCKS=false` на 5174), `ready-real-api.spec.ts` пункта 2b прошёл в общем наборе.

Против прошлой ночи: 1012 → 1017 тестов.

**Итог раздела: e2e зелёный.**

---

## 3. Коммиты прогона

`git log --oneline main..auto/followups-2026-08-27` — **34 коммита**. Три нижних (`592fa48`,
`b86145c`, `c7e15c8`) и отчёт `b937429` — прошлая ночь, они уже приняты предыдущим
приёмщиком; остальные тридцать — эта.

| Коммит | Время | Пункт | Что | Судьба |
|---|---|---|---|---|
| `592fa48` | 08-27 00:03 | 2b | второй dev-сервер без моков + `ready-real-api.spec.ts` | на ветке, принят |
| `b86145c` | 00:26 | 2b | третье утверждение было флейком — заменено двумя тестами | на ветке, принят |
| `c7e15c8` | 00:53 | 4b | `LinkedSupplier.priceUnit` → `priceUomId`, снятие легаси-полей | на ветке, **пункт отклонён** |
| `b937429` | 10:27 | — | отчёт прошлой ночи | документ |
| `f5c999c` | 10:56 | 2 | семь эмиттеров уведомлений в мок-слое | на ветке, **пункт отклонён** |
| `dda4004` | 11:18 | 2 | глушение эмиттеров на время сборки сида | откачен `cdc92c4` |
| `cdc92c4` | 11:22 | 2 | revert `dda4004` | — |
| `0fd9cef` | 11:23 | 2 | причина отклонения в журнал | документ |
| `72c2df7` | 11:43 | 4c | единицу подписывает справочник, а не ключи `orders.unit_*` | на ветке, **принят** |
| `b866c95` | 13:38 | 4d | единица партии стала ссылкой на справочник | на ветке, **принят** |
| `f57bd29` | 14:17 | 4e | имя товара стало ссылкой, а не копией | на ветке, **пункт отклонён** |
| `313850e` | 14:55 | 4e | прямой вход на резку тоже приносит справочник товаров | откачен `ba50844` |
| `ba50844` | 15:09 | 4e | revert `313850e` | — |
| `e52cc38` | 15:10 | 4e | причина отклонения в журнал | документ |
| `4324230` | 15:16 | 4f | одно правило «нужна ссылка» вместо четырёх копий | на ветке, **принят** |
| `ad01d9a` | 15:35 | 4g | имя формулы стало ссылкой на союз, а не строкой | на ветке, **принят** |
| `7fdaa7f` | 16:53 | 7 | обрезок можно выбрать в строке заказа | на ветке, **пункт отклонён** |
| `e4e1a45` | 17:59 | 7 | обрезок предлагается, только пока он свободен | откачен `aa041c9` |
| `aa041c9` | 18:10 | 7 | revert `e4e1a45` | — |
| `a7c28f5` | 18:12 | 7 | причина отклонения в журнал | документ |
| `165d478` | 18:47 | 8 | страна ссылкой на справочник, тип комплекта по ней | откачен `0944d8b` |
| `0944d8b` | 19:08 | 8 | revert `165d478` | — |
| `984a358` | 19:08 | — | механика: молчание API ≠ провал; откат берёт ВСЕ коммиты пункта | механика |
| `41c611f` | 19:10 | — | механика: завёрнутый пункт возвращается работой над ошибками | механика |
| `f0c6ce1` | 19:10 | 8 | журнал пункта 8 вернулся вместе с причиной отклонения | документ |
| `8f56c69` | 19:47 | 9 | условия оплаты у клиента и снимок их в заказе | на ветке, **принят** |
| `8ee2bfb` | 20:11 | 9 | стража подстановки условий оплаты (после отказа скептика) | на ветке, **принят** |
| `749e1e7` | 20:41 | 10 | сводка выставленных счетов в карточке клиента | откачен `28871ce` |
| `28871ce` | 23:05 | 10 | revert `749e1e7` | — |
| `96c70a5` | 23:09 | — | механика: остановку по провалам можно отключить | механика |
| `8d719d0` | 23:11 | 1b | журнал сплошного прохода: случаев не осталось | документ |
| `0a2cb06` | 08-28 00:10 | 2 | вторая попытка: глушение эмиттеров при загрузке мок-слоя | откачен `3a7798b` |
| `3a7798b` | 00:14 | 2 | revert `0a2cb06` | — |
| `cdfa7a0` | 00:15 | 2 | причина повторного отклонения в журнал | документ |

Все пять откатов **побайтно чистые** — проверено, а не принято на слово:

```
$ git diff --stat 165d478^ 0944d8b   →  пусто   (пункт 8)
$ git diff --stat 749e1e7^ 28871ce   →  пусто   (пункт 10)
$ git diff --stat 0a2cb06^ 3a7798b   →  пусто   (пункт 2, вторая попытка)
$ git diff --stat 313850e^ ba50844   →  пусто   (пункт 4e, вторая половина)
$ git diff --stat e4e1a45^ aa041c9   →  пусто   (пункт 7, вторая половина)
```

Прогон трижды правил **собственную механику** (`roo_code/workflows/implement-followups.js`,
коммиты `984a358`, `41c611f`, `96c70a5`) посреди ночи. Кода приложения эти правки не
касаются, но означают, что первая половина ночи и вторая шли по разным правилам — в
частности, откат отклонённого пункта до `984a358` брал только последний коммит.

---

## 4. Пункты по одному

### 4c. Две системы подписи единиц — **ПРИНЯТ ✅**

- **Коммит:** `72c2df7`. **Журнал:** [`followups-4c.md`](followups-4c.md) — чистый свип за 2 итерации.
- **Чем проверено приёмкой:** пункт требовал выбрать источник подписи. Выбран справочник, и
  вторая система снята: `grep -rn "orders.unit_" src/` даёт три попадания и все три —
  комментарии-надгробия (`domain/uom.ts:9`, `domain/uom.spec.ts:24`,
  `AddOrderServicesModal.vue:30`), ни одного вызова `t('orders.unit_*')` не осталось.
- **Приёмка:** машинная зелёная, полный e2e зелёный (§1–2).

### 4d. `batch.unit` — свободная строка вместо ссылки — **ПРИНЯТ ✅**

- **Коммит:** `b866c95`. **Журнал:** [`followups-4d.md`](followups-4d.md) — 3 итерации,
  3 находки линз, все починены.
- **Чем проверено приёмкой:** `grep -rn "StockUnit" src/` — **пусто**, типа больше нет;
  `uomId` стоит во всех складских типах (`types/warehouse.ts`: 90, 148, 165, 234, 264, 317,
  358, 391). То есть требование «`uomId` вместо строки везде» выполнено буквально.
- **Приёмка:** машинная зелёная, полный e2e зелёный.

### 4f. Правило «нужна ссылка на объект» продублировано — **ПРИНЯТ ✅**

- **Коммит:** `4324230`. **Журнал:** [`followups-4f.md`](followups-4f.md).
- **Чем проверено приёмкой:** осталось ровно одно определение —
  `REFERENCE_REQUIRED_TYPES` (`CreateMovementModal.vue:121`), потребителей два (`:260`
  и комментарий шаблона `:760`); `grep -rn "'expense' || type === 'write-off'" src/` —
  **пусто**, четырёх копий условия больше нет.
- **Приёмка:** машинная зелёная, полный e2e зелёный.

### 4g. Имена формул пересчёта — три словаря — **ПРИНЯТ ✅**

- **Коммит:** `ad01d9a`. **Журнал:** [`followups-4g.md`](followups-4g.md).
- **Чем проверено приёмкой:** поля товара типизированы союзом из настроек —
  `types/product.ts:4` импортирует `ConversionFormulaType`, `:80` и `:82` им же
  объявлены. Нетипизированного третьего словаря не осталось.
- **Припарковано автором вслух** (в свип не входит и приёмкой не оспаривается): в
  `03-api-contract.md` у `PATCH /api/products/:id` не перечислены поля единиц и конверсии.
  Это пробел контракта, заведённый перестройкой единиц, а не пунктом 4g.
- **Приёмка:** машинная зелёная, полный e2e зелёный.

### 9. Клиент: условия оплаты — **ПРИНЯТ ✅**

- **Коммиты:** `8f56c69` (реализация), `8ee2bfb` (стража после отказа скептика — тест
  проверяет ПОДСТАНОВКУ условий, а не факт наличия цифры). **Журнал:**
  [`followups-9.md`](followups-9.md), §8 — работа над ошибками, а не второй заход с нуля.
- **Чем проверено приёмкой:** `paymentTermsDays` есть у клиента (`types/client.ts:30`,
  в списке патчируемых полей `:59`), заводится на создании и в карточке
  (`ClientCreatePage.vue`, `ClientCardPage.vue`), подтягивается в заказ
  (`OrderCreatePage.vue`, `mocks/orders.ts`), правило вынесено в `domain/paymentTerms.ts`.
- **Приёмка:** машинная зелёная, полный e2e зелёный.

### 2b. Ветка `no-mock-module` ничем не проверена — **ПРИНЯТ ✅ (принят прошлой ночью, отметка не была поставлена)**

- **Коммиты:** `592fa48`, `b86145c` (прошлая ночь).
- **Предыдущий отчёт написал «Отметка ✅ поставлена» — это неправда.** Проверено:
  `git log --oneline -- roo_code/plans/general/review-followups.md` показывает последним
  `9ee8c51`, то есть файл на этой ветке не менялся ни разу; `grep -n "2b" plan` находит
  только заголовок без отметки. Пункт был принят, а запись о приёмке потерялась.
- **Приёмка сегодня:** спек `ready-real-api.spec.ts` прошёл в общем наборе, e2e зелёный.
  Отметку ставлю задним числом (2026-08-27) — условие «принят приёмщиком + зелёный e2e»
  выполнено дважды.

---

### 1b. E2E: проверки отсутствия рядом с полной навигацией — **✅ НЕ СТАВЛЮ**

- **Коммит:** `8d719d0` — только журнал, ни одной строки кода или тестов.
- **Прогон записал пункт как провал**: «агент реализации не вернул результат (падение или
  таймаут)». Журнал при этом коммитом лёг, то есть агент успел записать работу и умер
  после. Вердикта приёмщика по пункту **нет вовсе** — скептик не запускался.
- **Что в журнале:** сплошной проход тремя заходами (скрипт по 29 спекам, чтение
  четырнадцати пар «действие → полная загрузка», хуки) с выводом «настоящих случаев
  „мутация → полная загрузка → проверка отсутствия“ — ноль». Вывод правдоподобен и
  согласуется с закрытым пунктом 1c, но **никем не перепроверен**, а сам автор e2e не
  гонял («правок нет, гонять шестнадцать минут нечего доказывать»).
- **Почему не ставлю:** условие отметки — приёмка приёмщиком. Её не было. Пункт закрывается
  дешёво: нужен один независимый проход по тому же скрипту.

### 2. Бэкенд: каждое событие триггерит уведомление — **ОТКЛОНЁН ДВАЖДЫ, ✅ НЕ СТАВЛЮ**

- **Коммиты:** `f5c999c` (**остался на ветке**), `dda4004` → откачен, `0a2cb06` → откачен.
- **Основание отклонения (перепроверено мной, а не принято со слов):**
  1. Пункт называется «**Бэкенд**: каждое событие триггерит уведомление». В `backend/` не
     изменено ни строки: `git show --name-only f5c999c dda4004 0a2cb06 | grep -c '^backend/'`
     → 0. Вся работа — мок-слой фронта.
  2. Триггеров семь из восьми: `grep -n "export function notify" mocks/notifications.ts` →
     `notifyOrderStatusChanged`, `notifyWarehouseReady`, `notifyPaymentReceived`,
     `notifyBatchReceived`, `notifyStockDeficit`, `notifySupplierResponse`,
     `notifyPaymentOverdue`. Восьмого (`reserve_expiring`) нет и породить не из чего — у
     `StockReservation` полей срока не заведено.
- **Оба отклонения по существу верны.** Но второе отклонение стоило дороже, чем сам пункт:
  см. §6.1 — на ветке остался живой регресс.

### 4e. `productName` копируется в партию и обрезок — **ОТКЛОНЁН, ✅ НЕ СТАВЛЮ**

- **Коммиты:** `f57bd29` (**остался на ветке**), `313850e` → откачен `ba50844`.
- **Основание отклонения (перепроверено):** пункт в разделе «Факты» называет три хранилища,
  где `prod-012` зовётся по-разному, и одно из них — `src/mocks/warehouse-batches.ts`. Поле
  снято, но вторая копия имени в том же файле жива: заголовки секций `// ── prod-XXX: …`
  (15 из 15 расходятся с каталогом) и ХРАНИМОЕ поле `notes` у 20 партий из 100
  (`whb-088` — «Rebar A500C» при каталожном «Стальной лист S235 2мм»). `notes` рисуется на
  карточке партии, то есть эффект, ради которого пункт заведён, остался на экране.
- **Отклонение справедливо, но откат — нет:** см. §6.2.

### 7. Заказ: выбор обрезков — **ОТКЛОНЁН, ✅ НЕ СТАВЛЮ**

- **Коммиты:** `7fdaa7f` (**остался на ветке**), `e4e1a45` → откачен `aa041c9`.
- **Основание отклонения (перепроверено мной по коду кончика ветки):** обрезок можно выбрать
  в строку, но **нельзя продать**. `planShipment` (`mocks/orders.ts:3038-3039`) пропускает
  любую аллокацию без партии — `const batch = allocation.batchId ? batchById(...) : undefined;
  if (!batch) continue` — а у аллокации под обрезок `batchId === null`. Строка, покрытая
  куском целиком, имеет `shippable = 0` навсегда; смешанная строка (кусок + партия) не
  отгружает вообще ничего и получает `SHIPMENT_EXCEEDS_STOCK` на количестве, которое сама же
  предложила менеджеру.
- Это **регрессия, внесённая пунктом**: до него `mockFifoAllocation` жёстко ставила
  `offcutId: null`, ни одна аллокация не могла быть без партии.

### 8. Клиент: страна и автоопределение типа комплекта — **ОТКЛОНЁН, откачен целиком, ✅ НЕ СТАВЛЮ**

- **Коммит:** `165d478` → откачен `0944d8b` (побайтно чисто). На ветке от пункта не осталось
  ничего: `frontend_vue/src/domain/countries.ts` отсутствует.
- **Основание отклонения** ([`followups-8.md`](followups-8.md), §«ОТКЛОНЕНО ПРИЁМКОЙ»): план
  требует «LT → Локальный, **иначе → Экспорт**», а правка завела третью ветку — страна пустая
  → не подставляем ничего. Поле страны на создании клиента необязательное (`validate()`
  проверяет только `name`, `email`, `companyCode`), а `documentType` в форме по умолчанию
  `'local'`, за ним `vatMode: 'standard'` — то есть немецкая фирма без страны получает 21 %
  НДС в экспортном заказе. Тест автора этот путь не проходил ни разу
  (`it.each(['local','export'])` сам заранее выставляет `documentType`).
- Приёмщик отметил также, что его первый полный прогон `clients.spec.ts` был красным
  (1 failed / 74 passed) и позеленел только на повторе. Коммит откачен, но **наблюдение о
  недетерминированности осталось** — см. §6.4.

### 10. Клиент: сводка выставленных счетов — **ОТКЛОНЁН, откачен целиком, ✅ НЕ СТАВЛЮ**

- **Коммит:** `749e1e7` → откачен `28871ce` (побайтно чисто).
- **Основание отклонения** — только в сообщении реверта: «панель врёт на демо-данных
  приложения (ORD-100/CL-045), 1380 евро клиентских денег в сводку не попадают».
- **Журнал пункта потерян.** `followups-10.md` (316 строк) был добавлен тем же коммитом и
  удалён ревертом; в каталоге `verify-runs/` его нет. Для пункта 8 ту же потерю заметили и
  вернули журнал отдельным коммитом (`f0c6ce1`) — для пункта 10 не заметили. Разбор
  приёмщика существует единственной фразой в сообщении реверта.

### 4b. Код единицы по-английски попадает в хранимые данные — **остаётся отклонённым, ✅ НЕ СТАВЛЮ**

Пункт был отклонён прошлой ночью с узким возвратом: «нужно утверждение, которое краснеет на
восстановленном дефекте». Проверено сегодня — утверждения по-прежнему нет:

```
$ grep -rn "priceUomId" frontend_vue/tests/
tests/e2e/mocks/warehouse.ts:52:  priceUomId: 'uom-kg'     # фикстура, не утверждение
```

Единственное попадание — значение в моке. Незакоммиченная работа, которую предыдущий
приёмщик видел в грязном дереве, на ветку так и не легла.

### Не тронуты

`4` (услуги: валюта и единица из справочника), `11` (BCC-подпись из настроек), `12`
(почтовый сервер), `13` (финансовый модуль ↔ оплаты заказа) — прогон до них не дошёл:
остановился после двух подряд провалов, последний — пункт 2.

---

## 5. Отметки ✅, поставленные приёмкой

В [`review-followups.md`](../../plans/general/review-followups.md) добавлено шесть отметок —
и ни одной сверх условия «принят приёмщиком **и** подтверждён зелёным полным e2e»:

| Пункт | Отметка | Основание |
|---|---|---|
| 2b | ✅ СДЕЛАНО 2026-08-27 | принят приёмщиком прошлой ночи; отметка тогда не была записана в файл |
| 4c | ✅ СДЕЛАНО 2026-08-27 | `72c2df7`, скептик принял, свип чистый |
| 4d | ✅ СДЕЛАНО 2026-08-27 | `b866c95` |
| 4f | ✅ СДЕЛАНО 2026-08-27 | `4324230` |
| 4g | ✅ СДЕЛАНО 2026-08-27 | `ad01d9a` |
| 9 | ✅ СДЕЛАНО 2026-08-27 | `8f56c69` + `8ee2bfb` |

Не поставлены: **1b** (приёмки не было), **2**, **4b**, **4e**, **7**, **8**, **10**
(отклонены), **4**, **11**, **12**, **13** (не тронуты).

---

## 6. Что решать человеку утром

### 6.1. На ветке живёт регресс уведомлений, который никто не заказывал

Пункт 2 отклонён правильно, а откат сделан не той половиной. На ветке остался `f5c999c`
(семь эмиттеров), но снято `dda4004`/`0a2cb06` (глушение эмиттеров на время сборки сида).
Проверено по коду кончика ветки:

- `emit()` (`mocks/notifications.ts:478`) не имеет никакой защиты от сида;
- `buildShowcaseOrder()` вызывается на верхнем уровне модуля (`mocks/orders.ts:4232`, хвост файла)
  и — по замыслу — идёт через настоящие эндпоинты, а те зовут `notifyPaymentReceived`
  (`orders.ts:3778`), `notifyStockDeficit` (`warehouse.ts:1523`) и другие.

Следствие, названное в сообщении откаченного коммита: **один только импорт мок-слоя пишет в
ленту три записи с меткой «сейчас»** (два платежа ORD-2026-100 и дефицит Steel Pipe 60x4) и
даёт +3 к непрочитанным на колокольчике при каждой загрузке страницы. До пункта 2 этого не
было. Ни один тест этого не ловит — потому и приёмка, и e2e зелёные.

Варианты: (а) вернуть `0a2cb06` (`git revert 3a7798b`) — приёмщик пункта 2 сам назвал его
предмет проверенным и честным; (б) откатить `f5c999c` целиком и вернуть пункт к нулю.
Мержить как есть — значит смержить регресс.

### 6.2. Пункт 4e: откачена подтверждённая починка, а не то, за что отклонили

Отклонение 4e — про имена в `notes` и заголовках сидов. Откачен же `313850e`, который к
`notes` отношения не имеет: он чинит прямой вход на страницу резки. Приёмщик в том же
разборе назвал этот дефект **реальным**, а тест на него — не декоративным.

Проверено на кончике ветки: `ensureProductNames()` в `useWarehouseCutting.ts` вызывается
только в `loadBatches()` (строка 87), а `selectBatch()` (строка 97) зовёт один `getBatch(id)`.
Вход по ссылке `?batchId=…` (её строит карточка партии — то есть путь «из истории» и
«после перезагрузки») идёт именно этой веткой, и справочник не приносит никто: в шапке
операции резки будет **прочерк вместо имени товара**. Вместе с `313850e` откачен и e2e-тест
на это (23 строки в `cutting.spec.ts`).

Вариант: `git revert ba50844` — вернуть починку, оставив пункт открытым по `notes`.

### 6.3. Пункт 7: на ветке половина, которая продать обрезок не даёт

`7fdaa7f` оставлен, `e4e1a45` откачен. В таком виде ветка держит выбор куска **без фильтра
занятости** (`mockGetOffcutOffers` фильтрует только по `status !== 'available'`,
`mockOffcutAllocations` — тем же одним условием; резервирование по куску есть, но
конкурирующая строка просто не получит холд и никакой ошибки) — и по-прежнему без отгрузки
(§4, пункт 7). Это худшее из двух состояний: и не работает, и не откачено.

Вариант: `git revert 7fdaa7f` до мержа — либо мержить, зная, что кусок в заказе выбрать
можно, продать нельзя, и строка, которая его назвала, застревает неотгруженной навсегда.

### 6.4. Мелочи, которые стоит записать

- **Журнал пункта 10 потерян** вместе с ревертом (§4). Единственный след разбора — сообщение
  `28871ce`. Стоит восстановить `followups-10.md` из `749e1e7` до того, как пункт возьмут снова.
- **Наблюдение о недетерминированности** `clients.spec.ts` (приёмщик пункта 8: первый полный
  прогон красный, повтор зелёный) и БАГ-24 из журнала 8 (недетерминированный e2e карточки
  заказа) — в bugs-file они есть, но пунктом плана не прикрыты. Мой полный прогон был
  зелёный с первого раза и `retries: 0`, то есть сегодня это не воспроизвелось.
- **Незакоммиченная работа ночи не потеряна, а спрятана в `git stash`** — семь записей,
  шесть из них от этой ветки. Существенны две:
  `stash@{1}` (08-27 23:39) — «недоделанный пункт 11 (BCC) — процесс Claude Code вышел 23:3x»:
  114 строк в `useBccRequest.ts`, правки `BccRequestPage.vue`, `i18n/admin/bcc.ts` и
  `bcc-request.spec.ts`, а **вместе с ними** — те же файлы пункта 2, что позже легли в
  `0a2cb06` и были откачены. То есть слепой `git stash pop` вернёт и незаконченный пункт 11,
  и откаченную половину пункта 2 одновременно.
  `stash@{0}` (23:51) — только 99 строк журнала `followups-2.md`.
  В приёмку ни одна из этих правок не входит: они не на ветке и ничем не проверены.

- **Прогон правил собственную механику трижды** (§3). Правки разумные, но означают, что
  правила ночи менялись по ходу; в частности, до `984a358` откат отклонённого пункта брал
  только последний коммит — отсюда и §6.1–6.3.

---

## 7. Итог

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Финальная приёмка: auto/followups-2026-08-27 @ cdfa7a0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Машинная приёмка   typecheck 0 · lint 0 · prettier 0 · unit 0
                   28 файлов / 603 теста passed
E2E                1017 passed, код возврата 0, падений 0, повторов 0 (retries: 0)
Коммитов в ветке   34 (30 — этой ночью)
Пунктов принято    5 этой ночью (4c, 4d, 4f, 4g, 9) + 1 задним числом (2b)
Пунктов отклонено  5 (2, 4e, 7, 8, 10) + 4b остаётся отклонённым с прошлой ночи
Без приёмки        1b — агент не вернул результат, скептик не запускался
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**Ветка технически зелёная — и это не то же самое, что «готова к мержу».** Три отклонённых
пункта оставили на ней свою первую половину: `f5c999c` (пункт 2), `f57bd29` (4e) и `7fdaa7f`
(7). Из них две половины несут живые дефекты, которых до этой ночи не было:

1. импорт мок-слоя пишет три «сейчас»-уведомления и +3 на колокольчик при каждой загрузке
   (§6.1) — потому что откатили именно ту половину, которую приёмщик признал верной;
2. строка заказа, покрытая обрезком, не отгружается никогда, а смешанная строка не
   отгружает вообще ничего (§6.3).

Плюс откачена **подтверждённая** починка прямого входа на страницу резки (§6.2): там теперь
прочерк вместо имени товара.

Ни один из трёх не виден ни машинной приёмке, ни e2e — тестов на них нет, и в этом вся суть:
зелёный прогон подтверждает отсутствие известных регрессов, а не отсутствие дефектов.

**Что решать человеку утром — по убыванию цены ошибки:**

1. **Вернуть `0a2cb06`** (`git revert 3a7798b`) или откатить `f5c999c` — иначе в `main`
   уезжает лента уведомлений, которая врёт при каждой загрузке.
2. **Вернуть `313850e`** (`git revert ba50844`) — починка реальна и подтверждена, отклонение
   4e было про другое.
3. **Решить судьбу `7fdaa7f`** — либо откатить до мержа, либо завести отдельным пунктом
   отгрузку строки с обрезком (в плане такого пункта нет).
4. **Восстановить `followups-10.md`** из `749e1e7` — журнал пункта 10 потерян ревертом.
5. **Разобрать `git stash`** — `stash@{1}` держит недоделанный пункт 11 вперемешку с
   откаченной половиной пункта 2 (§6.4).

Ветка не смержена и не запушена.

# Финальная приёмка ночного прогона по `review-followups.md` — 2026-08-28 (третий прогон суток)

Ветка `auto/followups-2026-08-28`, кончик `df29ff5`, база `19bfaba` (= кончик `main`).
Приёмщик кода не менял. Единственные правки от приёмки — отметки ✅ в плане (§5) и этот
отчёт. Ветка **не смержена и не запушена** — решение за человеком.

> **Почему у файла суффикс `-3`.** Имена `followups-run-2026-08-28.md` и
> `followups-run-2026-08-28-2.md` уже заняты отчётами двух предыдущих отрезков этих же
> суток (`8d7100e` и `20a55fc`). Затирать их нельзя, поэтому нынешний лёг рядом. Читать
> подряд: §6 предыдущего отчёта оставил человеку два открытых вопроса, и один из них эта
> ночь закрыла, а второй — нет (см. §6 здесь).

Итоги, заявленные прогоном: сделано 12, 13, 7; устарело 1b; провалено — нет; не тронуто —
нет; остановки не было.

---

## 0. Где гонялась приёмка

Рабочее дерево **чистое** — снято до начала, и повторно после каждого временного зонда и
каждой инверсии (§4):

```
$ git rev-parse HEAD          →  df29ff5
$ git status --porcelain      →  пусто
```

Портов 5173/5174 до запуска не слушал никто. Это существенно, а не педантизм:
`playwright.config.ts` держит `reuseExistingServer: !CI`, и живой чужой dev-сервер молча
подменил бы предмет проверки. Проверено не на слово, а по родству процессов — оба `vite`
поднял сам Playwright:

```
54496  sh -c playwright test              Fri Aug 28 15:04:26
54543  sh -c vite                         Fri Aug 28 15:04:27   (ppid → webServer Playwright)
54580  sh -c vite --port 5174 --strictPort  Fri Aug 28 15:04:28
5173 ← pid 54544,  5174 ← pid 54581
```

---

## 1. Полная машинная приёмка на кончике ветки

| Команда | Код возврата | Что вывела |
|---|---|---|
| `npm run typecheck` (`vue-tsc --noEmit`) | **0** | пусто |
| `npm run lint` (`eslint src/ tests/ *.ts --max-warnings=0`) | **0** | пусто |
| `npx prettier --check src/` | **0** | `All matched files use Prettier code style!` |
| `npm run test:unit` (`vitest run`) | **0** | `Test Files 35 passed (35)` · `Tests 721 passed (721)` |
| `cd backend && python3 -m unittest discover -s tests -t .` | **0** | `Ran 9 tests` · `OK` |

Коды возврата сняты сразу после каждой команды (`echo "..._EXIT=$?"`), а не по последней
строке вывода.

Рост относительно прошлого отчёта: 30 → 35 файлов, 646 → 721 проверка. Прибавка приходится
на `domain/receivable.spec.ts` и `services/mocks/finance-receivables.spec.ts` (пункт 13),
`services/mocks/notification-triggers.spec.ts` (пункт 13), новый блок «правка количества
строки, покрытой обрезком» в `offcut-order-pick.spec.ts` и проверку `shippable` в
`orders.spec.ts` (пункт 7), `types/mailConfigured.spec.ts` и `mocks/mail-settings.spec.ts`
(пункт 12). Бэкендовый набор из 9 проверок — новый целиком: до пункта 12 тестов в
`backend/` не было ни одного.

---

## 2. Полный e2e

```
$ cd frontend_vue && npm run test:e2e     # playwright test, весь набор

  1027 passed (17.0m)
E2E_EXIT=0
```

Вердикт снят **по коду возврата**, а не по последней строке, и подтверждён грепом по всему
файлу вывода (через `tail` вывод не пропускался ни разу):

```
$ grep -c "✓" e2e.log                                   → 1027
$ grep -acE " failed| flaky| skipped|✘|did not run" e2e.log → 0
```

Ни одной упавшей, флейкующей, пропущенной или ненайденной проверки. Базлайны снимков не
перезаписывались.

---

## 3. Коммиты прогона

Отрезок этой ночи — всё, что легло после `20a55fc` (отчёт предыдущей приёмки):

| Коммит | Пункт | Что в нём | Объём |
|---|---|---|---|
| `b038efe` | 1b | сплошной проход заново, разбором AST: случаев нет | 1 файл, только журнал |
| `34e33bd` | 12 | «одна транзакция, все адреса в BCC» — код на бэкенде и тест | 31 файл, +1743 −49 |
| `3609481` | 13 | реестр входящих сошёлся с карточкой заказа | 20 файлов, +1899 −762 |
| `04a7c40` | 7 | предложение отгрузки считает сам план; строка без обрезка снова уезжает | 20 файлов, +1579 −55 |
| `df29ff5` | 7 | неделимость обрезка закрыта в четвёртом месте — при правке количества | 8 файлов, +364 −30 |

Коммит на пункт соблюдён; пункт 7 закрывался двумя, потому что второй чинит дыру, которую
приёмка нашла рядом с первым. Ревертов в этом отрезке нет — в отличие от двух предыдущих,
где их было восемь.

---

## 4. Пункты по одному

### 4.1. Пункт 1b — сплошной проход по проверкам отсутствия · ПРИНЯТ (исчерпан)

**Коммит:** `b038efe` (кода не менял, только журнал).
**Заявление:** случаев «мутация → полная загрузка → проверка отсутствия» в
`frontend_vue/tests` нет.

**Чем проверено — не чтением журнала, а собственным проходом.** Скрипт автора воспроизвести
дословно нельзя: в журнале §4 приведена только половина, финальный обход спеков описан
прозой. Поэтому написан **независимый разбор AST** (`typescript` из `node_modules`,
скрипт в скретчпаде, репозиторий не тронут) с той же постановкой: мутация → `goto`/`reload`
→ утверждение об отсутствии, внутри одного теста, с раскрытием хелперов по транзитивному
замыканию и с учётом операторов `beforeEach`/`beforeAll` своей области.

```
tests parsed: 961   with full load: 583   absence assertions: 248
MUT -> NAV -> ABS hits: 8
MUT -> HIST -> ABS hits: 3
```

Числа сошлись с журналом (959 / 581 / 241 / 8 / 3) — расхождения в единицы объясняются
разными наборами матчеров, и это ровно то, о чём говорит §1 журнала: спор о счёте
неразрешим, решает покрытие. **Все восемь срабатываний — те же самые файлы и строки**, что
у автора, и ни одно не является случаем пункта. Два прочитаны мной целиком заново:

- `clients.spec.ts:668` — «мутация» это клик по `a.name-link`, то есть переход внутри SPA,
  ничего не меняющий; само утверждение (`not.toHaveCount(unfiltered)`) стоит после `fill`
  фильтра и говорит о фильтре, а не об исчезновении;
- `suppliers-list.spec.ts:586` — сохранённый вид лежит в `localStorage`, перезагрузка и
  есть предмет теста, а присутствие канбана утверждается строкой выше, так что ноль у
  таблицы не может быть истиной «страница пуста».

**Детектор проверен инверсией** (питфолл #68 — проверка, которая всегда печатает ноль).
Подложены три заведомо плохих образца и два законных:

| Образец | Пойман |
|---|---|
| A: `click` → `page.goto` → `toHaveCount(0)` | да |
| B: мутация внутри хелпера → `page.reload()` → `not.toBeVisible` | да |
| C: мутация в `beforeEach` → `navigateToAdmin` → `toBeHidden()` | да |
| D: законный порядок «загрузка → мутация → отсутствие» | нет (и правильно) |
| E: законный «свежая страница без мутации» | нет (и правильно) |

**Вердикт:** пункт исчерпан. Прошлый приёмщик отклонил первый проход по счёту утверждений
(«166 против 102») — этот счёт действительно ничего не решает, а покрытие теперь показано:
961 тестовый блок разобран, дыр не осталось. Отметка ✅ поставлена.

### 4.2. Пункт 12 — почтовый сервер в настройках · ПРИНЯТ

**Коммит:** `34e33bd` (третий заход; первые два откачены в предыдущие отрезки).

Пункт требует четырёх вещей. Проверены все четыре, каждая своим инструментом:

| Требование пункта | Чем проверено | Итог |
|---|---|---|
| раздел почтового сервера в общих настройках | `src/views/admin/settings/MailSettings.vue`, три маршрута мока (`GET`/`PATCH`/`POST /test`), 24 e2e-проверки в `settings.spec.ts` | есть |
| BCC-инструмент берёт параметры оттуда | `useBccRequest.ts:68-79` — `mailFrom` и `mailReady` читают `settings.mail` | есть |
| пароль не возвращается на фронт | `mockGetMail()` отдаёт `passwordSet: mailPassword.length > 0`, самого пароля в ответе нет; правило `isMailConfigured` живёт в одном месте (`src/types/settings.ts:167`), зовущих три | есть |
| «одна транзакция, все адреса в BCC» — кодом и тестом на бэкенде | `backend/app/modules/bcc/features/send_request/domain.py`: `transport.send_message(message)` ровно один раз, вне всякого цикла; адреса в `Bcc`, `To` — сам отправитель | есть |

**Собственная инверсия приёмщика — на главном заявлении пункта.** `send_bcc_request`
переписан на рассылку циклом (по письму на получателя), прогнан весь бэкендовый набор,
затем файл восстановлен из копии:

```
$ python3 -m unittest discover -s tests -t .
AssertionError: 'sales@beta.lv' not found in 'metals@alpha.lt'
Ran 9 tests · FAILED (failures=2) · exit=1
$ # восстановление
Ran 9 tests · OK · exit=0        $ git status --porcelain → пусто
```

Это ровно тот случай, ради которого пункт требует теста: рассылка циклом даёт те же письма
в тех же ящиках и отличается только числом транзакций. Теперь она краснеет — проверено
мной, а не пересказано из журнала.

Шаг внесён в гейт: `.github/workflows/ci.yml:51` — `backend unittest`, без зависимостей,
поэтому «отвалиться по установке» он не может.

**Что по этому пункту остаётся открытым — и почему это не основание для отказа:**
HTTP-слайса `POST /api/bcc/send` нет, записи в `bcc_events` нет, отправка ниоткуда не
зовётся. Пункт этого и не заказывал: он требует раздела настроек, чтения параметров
инструментом, нечитаемости пароля и правила «одна транзакция», проверенного тестом. Поднять
слайс целиком в этой машине нельзя средой, а не желанием — `fastapi`/`pydantic`/`sqlalchemy`
не установлены (`from app.main import app` → `ModuleNotFoundError`, так было и до правки).
Автор сказал это вслух в журнале, а не спрятал. Отметка ✅ поставлена.

### 4.3. Пункт 13 — финансовый модуль и оплаты заказа · ПРИНЯТ

**Коммит:** `3609481` (третий заход; два предыдущих откачены).

Прошлая приёмка отклонила пункт не за неверную правку, а за то, что **штатный путь
пользователя** оставлял оплату без ссылки на счёт: карточка считала деньги полученными, а
«Входящие» рисовали по тому же документу «Просрочен, оплачено 0.00». Проверено, что чинилось
именно это:

- `OrderCardPage.openPaymentModal` (`:752-768`) подставляет старейший непокрытый счёт
  (`nextUnsettledInvoice`) и предлагает его остаток суммой; выбор «без документа» остался,
  но перестал быть значением по умолчанию;
- «подставить остаток» пошло за выбранным документом (`paymentTargetOutstanding`), а не за
  остатком заказа — иначе расхождение возвращалось одним кликом;
- расчёт «сколько выставлено и сколько по этому пришло» стал один: `invoiceBalances` в
  `src/domain/receivable.ts`, зовут его реестр входящих (`orders.ts:4050`), сводка клиента
  (`:4565`) и карточка заказа (`OrderCardPage.vue:751`). Копий, которые уже разошлись на
  деньгах корректировки, больше нет.

**Собственная инверсия приёмщика.** `nextUnsettledInvoice` заставлен возвращать `null`,
прогнан весь юнит-набор, файл восстановлен:

```
Tests  4 failed | 717 passed (721)     exit=1
 FAIL  domain/receivable.spec.ts        > старейший из непокрытых
 FAIL  finance-receivables.spec.ts      > оплата штатным путём видна реестру тем же числом
 FAIL  finance-receivables.spec.ts      > частичная оплата — X из Y тем же расчётом
 FAIL  notification-triggers.spec.ts    > по счёту, закрытому штатным путём, колокольчик молчит
$ git status --porcelain → пусто
```

Четыре красных в трёх файлах — утверждения настоящие, а не декоративные.

**Собственный зонд по сидовому стору** (временная спека, удалена; сиды не трогались):

```
PROBE13 ordersWithInvoices=2  rows=4
PROBE13 ORD-2026-100 paidOrder=3380.00 paidRegistry=3500.00 unnamedIncoming=0.00 stillOwed=true
```

ORD-2026-009, на котором прошлая приёмка ловила расхождение в 22 256,26, больше не
расходится вовсе. Осталось одно расхождение — 120,00 на ORD-2026-100, и оно разобрано до
причины:

```
PAY  1500  invoiceId=ORD-100-INV-3   Advance against the proforma
PAY  2000  invoiceId=ORD-100-INV-1   Part payment on the first delivery
PAY  -120  invoiceId=null            Rebate returned to the client
```

Это **уход денег** (возврат скидки), не названный документом. Карточка вычитает его из
`paidAmount` (3380), реестр — представление над счетами — его не видит и показывает 3500 по
двум своим строкам. Автор это расхождение назвал сам (Л4 журнала: «осталось только на
осознанном возврате −120»), и оно согласуется с моделью самого пункта: строка реестра — это
счёт, а деньги, не названные счётом, ничьего долга не закрывают. Стража в
`finance-receivables.spec.ts:221` считает нарушителями только **пришедшие** деньги
(`p.amount > 0`), поэтому отрицательный платёж мимо неё проходит по замыслу, а не по
недосмотру.

**Это принято, но записано человеку как остаток**, а не спрятано: две страницы по одному
заказу называют разные суммы полученного, и разница видна глазами. Если решено, что возврат
обязан называть документ, — это отдельный пункт плана, а не недоделка этого. Отметка ✅
поставлена.

### 4.4. Пункт 7 — выбор обрезков в заказе · ПРИНЯТ

**Коммиты:** `04a7c40` (пятый заход) и `df29ff5` (шестой). Пункт отклонялся трижды, и оба
последних отказа были не за недоделку, а за дыру рядом.

Что требовал пункт и что проверено:

| Требование | Чем проверено | Итог |
|---|---|---|
| в добавлении позиции показываются свободные куски товара с размером, весом, партией-родителем и местом | `AddOrderItemsModal.vue:273-333, 777+`; e2e `order-offcuts.spec.ts` | есть |
| выбор куска пишет `offcutId` в аллокацию | `mockOffcutAllocations` (`warehouse.ts:1062`) | есть |
| рядом ссылка «создать обрезок» на экран резки | `AddOrderItemsModal.vue:783-789`, `:to="{ name: 'admin-warehouse-cutting' }"` под флагом `warehouseCutting` | есть |
| обрезки в автоматический FIFO не попадают | `mockFifoAllocation` по-прежнему проставляет `offcutId: null` (`warehouse.ts:1770`); кусок берут только руками | есть |
| кусок нельзя занять дважды | `takenOffcuts()` отсеивает занятые и в списке предложений, и в отказе на сохранении (`warehouse.ts:1019, 1066`) | есть |

**Регрессия, за которую отклоняли, закрыта.** Прошлая приёмка нашла, что новая
`offerableQuantity` перебирала только границы аллокаций и отдавала 0 строке **без единого
обрезка**, у которой свободно 793 из 801, — то есть удар приходился на основной поток.
Функция удалена целиком; предложение считает сам `planShipment` тем же проходом, который
решает по каждой аллокации, и отдаёт полем `offerable` (`orders.ts:3140-3210`);
`mockPlanOrderShipment` берёт `line.offerable` и ничего не вычитает задним числом
(`:3249`). Второго счёта рядом с первым нет — проверено грепом: `offerable` вычисляется в
одном месте, потребителей три, все читают.

**Дыра, найденная приёмкой во втором заходе, закрыта в шестом.** Правка количества строки
резала неделимый кусок пополам молча (`orderLineEdits.ts`, ветка `quantity`): дальше план
читал усечённую аллокацию как целый кусок, отгрузка списывала половину, а кусок помечался
`sold` целиком — металл исчезал без записи. Теперь отказ `QUANTITY_SPLITS_OFFCUT`, и
геометрия «где в количестве стоит кусок» вынесена в одну функцию `wholePieceRanges`
(`services/orderLines.ts:272`), которую зовут оба места — план и правка количества. Второго
экземпляра правила, за который пункт уже отклоняли, не заведено.

**Собственная инверсия приёмщика.** Отказ снят, прогнана спека, файл восстановлен:

```
Tests  2 failed | 23 passed (25)     exit=1
 FAIL  правка количества строки, покрытой обрезком > количество внутрь куска отклонено,
       и разбивка осталась прежней
 FAIL  правка количества строки, покрытой обрезком > после отказа кусок уезжает целым,
       а не половиной
$ git status --porcelain → пусто
```

Неделимость теперь стоит во всех четырёх местах, где меняется количество строки:
добавление (`OFFCUTS_EXCEED_QUANTITY`), правка количества (`QUANTITY_SPLITS_OFFCUT`),
отгрузка (`round2(whole - size) <= 0`), возврат (`RETURN_SPLITS_OFFCUT`). Отметка ✅
поставлена.

---

## 5. Отметки ✅, поставленные приёмкой

| Пункт | Отметка | Основание |
|---|---|---|
| 1b | ✅ СДЕЛАНО 2026-08-28 | сплошной проход воспроизведён независимым разбором AST, детектор проверен инверсией, случаев нет |
| 7 | ✅ СДЕЛАНО 2026-08-28 | все пять требований на месте, регрессия обычной отгрузки закрыта, неделимость в четвёртом месте, инверсия красная |
| 12 | ✅ СДЕЛАНО 2026-08-28 | все четыре требования на месте, инверсия «рассылка циклом» краснит бэкендовый набор |
| 13 | ✅ СДЕЛАНО 2026-08-28 | штатный путь называет документ, расчёт один, сид сошёлся, инверсия красит 4 проверки |

Провалённых и остановленных пунктов в этом отрезке нет — отмечать нечего.

---

## 6. Что решать человеку утром

### 6.1. ЗАКРЫТО: пункт 7 на ветке больше не половина

Прошлый отчёт (§6.4) писал, что от первого захода на ветке остался `7fdaa7f` — выбор куска
есть, фильтра занятости нет. Проверено на кончике: `mockGetOffcutOffers` теперь отсеивает и
по статусу, и по `takenOffcuts()`, а тот же набор проверяет отказ на сохранении
(`mockOffcutAllocations`). Пункт описывает ровно то, что на ветке.

### 6.2. ОСТАЛОСЬ: на ветке живёт регресс уведомлений от пункта 2

Ничего не изменилось со вчера, и это не входило в задание прогона. Откат пункта 2 сделан не
той половиной: на ветке остался `f5c999c` (семь эмиттеров), но снято `0a2cb06`/`dda4004`
(глушение эмиттеров на время сборки сида).

**Проверено мной зондом на кончике** (временная спека, удалена, `git status --porcelain`
пуст). Спека статически импортирует только `mocks/notifications`, а `mocks/orders`
подтягивает динамически — иначе замер был бы снят уже после эмиссии:

```
PROBE2 before total=20 unread=5
PROBE2 after  total=23
PROBE2 head notif-ev-003  2026-08-28T12:22:19.371Z  read=false
PROBE2 head notif-ev-002  2026-08-28T12:22:19.371Z  read=false
PROBE2 head notif-ev-001  2026-08-28T12:22:19.364Z  read=false
```

То есть **один только импорт мок-слоя заказов пишет в ленту три непрочитанных записи с
меткой «сейчас» и даёт +3 на колокольчик при каждой загрузке страницы.** `emit()`
(`mocks/notifications.ts:478`) не имеет защиты от сида, `buildShowcaseOrder()` вызывается на
верхнем уровне модуля (`mocks/orders.ts:4525`). Ни один тест этого не ловит — потому и
приёмка, и e2e зелёные.

Варианты прежние: (а) `git revert 3a7798b` — приёмщик пункта 2 сам назвал предмет этого
коммита проверенным и честным; (б) откатить `f5c999c` целиком.

### 6.3. К СВЕДЕНИЮ: расхождение в 120,00 на ORD-2026-100

Разобрано в §4.3. Не дефект пункта 13 и не основание для отказа, но две страницы по одному
заказу называют разные суммы полученного. Решать, обязан ли возврат называть документ, —
человеку; это отдельный пункт плана.

### 6.4. Мелочи, которые никуда не делись

- **`git stash` — семь записей**, шесть от этих веток. `stash@{1}` («недоделанный пункт 11»)
  устарел наполовину: пункт 11 сделан заново и принят (`c7693d6`), но в том же стэше лежат
  файлы откаченной половины пункта 2. Слепой `git stash pop` вернёт мёртвую версию поверх
  живой. Разбирать выборочно или выбросить.
- **Два чужих рабочих дерева зарегистрированы** в `git worktree list` — `scratchpad/gate`
  (`8d719d0`) и `scratchpad/wt-4b` (`c7e15c8`), оба от прошлых приёмок, оба в скретчпаде.
  На мерж не влияют, но `git worktree prune` их не уберёт, пока каталоги живы.

---

## 7. Итог

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Финальная приёмка · ветка auto/followups-2026-08-28 · кончик df29ff5
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Машинная приёмка:  typecheck 0 · lint 0 · prettier 0 · unit 0 (721/35)
                   backend unittest 0 (9)
E2E:               exit=0 · 1027 passed (17.0m) · failed/flaky/skipped — 0 строк
Пунктов принято:   4 из 4 (1b, 7, 12, 13) · отклонено 0 · остановки не было
Отметок ✅:        4
Инверсий приёмщика: 3 (цикл вместо транзакции · nextUnsettledInvoice → null ·
                    снят QUANTITY_SPLITS_OFFCUT) — все три красные, дерево восстановлено
Зондов приёмщика:  3 (сходимость реестра · лента уведомлений · разбор ORD-2026-100)
Открыто человеку:  1 регресс не из этого прогона (уведомления, §6.2) + 1 остаток (§6.3)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Ветка **не смержена и не запушена**. Дерево чистое, кончик `df29ff5`.

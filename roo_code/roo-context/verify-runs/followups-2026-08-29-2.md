# Пункт 2 — Страница резки: шесть расхождений разом

План: [`review-followups.md` §2](../../plans/general/review-followups.md).
Решение владельца от 2026-08-30: **берём все пять** (шестое закрыто пунктом 8).

---

## Что сделано

| № | Расхождение | Как закрыто |
|---|---|---|
| 1 | «Исходная партияИсходная партия» | панели передавали и `:title`, и слот `#header` со своим `panel-title`; в слоте осталась только кнопка |
| 2 | кнопка подписана текстом подсказки | свой ключ `cutting_change_batch` в трёх языках |
| 3 | текстовое поле без базового класса | **закрыто пунктом 8**, проверено в DOM |
| 4 | удаление строки текстовой кнопкой | `action-icon-btn action-danger` + `SvgIcon trash`, подпись уехала в подсказку |
| 5 | одиннадцать инлайновых `style=` | классы в `<style scoped>` страницы |
| 6 | нет пагинации у выбора партии | серверная постраничность: `usePagination` + общий `Pagination` |


Пагинация именно СЕРВЕРНАЯ, а не срез на клиенте: `getBatches` умеет постраничность с
самого начала, ею просто не пользовались — запрашивались первые пятьдесят и рисовались все
разом.

> **Поправка (приёмка, 2026-08-30).** В коммите и в первой версии этого журнала было
> написано, что партии за пятидесятой не существовало «в том числе для поиска». **Это
> неверно.** `mockGetBatches` (`mocks/warehouse.ts:579`) фильтрует по строке поиска ДО
> нарезки страницы (срез на 611), то есть поиск серверный и по номеру такая партия
> находилась и раньше. Ошибка была в рассказе, не в коде: на экране партии за пятидесятой
> действительно не было, но поиск её доставал.

Ключи i18n: паритет полный. **Поправка:** в первой версии журнала стояло 595 на язык — мой
счётчик считал регулярным выражением по отступу и часть ключей пропускал. Настоящее число —
600/600/600, проверено приёмкой. Вывод от этого не меняется, но число было названо неверно,
и это тот же класс ошибки, что «счёт по обрезанному выводу».

## Проверка в браузере

```
ВЫБОР: заголовков 1 («Исходная партия»), строк 10, пагинация «1-10 из 100», сырых ключей 0
РЕЗКА: заголовков 1, подпись кнопки «Сменить партию»,
       удаление = action-icon-btn action-danger, иконка есть, текста нет,
       инлайновых style в разметке 0
```

Единственный оставшийся инлайновый стиль на странице — `height` у `AutoResizeTextarea`:
компонент считает себе высоту сам, это работа, а не разметка. Он же с классом
`glass-input` — подтверждение, что пункт 8 закрыл расхождение 3.

## Инверсия — по каждому из пяти

| сломано | exit |
|---|---|
| контроль | 0 |
| вернуть двойной заголовок | 1 |
| вернуть подсказку подписью кнопки | 1 |
| вернуть текстовую кнопку удаления | 1 |
| вернуть инлайновый `style=` | 1 |
| убрать серверную постраничность | 1 |

---

# Две находки, добытые инверсией, и обе шире пункта

## 1. Мой собственный тест был питфоллом #68

Инверсия «убрать серверную постраничность» СНАЧАЛА НЕ ПОКРАСНЕЛА. Разбор:

```ts
await expect(rows.first()).not.toHaveText(firstOnPageOne)   // ❌ истинно всегда
```

`firstOnPageOne` снят через `innerText()` — с табами и переносами. `toHaveText` нормализует
пробелы. Такие строки не совпадут НИКОГДА, то есть `not.toHaveText` выполнялось при любом
коде. Тест был зелёным и на сломанной пагинации.

Замер под инверсией показал, что данные и правда не меняются:
`perPage=50`, счётчик «1-10 of 100», первая строка до и после клика одна и та же.

Исправлено на сравнение одинаковым способом с обеих сторон:

```ts
await expect.poll(async () => (await rows.first().innerText()).trim()).not.toBe(firstOnPageOne)
```

**Отдельный урок про сам приём инверсии:** первый заход давал ложную зелень ещё и потому,
что прогон запускался сразу после записи файла — dev-сервер отдавал старый модуль. Между
правкой и прогоном нужна пауза, иначе инверсия проверяет прежний код.

## 2. Скил учит неверному признаку — и по нему написан действующий тест

`vue-rules.md`, питфолл #64, приводил счётчик страниц «26-50 of 179» как признак
отрисованной страницы. Это неверно: `showingFrom`/`showingTo` в `usePagination.ts:12-13`
считаются из ЛОКАЛЬНОГО `page`, а не из ответа сервера.

Замер, снимки после клика «вперёд»:

```
T+   0мс  счётчик «11-20 из 100»  первая строка INV-2025-046  (СТАРАЯ)
T+ 100мс  счётчик «11-20 из 100»  первая строка INV-2025-046  (СТАРАЯ)
T+ 300мс  счётчик «11-20 из 100»  первая строка INV-2025-097  (новая)
```

Ожидание по счётчику возвращается со старыми строками — ловушка #64, замаскированная под
её же решение.

По этому совету написан `warehouse.spec.ts:273-277` — тест «every movement type is a
label». Он читает подписи ПРЕДЫДУЩЕЙ страницы, и последняя может не попасть в набор вовсе:
ровно та болезнь, о которой предупреждает его собственный комментарий двумя строками выше.

Чужой тест **не чинен молча** — заведён баг
[`pagination-counter-is-not-a-data-signal.md`](../../plans/bugs/pagination-counter-is-not-a-data-signal.md).
Скил исправлен: пример заменён, добавлено различие «величина из локального состояния» против
«величины из ответа» и предупреждение про `toHaveText` против `innerText()`.

## Приёмка

```
npm run verify   exit 0 — 732 юнита
полный e2e       1044 passed (15.9m)  exit=0
```

Отметку `✅ СДЕЛАНО` ставит приёмка, не автор.

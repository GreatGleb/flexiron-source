# Пункт 7 — обрезок можно выбрать в строке заказа

План: [`review-followups.md` § 7](../../plans/general/review-followups.md)
Прогон: 2026-08-27, автономный. Отметку ✅ в плане ставит не автор правки.

---

## 1. Воспроизведение

Пункт утверждает две вещи, и обе проверены грепом ДО единой правки.

**(а) В диалоге добавления позиций нет ни одного упоминания обрезков.**

```
$ grep -rn "offcut" -i src/views/admin/orders/
$ echo "exit=$?"
exit=0        # вывод пуст: ни одного совпадения во всём каталоге заказов
$ ls src/views/admin/orders/
AddLineModeChooser.vue  AddOrderItemsModal.vue  AddOrderServicesModal.vue
OrderCardPage.vue       OrderCreatePage.vue     OrdersListPage.vue
```

Пункт называл файлы `AddOrderItemsModal.vue` и `AddLineModeChooser.vue` — они лежат в
`src/views/admin/orders/`, а не в `src/components/admin/orders/` (такого каталога нет).
Совпадений ноль в обоих.

**(б) FIFO строится только из партий, `offcutId` жёстко `null`.**

```
$ grep -n "offcutId" src/services/mocks/warehouse.ts | head
…
1553:    offcutId: null,          ← внутри mockFifoAllocation
```

Прочитано целиком: `mockFifoAllocation` перебирает `batchesForProduct(productId)` и каждому
элементу `FifoBatch` проставляет `offcutId: null`. Обрезки в пул не попадают вовсе.

**Вывод:** пункт воспроизводится дословно. Обрезок в заказ не попадал ни одним путём.

---

## 2. Что сделано

Пункт принял решение сам («Разумно — только руками»), поэтому догадок здесь нет: обрезки в
автоматический FIFO НЕ попадают, а попадают выбором руками. Реализованы все четыре «Надо».

### Правило — одно и в одном месте

`src/services/orderLines.ts` → `offcutAllocation(offcut, batch)`. Что аллокация обрезка
устроена именно так, решает эта функция и только она:

- `batchId: null` — материал куска ушёл с партии в момент резки (`mockCreateOffcut` пишет
  движение `offcut`, а движение единственный владелец количества партии). Названная партия
  вычла бы его второй раз;
- количество — **материал куска в единице ПАРТИИ** через `resolveOffcutMaterial`, а не
  `offcut.quantity` (счётчик кусков, «1 шт»);
- себестоимость — цена партии-родителя за единицу;
- `null` — отказ, а не ноль: кусок, размер которого в единице партии невыразим, количества
  не имеет.

### Склад

- `mockGetOffcutOffers(productId)` — куски, которые строка может взять: только `available`,
  только этого товара, только выразимые. Собирает `OffcutOffer` через `offcutAllocation`;
- `mockOffcutAllocations(productId, ids)` — аллокации под выбранные куски, с поимёнными
  отказами `OFFCUT_NOT_FOUND` / `OFFCUT_PRODUCT_MISMATCH` / `OFFCUT_NOT_AVAILABLE` /
  `OFFCUT_SIZE_NOT_EXPRESSIBLE`;
- новый тип `OffcutOffer` и маршрут `GET /api/warehouse/offcuts/offers`. Отдельный тип и
  отдельный маршрут, потому что `OffcutListItem` не знает ни толщины, ни единицы партии, ни
  её цены — а без них кусок нельзя ни выразить в количестве строки, ни оценить. Считать это
  на клиенте значило бы завести вторую реализацию `offcutAllocation`.

Маршрут зарегистрирован **выше** `/offcuts/:id`: `/offcuts/offers` подходит под шаблон
карточки, и обобщённое совпадение увело бы запрос в «обрезок не найден».

### Заказ

- `POST /api/orders/:id/items` принимает `offcutIds`. Выбранные куски — это то, ЧЕМ строка
  уже покрыта; `coverFromStock` добирает остаток из партий FIFO, то есть кусок стоит первым
  и партией не подменяется;
- отказ `OFFCUTS_EXCEED_QUANTITY`, когда выбранных кусков больше, чем помещается в строку:
  обрезок неделим, и урезать его значило бы списать половину куска;
- отказ `OFFCUTS_WITH_BATCH` — партия целиком и отдельные куски это два разных ответа на
  один вопрос, и ниже победил бы прочитанный первым;
- `mockReserveOrder` держит именно кусок: `batchId: null`, `offcutId` — свой. Без этой
  ветки строка, покрытая обрезком, никогда не становилась бы «зарезервирована» целиком
  (`fullyReserved` считает по `reservedForLine`). `holdOnBatch` теперь ключуется парой
  (партия, кусок), иначе два разных обрезка (у обоих `batchId` пустой) слились бы в один
  хват. Для существующих вызовов это ноль изменений: во всех них `offcutId` был `null`.

### Экран

`AddOrderItemsModal.vue`: под каждой выбранной строкой — таблица доступных обрезков
(размеры, вес, партия-родитель ссылкой на её карточку, место хранения, сколько кусок
заберёт из строки). Выбор пишет `offcutIds` и **поднимает количество строки** под выбранные
куски, чтобы отказ сервера не прилетал на сохранении. Рядом — ссылка «Создать обрезок» на
экран резки (`admin-warehouse-cutting`), показывается только при включённом флаге
`warehouseCutting`: ссылка на маршрут, который отобьёт гвард, — мёртвая ссылка.

Список спрашивается только для товаров, уже положенных в заказ, и сбрасывается при каждом
открытии диалога — кусок мог уйти в другой заказ между двумя открытиями.

### Чего пункт НЕ просил и чего здесь нет

Отгрузки обрезка нет. `planShipment` пропускает аллокацию без партии (`if (!batch) continue`)
и сообщает недостачу — то есть отказа-падения не будет, но списать кусок с полки движением
`sale` сегодня нельзя. Это отдельное решение (списывать ли партию-родителя второй раз, как
кусок помечается проданным), и пункт 7 его не принимает. Записано здесь, чтобы никто не
принял «выбрать можно» за «продать можно до конца».

---

## 3. Файлы

```
frontend_vue/src/services/orderLines.ts                     offcutAllocation — правило
frontend_vue/src/types/warehouse.ts                         OffcutOffer
frontend_vue/src/services/warehouseService.ts               getOffcutOffers
frontend_vue/src/services/mocks/warehouse.ts                mockGetOffcutOffers, mockOffcutAllocations
frontend_vue/src/services/mocks/index.ts                    маршрут /offcuts/offers
frontend_vue/src/services/mocks/orders.ts                   offcutIds в POST /items, ветка резерва
frontend_vue/src/services/mocks/reservations.ts             reservedOnOffcut, ключ хвата
frontend_vue/src/services/ordersService.ts                  offcutIds в payload
frontend_vue/src/services/orderLineEdits.ts                 шесть кодов → сообщения
frontend_vue/src/composables/useOrderCard.ts                проброс offcutIds
frontend_vue/src/composables/useOrderCreate.ts              проброс offcutIds
frontend_vue/src/i18n/admin/orders.ts                       15 ключей × 3 локали
frontend_vue/src/views/admin/orders/AddOrderItemsModal.vue  выбор кусков + ссылка
frontend_vue/src/services/mocks/offcut-order-pick.spec.ts   новая спека (6 проверок)
frontend_vue/src/services/mocks/offcut-offers-route.spec.ts новая спека (2 проверки)
frontend_vue/tests/e2e/admin/orders/order-offcuts.spec.ts   новая e2e-спека (3 проверки)
roo_code/plans/orders/orders-backend-contract.md            §6 коды, §5 payload
roo_code/roo-context/03-api-contract.md                     GET /offcuts/offers
```

---

## 4. Машинная приёмка

```
$ cd frontend_vue && npm run verify
> typecheck · lint · dupes · format:check · test:unit
vue-tsc --noEmit                       — без вывода, exit 0
eslint src/ tests/ *.ts --max-warnings=0 — без вывода, exit 0
jscpd: Total 543 files, 682 clones, 18878 (9.32%) — порог 10 % в .jscpd.json
prettier --check src/ tests/           — All matched files use Prettier code style!
vitest run                             — Test Files 27 passed, Tests 588 passed (было 25 / 580)
exit=0
```

```
$ npm run test:audit
 Test Files  22 passed (22)
      Tests  97 passed (97)
```

**Приёмка нашла настоящее — дважды, и оба раза до коммита.**

1. `order-audit-authority-1` покраснел: `POST item quantity = -5` возвращал
   `OFFCUTS_EXCEED_QUANTITY` вместо отказа про количество. Причина: сумма пустого набора
   кусков — ноль, а `0 > -5` истинно. Проверка сужена до `chosen.length > 0 && …`;
2. `order-audit-contract-conformance` покраснел дважды: шесть новых кодов не были в §6
   контракта, и четыре из них были заведены в таблице сообщений, но «никем не бросаются»
   (они бросаются в `warehouse.ts`, которого нет в списке источников спеки). Обе половины
   закрыты записью раздела «Обрезки, выбранные руками» в §6.

Вне гейта:

```
$ npm run audit     → found 0 vulnerabilities
$ npm run deadcode  → Unused exports (58), Unused exported types (21)
                      база из verify.md — 59 и 22, то есть не выросло
$ npx vite build    → ✓ built in 8.14s, exit 0   (питфолл #67: шаблон компилируется)
```

---

## 5. Линзы

| Линза | Чем проверял | Что вернулось | Вывод |
|---|---|---|---|
| **Л1** реактивность | чтение всех новых `ref` в модалке | `Map`/`Set` заменяются целиком (`new Map(...)`), `watch({deep:true})` не добавлялся, `structuredClone`/`toRaw` не появились | чисто |
| **Л2** i18n | скрипт-счётчик по `src/i18n/admin/orders.ts` | все 15 ключей ровно по 3 вхождения (ru/en/lt), `@` в новых значениях нет | чисто |
| **Л3** контракт и HTTP | новая спека `offcut-offers-route.spec.ts` через сервисный слой + `test:audit` | 2 passed; conformance-спека сверяет §6 с тем, что бросает мок | чисто после правки контракта |
| **Л4** мок = правда | чтение `mockGetOffcutOffers` / `mockOffcutAllocations` | возвращают новые объекты, а не ссылку на `offcutStore`; русских строк в данные не добавлено | чисто |
| **Л5** один источник | `grep -rn "offcutId:" src/ \| grep -v null` и `grep -rn resolveOffcutMaterial src/` | аллокацию из обрезка строит ровно одно место — `orderLines.ts:242`; обе функции мока зовут его | чисто |
| **Л6** UI и CSS | чтение шаблона + `npx vite build` | `name-link` определён локально (#63), пустое состояние внутри карточки и не мигает при загрузке (#30, #52), брейкпоинт 600px (#29), `@click` однооператорный (#67), HTML-комментариев в шаблоне нет (#9) | чисто |
| **Л7** права, флаги, роутинг | `grep -n "admin-warehouse-cutting\|admin-warehouse-batch" src/router/index.ts` | обе строки есть; ссылка на резку под флагом `warehouseCutting`; новых флагов не заведено | чисто |
| **Л8** сохранение | чтение `save()` в `useOrderCard` | ошибка добавления строки идёт через `lineEditErrorKey`, невыведенная строка остаётся в `pendingItems`; clean-slate не нарушен — выбор кусков живёт локально до Save | чисто |
| **Л9** тесты | инверсия, 7 штук — см. ниже | каждое проверяемое поведение краснит СВОЙ тест | чисто |
| **Л10** целостность | `git status`, роутер и i18n-индекс не менялись | новых маршрутов и доменов переводов нет; `npx vite build` поднимается | чисто |

### Л9 — инверсия по каждому утверждению

Ломалось по одному, спека прогонялась, поведение возвращалось точной обратной заменой
(`git restore` запрещён и не применялся).

| Что сломано | exit | Покраснело |
|---|---|---|
| `mockGetOffcutOffers` перестал фильтровать по `status` | 1 | 4 теста, включая «предлагает только свободные куски» |
| `mockFifoAllocation` стал возвращать `offcutId` | 1 | «в автоматический FIFO обрезки не попадают» |
| выбранные куски выброшены из `coverFromStock` | 1 | «выбранный кусок становится аллокацией», «резервирование держит кусок» |
| снят отказ `OFFCUTS_EXCEED_QUANTITY` | 1 | «отказывает, когда куски не помещаются» |
| снят отказ `OFFCUT_PRODUCT_MISMATCH` | 1 | «отказывает куску чужого товара» |
| ветка обрезка убрана из `mockReserveOrder` | 1 | «резервирование держит именно этот кусок» |
| маршрут `/offcuts/offers` убран из диспетчера | 1 | обе проверки маршрута |

Ни один тест не остался зелёным на сломанном поведении.

---

## 6. E2E

Правка задела `src/services/mocks/index.ts` — общий пол по классификации `verify.md`,
поэтому уровень 2: полный набор, дважды на одном дереве.

Новая спека `tests/e2e/admin/orders/order-offcuts.spec.ts` — три проверки. Товар и номер
партии читаются с самой складской вкладки (питфолл #15: данные не зашиты), свободный кусок
узнаётся по кнопке «отметить использованным» — она рисуется только при статусе `available`.
Признак — строка про ИМЕННО ЭТОТ кусок, а не «панель появилась» (#64). Количество перед
выбором ставится в 0.01, чтобы подтягивание было видно любым куском: «стало не меньше
материала» устроило бы и бездействие (#68).

Инверсия по каждой из трёх (правка возвращалась точной обратной заменой):

| Что сломано | Покраснело |
|---|---|
| таблица кусков перестала рисоваться | 2 из 3 |
| снято подтягивание количества | «выбор куска поднимает количество» |
| убрана ссылка на резку | «рядом стоит ссылка на экран резки» |

Полные прогоны — код возврата снят сразу после команды, вердикт по строкам
`passed`/`failed`/`flaky`, а не по последней строке:

```
$ npx playwright test --workers=3 > p7-e2e-A.txt 2>&1; echo "exit=$?"
  1015 passed (17.7m)
exit=0
$ grep -cE " failed| flaky| skipped" p7-e2e-A.txt
0

$ npx playwright test --workers=3 > p7-e2e-B.txt 2>&1; echo "exit=$?"
  1015 passed (17.8m)
exit=0
```

Пара зелёная на одном дереве.

---

## 7. Итог

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Цикл проверок: пункт 7 — выбор обрезков в заказе
Итераций: 3 из 30        Вердикт: чистый свип
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Машинная приёмка:  typecheck OK · lint OK · dupes OK (9.32%) · format OK · unit OK (588)
                   test:audit OK (97) · e2e уровень 2 — 1015 passed ×2
Вне гейта:         npm run audit — 0 high · deadcode — 58 экспортов, 21 тип (база 59/22)
Линзы:             Л1–Л10 подтверждены
Найдено за прогон: 3       Починено: 3      Отклонено: 0
В bugs-file ушло:  0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Три находки — все свои и все от приёмки, не от чтения: отказ про количество, перекрытый
отказом про обрезки; шесть кодов вне §6 контракта; четыре кода с сообщением, но «никем не
бросаемые». Все три починены до коммита.

---

## Отклонено приёмкой — 2026-08-27

Приёмка прогнала пункт целиком и осталась зелёной: три буллета «Надо» на месте, инверсии
краснеют ровно на своих тестах. Пункт всё равно не закрыт: обрезок по-прежнему НЕЛЬЗЯ
ПРОДАТЬ, а строка, которая его назвала, перестала отгружаться вовсе.

> Раздел про вторую попытку («доступность обрезка», коммит `e4e1a45`) исчез из этого файла
> вместе с откатом самого коммита — журнал вернулся к состоянию первой попытки. Проверки,
> перечисленные ниже, относились к дереву на `e4e1a45`.

### Что сошлось (перепроверено приёмкой независимо, не со слов автора)

- Показ кусков товара в добавлении позиции — размер, вес, партия-родитель, место
  (`AddOrderItemsModal.vue:804-855`); ссылка «создать обрезок» на `/admin/warehouse/cutting`
  (`:783-790`, флаг `warehouseCutting = true`); выбор пишет `offcutId` в аллокацию; FIFO
  обрезки не берёт.
- `npm run verify` → exit 0 (591 unit) · `test:audit` → 97 · e2e `order-offcuts.spec.ts` →
  3 passed.
- Все три инверсии красят ровно свои тесты. Удаление строки и удаление заказа кусок
  возвращают. Вторая строка того же заказа и второй заказ получают `OFFCUT_NOT_AVAILABLE`.

### Основание отклонения

Проверено на чистом дереве коммита `e4e1a45`, мок-уровень, без резервирования.

**1. Строка, покрытая куском ЦЕЛИКОМ (`quantity = material`), имеет `shippable = 0`
навсегда.** Покрытая куском аллокация имеет `batchId = null`, а `planShipment`
(`orders.ts:3036`) пропускает любую аллокацию без партии — «goods that are on no shelf
cannot leave one» — и записывает её в `shortage`. Ни одного движения `'sale'` по куску не
пишется, статус куска остаётся `'available'`, в предложениях его больше нет: кусок заперт
вне рынка, а заказ по этой строке не завершается никогда. То есть требование ТЗ, ради
которого пункт и заведён («обрезки становятся доступны для продажи»), не выполнено — кусок
можно выбрать и зарезервировать, но не продать.

**2. Хуже: СМЕШАННАЯ строка (кусок 2 + партия 5 из FIFO, `quantity` 7) не отгружает
ВООБЩЕ НИЧЕГО**, хотя партийная половина на полке лежит. `mockPlanOrderShipment` обещает
`shippable = 5`, а `mockCreateShipment` на этих же 5 бросает `SHIPMENT_EXCEEDS_STOCK`
(`orders.ts:3111`): `splitAllocations` (`orderLines.ts:191`) отдаёт первыми 2 куска — их
пропускают — плюс 3 партии, `missing = 2`. Сломан заявленный в докблоке `planShipment`
(`orders.ts:3070-3071`) инвариант «Same planner as the shipment itself, so the dialog cannot
offer a quantity the write-off would then refuse». Путь пользовательский:
`OrderCardPage.vue:557` подставляет в поле именно `line.shippable` и ограничивает
`:max` им же (`:2344`) — менеджеру предлагают то количество, которое запись гарантированно
отклонит. Контроль: та же строка без куска (`quantity` 7) — план 7, отгрузка 7 проходит.

### Почему это не «вне пункта»

Автор объявил отгрузку вне пункта («пункт её не просит»), но это не «не реализовано», а
регрессия, внесённая пунктом 7: до него ни одна аллокация не могла иметь `offcutId` —
`mockFifoAllocation` жёстко ставит `offcutId: null` (`warehouse.ts:1614`, поле на `:1625`).
Отдельного пункта на отгрузку обрезка в `review-followups.md` нет — дыре некуда деться.

Ни одного теста на отгрузку строки с куском не добавлено: `offcut-order-pick.spec.ts` и
`order-offcuts.spec.ts` не упоминают отгрузку вовсе, а `orders.spec.ts` знает про `offcutId`
единственной строкой `offcutId: null` (`:706`). Поэтому оба прогона — e2e и unit — зелёные и
об этом ничего не знают.

### Что откачено

Откачен только `e4e1a45` (`git revert --no-edit`, без конфликтов). `7fdaa7f` — первая
попытка, которая и завела `offcutId` в аллокации, — оставлен в ветке: откат его не
заказывали. В таком виде ветка держит выбор куска БЕЗ фильтра занятости (находка второй
попытки вернулась) и по-прежнему без отгрузки. Судьбу `7fdaa7f` решает человек вместе с
судьбой пункта 7.

---

## Приёмка 2026-08-28 — отклонено, откачено полностью

Коммиты `654da1a` и `659a9e9` (третий и четвёртый заходы пункта 7) отклонены и откачены
целиком: `git revert --no-edit 659a9e9` → `38357eb`, затем `git revert --no-edit 654da1a` →
`8f3ed83`. Конфликтов не было, дерево чистое.

### Причина отклонения: регрессия в ОБЫЧНОЙ отгрузке

Новая `offerableQuantity` (`src/services/mocks/orders.ts:3182-3197`) перестала считать
`остаток − недостача` и вместо этого перебирает только ГРАНИЦЫ АЛЛОКАЦИЙ — накопительные
суммы `quantity` по разбивке. Если ни одна граница не принята планировщиком, функция отдаёт 0.
Но настоящий максимум почти никогда не лежит на границе: частично занятая партия обрезает его
ВНУТРИ своей аллокации.

Проба (временная спека, удалена; сид не трогался):
- prod-084, строка 801 шт, разбивка [whb-016: 796, whb-017: 5], `wholePieces: []` — куска в
  строке НЕТ;
- другой заказ резервирует 3 шт с whb-016 — рядовой сценарий «два заказа на один товар»;
- `mockPlanOrderShipment` → `shippable: 0`;
- при этом `mockCreateShipment(orderB, [{ quantity: 793 }])` тем же планировщиком проходит
  (ORD-101-SHP-1).

Последствие в UI прямое: `OrderCardPage.vue:557` `shipQty()` берёт `line.shippable` (0),
`:max="line.shippable"` = 0, `shipSelection` отфильтровывает нули, кнопка отгрузки погашена
через `:disabled="… || shipSelection.length === 0"`. Строку нельзя отгрузить ВООБЩЕ, хотя
793 из 801 свободны. До коммита та же проба даёт 798 — число тоже неверное, но менеджер мог
вписать 793 руками и отгрузить.

### Заявленный предел не совпадает с фактическим

Журнал §2.6 и текст задачи сужают дефект до «строка упирается в частично свободную партию И за
ней стоит кусок». В пробе куска нет (`wholePieces: []`) — хватает частично зарезервированной
первой партии. Значит удар приходится на заказы без обрезков, то есть на основной поток, а не
на край пункта 7.

### Тесты этого не ловят

662 unit + 97 audit зелёные. `shippable` для строки БЕЗ куска не утверждает ни одна спека —
есть только «куска нет на полке — предложено ноль».

### Что подтвердилось

Машинная приёмка зелёная; требования пункта 7 (показ кусков с размером/весом/партией/местом,
ссылка «создать обрезок», FIFO только руками) в коде есть; контракт и локали дописаны;
инверсии красят свои тесты. Основание отклонить — именно побочный ущерб за границами пункта.

---

## Заход пятый — 2026-08-28, работа над ошибками по разбору выше

Основание для работы: разбор приёмки 2026-08-28. Отклонили не за недоделку, а за
**регрессию в обычной отгрузке**, поэтому чистого листа здесь нет: то, что приёмщик
подтвердил независимо (показ кусков с размером/весом/партией-родителем/местом, ссылка
«создать обрезок», FIFO обрезки не берёт, отгрузка куска целиком, занятость куска,
контракт и локали, инверсии красят свои тесты), не переписывалось.

### 1. Воспроизведение

Код обоих откаченных заходов возвращён в дерево **обратными откатами**, а не переписан
заново:

```
$ git revert --no-commit --no-edit 8f3ed83     # возвращает 654da1a
$ git revert --no-commit --no-edit 38357eb     # возвращает 659a9e9
```

Оба раза единственный конфликт — сам этот журнал; разрешён в пользу текущей версии
(`git show HEAD:<путь> > <путь>`, чужие разделы не тронуты). Код слился без конфликтов.

Дефект на возвращённом дереве воспроизведён **пробой приёмщика дословно** (временная
спека `zz-probe.spec.ts`, удалена; сид не трогался):

```
батчи prod-084: [["whb-016",796],["whb-017",699]]
разбивка:       [["whb-016",null,796],["whb-017",null,5]]     ← wholePieces []: куска НЕТ
план:           {"remaining":801,"shippable":0,"wholePieces":[]}
```

То есть строка без единого обрезка, у которой свободно 793 из 801, предлагала ноль:
`OrderCardPage.vue` подставляет `line.shippable` в поле и в `:max`, `shipSelection`
отфильтровывает нули, кнопка гаснет — строку нельзя было отгрузить вовсе.

### 2. Что сделано — одна правка по существу

`offerableQuantity` (перебор границ аллокаций) удалена целиком. Вместо неё
`planShipment` считает предложение **тем же проходом**, который решает по каждой
аллокации, и отдаёт его полем `ShipmentPlanLine.offerable`:

- `at` — сколько количества уже позади; `offerable` — последняя точка, до которой
  списание доходит без недостачи; `blocked` — первая аллокация, прошедшая не целиком;
- партия: `if (!blocked) offerable = at + take`, и `if (take < size) blocked = true`;
- кусок: три условия «весь или никак» собраны в один `takeable`, дальше то же правило;
- `mockPlanOrderShipment` берёт `line.offerable` и больше ничего не вычитает.

Почему проходом, а не вторым перебором: настоящий максимум почти никогда не лежит на
границе разбивки — частично занятая партия обрезает его ВНУТРИ своей аллокации. Любой
второй счёт рядом с первым — второй экземпляр того же правила (Л5), а расхождение здесь
означает ровно то, за что отклонили: диалог называет число, которое запись отклонит.

На той же пробе после правки: `shippable: 793`, и `mockCreateShipment(793)` проходит
(`ORD-101-SHP-1`).

Побочно правкой закрыт и старый случай той же болезни, живший ДО пункта 7: строка вида
[оценка 3, партия 5] обещала 5, а запись отклоняла её целиком — за неразмещённым
префиксом до партии не добраться. Теперь она честно предлагает 0.

### 3. Тест, которого не было вовсе

`orders.spec.ts` → «offers what the write-off takes when the first batch is only partly
free». Строка БЕЗ куска (`allocations.every(a => a.offcutId === null)`, `wholePieces`
пуст), два батча в разбивке [10, 5→2], соперник резервирует 3 с первого. Утверждается
`shippable === 7`, что 9 («остаток минус недостача») запись отклоняет, и что 7 проходит.

Ни одно из двух неверных чисел тест не устраивает (#68): 0 и 9 оба краснят его.

### 4. Машинная приёмка

```
$ cd frontend_vue && npm run verify              exit=0
  vue-tsc --noEmit                               — без вывода
  eslint src/ tests/ *.ts --max-warnings=0       — без вывода
  jscpd                                          — Found 684 clones, 8.94 % (порог 10 %)
  prettier --check src/ tests/                   — All matched files use Prettier code style!
  vitest run                                     — Test Files 35 passed, Tests 718 passed

$ npm run test:audit                             exit=0
  Test Files 22 passed (22) · Tests 97 passed (97)

$ npx vite build                                 exit=0 — ✓ built in 8.38s   (питфолл #67)
```

Вне гейта:

```
$ npm run audit      → found 0 vulnerabilities
$ npm run deadcode   → Unused exports (56), Unused exported types (24)
```

База из `verify.md` (59/22) снята 2026-08-25, с тех пор в ветку легли пункты 8–13.
Экспортов стало меньше; из типов, заведённых этим пунктом, в списке нет ни `WholePieceRange`,
ни `OffcutStatus` — проверено грепом по выводу.

### 5. Линзы

| Линза | Чем проверял | Что вернулось | Вывод |
|---|---|---|---|
| **Л1** реактивность | `git diff` по `OrderCardPage.vue` | добавлены `computed` и две функции; ни `watch({deep:true})`, ни `structuredClone`, ни `toRaw` не появились | чисто |
| **Л2** i18n | счёт вхождений новых ключей в `src/i18n/admin/orders.ts` | `ship_qty_splits_offcut` — 3, `error_return_splits_offcut` — 3 (ru/en/lt); `@` в новых значениях — 0 | чисто |
| **Л3** контракт | `git diff` обоих контрактов + `order-audit-contract-conformance` в `test:audit` | формулировка `shippable` («число, которым план спросили») больше не соответствовала коду — переписана в обоих файлах на «наибольшее количество от начала строки, посчитанное тем же проходом»; conformance зелёная | находка, починена |
| **Л4** мок = правда | ледж-спеки `order-audit-ledger-reconcile` / `-orphans` | обе проверяют ПОСЫЛКУ пропуска (у каждого пропущенного движения есть `offcut` того же куска с той же партии) и непустоту пропущенного набора | чисто |
| **Л5** один источник | `grep -rn "offerable\|shippable" src/ --include=*.ts --include=*.vue` без спек | `offerable` считается ровно в одном месте — цикле `planShipment`; потребителей три, все читают, ни один не считает | чисто |
| **Л6** UI и CSS | `grep` классов + `npx vite build` | `.field-error` определён в `components/_forms.css`, который грузит `admin-core.scss`; `.cell-input.has-error` — в `orders_card.css`, импортируемом страницей; `--danger` есть в `erp-base.css` | чисто |
| **Л7** права, флаги, роутинг | `git diff HEAD -- src/config/featureFlags.ts tests/e2e/helpers/flags.ts src/router/index.ts` | пусто: ни флагов, ни маршрутов эта правка не заводит | чисто |
| **Л8** сохранение | чтение `confirmShipment` | отправка блокируется при `shipSplitsPiece`; ничего локального не теряется | чисто |
| **Л9** тесты | пять инверсий, см. ниже + `grep -rn "await page.waitForLoadState" tests/` (пусто) | каждое утверждение краснит свой тест | чисто |
| **Л10** целостность | `git diff --name-only` по роутеру и `src/i18n/`, `npx vite build` | роутер не менялся, из i18n — только `admin/orders.ts` (домен уже подключён); сборка зелёная | чисто |

### Л9 — инверсии

Ломалось по одному, поведение возвращалось точной обратной заменой (`git restore` запрещён
и не применялся; для страховки файл копировался в скретч).

| Что сломано | Покраснело |
|---|---|
| снято `if (take < size) blocked = true` — предложение проходит сквозь частично занятую партию | «offers what the write-off takes when the first batch is only partly free» (получено 12 вместо 7) |
| предложение сведено к границам аллокаций — та самая отклонённая регрессия | тот же тест (получено 0 вместо 7); проба приёмщика на prod-084 даёт `shippable: 0` |
| из `takeable` убрана проверка `shelvedOffcut` | «куска нет на полке — строка не отгружается», «куска нет на полке — предложено ноль» |
| из `takeable` убрана проверка `round2(whole - size) <= 0` | «на погрузке кусок не режут», «диалог не предлагает количество, которое разрежет кусок» |
| из `takeable` убрана проверка `reservedOnOffcut(...) === 0` | **никто** — 718 юнит-тестов зелёные |

Последняя строка записана как есть, а не спрятана. Ветка защитная и в живом приложении
недостижима: `takenOffcuts` не даёт второй строке назвать занятый кусок, а резерв на кусок
возникает только из разбивки, которая его назвала. Единственный путь развести две строки на
один кусок — `mockSplitOrderItem`, а его единственный вызов (`useOrderCard.ts:1559`) передаёт
`line.shippedQuantity`, то есть количество, которое отгрузка уже приняла, — а она куска не
режет. Ветка оставлена симметрии ради (у партии проверка чужого хвата есть) и помечена здесь
как непокрытая, а не объявлена проверенной.

### 6. E2E — уровень 2, пара на одном дереве

Правка задевает `src/services/mocks/orders.ts` и сиды движений — через них проходит
всякий тест, который открывает заказ или склад, поэтому уровень 2: полный набор дважды.
Код возврата снят сразу после команды, вердикт — по строкам `failed`/`flaky`/`skipped`,
а не по последней строке (`verify.md`).

```
$ npx playwright test --workers=3 --reporter=line > p7-e2e-A.txt 2>&1; echo "exit=$?"
  1027 passed (18.6m)
exit=0

$ npx playwright test --workers=3 --reporter=line > p7-e2e-B.txt 2>&1; echo "exit=$?"
  1027 passed (18.5m)
exit=0

$ grep -acE " failed| flaky| skipped" p7-e2e-A.txt p7-e2e-B.txt
0   0
```

Отдельно важное: сиды демо-заказов строят отгрузки через тот же `shippable`
(`createScenarioShipments`, `truck`), то есть правка меняет НАЧАЛЬНОЕ состояние всего
приложения. Ни один снимок и ни одно ожидание от этого не поехали — обе пары зелёные
на одном дереве, базлайны не перезаписывались.

### 7. Итог

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Цикл проверок: пункт 7 — обрезок можно выбрать И продать, отгрузка без обрезков цела
Итераций: 2 из 30        Вердикт: чистый свип
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Машинная приёмка:  typecheck OK · lint OK · dupes OK (8.94%) · format OK · unit OK (718)
                   test:audit OK (97) · vite build OK · e2e уровень 2 — 1027 passed ×2
Вне гейта:         npm run audit — 0 high · deadcode — 56 экспортов, 24 типа
Линзы:             Л1–Л10 подтверждены
Найдено за прогон: 1       Починено: 1      Отклонено: 0
В bugs-file ушло:  0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Находка своя и от линзы Л3: описание `shippable` в обоих контрактах осталось от
удалённой `offerableQuantity` («число, которым план спросили») и коду больше не
соответствовало. Переписано в обоих файлах.

Отметку ✅ в плане ставит не автор правки.

---

## Заход 3 — 2026-08-28. Четвёртое место неделимости: правка количества

Приёмка отклонила заход 2 не за неверную правку, а за **дыру рядом с ней**, введённую тем
же коммитом. Разбор приёмщика принят целиком и не перепроверялся заново там, где он
подтверждает верное: показ кусков в модалке, ссылка «создать обрезок», FIFO кусков не
берёт, `offerable` из `planShipment` — не тронуто ни строкой.

### 1. Воспроизведение — прогон, а не рассуждение

Временная спека (`src/services/mocks/zz-tmp-repro.spec.ts`, удалена после прогона; сиды не
трогались). Свежий кусок отрезан из настоящей партии, строка «кусок + хвост партии»:

```
$ npx vitest run src/services/mocks/zz-tmp-repro.spec.ts --reporter=verbose --silent=false
offer material 0.3 quantity 5.3
after add   : [["offcut-014",null,0.3],[null,"whb-010",5]]
after up    : [["offcut-014",null,0.3],[null,"whb-010",10]]
after down  : [["offcut-014",null,0.3],[null,"whb-010",1]]
after shrink below piece: [["offcut-014",null,0.15]]      ← половина неделимого куска, молча
plan: {"remaining":0.15,"shippable":0.15,"wholePieces":[{"from":0,"to":0.15}]}
piece status after shipping: sold
```

То есть правка количества резала кусок пополам без единого слова, а дальше план читал уже
УСЕЧЁННУЮ аллокацию и честно считал её целым куском: продажа списала 0.15, кусок помечен
`sold` целиком — 0.15 металла исчезали без записи.

Место — одно: [`orderLineEdits.ts`](../../../frontend_vue/src/services/orderLineEdits.ts),
ветка `quantity`:

```ts
if (isGoods(line)) line.allocations = splitAllocations(line.allocations, op.value).shipped
```

`splitAllocations` режет разбивку по числу и не спрашивает, попал ли разрез внутрь куска.
Три остальных места правило держали: `OFFCUTS_EXCEED_QUANTITY` при добавлении,
`round2(whole - size) <= 0` при отгрузке, `RETURN_SPLITS_OFFCUT` при возврате.

### 2. Правка

**Отказ, а не усечение** — то же решение, что в трёх соседних местах. Новый код
`QUANTITY_SPLITS_OFFCUT`.

Второй экземпляр правила заводить было нельзя — именно за это пункт отклоняли, — поэтому
геометрия «где в количестве стоит кусок» вынесена в ОДНУ функцию и оба места зовут её:

| файл | было | стало |
|---|---|---|
| `services/orderLines.ts` | — | `wholePieceRanges(allocations)` — экспорт, единственная реализация прохода |
| `services/mocks/orders.ts` | локальная `wholePieceRanges(item)` со своим циклом | `unshippedPieceRanges(item)` = та же функция по неотгруженному остатку |
| `services/orderLineEdits.ts` | резал молча | `splitsWholePiece(op.value, wholePieceRanges(line.allocations))` → отказ |

Разница между двумя вызовами — только в том, ПО КАКОЙ части разбивки спрашивают: план — по
неотгруженному остатку, правка количества — по всей разбивке (количество строки абсолютно,
и отгруженный префикс входит в него).

Отказ падает ДО первой записи: `applyLineEdit` бросает раньше присваивания `allocations`, а
`mockUpdateOrderItem` работает по копии и подменяет строку только после всех операций.

Границы намеренно не запрещены: количество ровно в размер куска оставляет кусок целым,
количество за ним режет только хвост партии. Убрать кусок из строки — пересобрать строку;
удаление строки возвращает кусок в продажу (тест на это уже был).

i18n: `orders.error_quantity_splits_offcut` в трёх локалях. Карточка ловит отказ сама —
`editLine` уже показывает `lineEditErrorKey(e)` тостом, а `onCellCommit` возвращает ячейке
то, что в строке действительно лежит. Кода в карточке не добавлено ни строки.

### 3. Контракт (линза Л3)

`QUANTITY_SPLITS_OFFCUT` внесён в §6 обоих контрактов, и рядом записано правило целиком:
неделимость обязана стоять во ВСЕХ ЧЕТЫРЁХ местах, где меняется количество строки —
добавление, правка количества, отгрузка, возврат; три из четырёх защитой не являются.

Проверено машиной, а не глазами: `order-audit-contract-conformance.spec.ts` покрывает три
направления сразу — брошенный код обязан быть в каталоге, код каталога обязан доходить до
человеческого сообщения, ни один код не подстрока другого. Первый прогон после правки был
КРАСНЫМ (`absent from §6: [ 'QUANTITY_SPLITS_OFFCUT' ]`) — то есть гейт настоящий.

### 4. Тесты и инверсии (линза Л9)

Три теста в `offcut-order-pick.spec.ts`, describe «правка количества строки, покрытой
обрезком». У каждого — своя посылка на данных (кусок стоит первым и занимает (0; material)),
чтобы тест не мог «пройти» на строке без куска.

Три инверсии, каждая своя, и краснеет ровно то, что должно:

| что сломано | покраснело |
|---|---|
| снят сам отказ (`QUANTITY_SPLITS_OFFCUT`) | 2 из 3 новых; третий (границы) зелёный — он про другое |
| разбивка не обрезается вовсе | только третий (границы) |
| общая `wholePieceRanges` возвращает `[]` | 3: два новых **и** «диалог не предлагает количество, которое разрежет кусок» |

Третья инверсия — доказательство Л5, а не Л9: она показывает, что вынос геометрии дал ОДИН
источник, а не копию. Сломав его, краснеют оба потребителя сразу.

### 5. Линзы

| Линза | Чем проверял | Что вернулось | Вывод |
|---|---|---|---|
| Л1 | `git diff --name-only` | ни одного `.vue`, ни одного композабла | неприменима, кода реактивности не тронуто |
| Л2 | подсчёт ключей по блокам локалей `orders.ts` | ru 439 · en 439 · lt 439, `missing: []`, новый ключ в трёх | чисто |
| Л2 | `grep '@'` в новых строках | 0 | чисто |
| Л3 | `npm run test:audit` (конформанс контракта) | 97 passed после правки контрактов, КРАСНЫЙ до неё | чисто |
| Л4 | правка мока = правка контракта, оба обновлены | — | чисто |
| Л5 | grep по геометрии кусков и по всем `.allocations =` | одна реализация прохода; единственное место, где разбивку УРЕЗАЮТ, — `applyLineEdit`; `coverFromStock`/`settleCostAfterQuantity` только дополняют | чисто |
| Л6 | шаблоны и CSS не менялись | — | неприменима |
| Л7 | права/флаги/роуты не менялись | — | неприменима |
| Л8 | отказ до первой записи, строка после отказа сверена в тесте | разбивка и количество не изменились | чисто — это и есть починка потери |
| Л9 | три инверсии выше | краснеет ровно ожидаемое | чисто |
| Л10 | `npm run verify`, `npx vite build` | exit 0 / exit 0 | чисто |

Отдельно рассмотрено и ОТКЛОНЕНО как ложное срабатывание Л5: карта `wholePieces` внутри
`planShipment` (orders.ts:3134) — это не вторая копия геометрии, а другой вопрос
(«сколько куска осталось неотгруженным»), заданный по срезу, а не по количеству. Приёмщик
подтвердил её как верную; трогать запрещено.

### 6. Приёмка

```
$ npm run verify        → exit 0 (typecheck · lint · dupes 8.93% · format · unit 721)
$ npm run test:audit    → exit 0 (97)
$ npx vite build        → exit 0
$ npm run audit         → 0 high
$ npm run deadcode      → 56 экспортов, 24 типа (база не выросла: новая функция используется)
```

E2E — уровень 1 по [`verify.md`](../../skills/verify.md): правка лежит в одной области
(мок заказов, сервисы строки, спека и переводы), общего пола не касается. Прогнан всё же
полный набор — он строго накрывает затронутый круг:

```
$ npx playwright test --workers=3 --reporter=line > e2e1.txt 2>&1; echo "exit=$?"
  1027 passed (19.0m)
exit=0
$ grep -acE " failed| flaky| skipped| did not run" e2e1.txt
0
```

Вердикт снят по коду возврата и по строкам `failed`/`flaky`/`skipped`, а не по одной
последней строке. Число тестов то же, что в заходе 2 (1027): новые проверки здесь юнитовые,
браузерных не добавлено. Базлайны снимков не перезаписывались.

Юнитов стало 721 против 718 — ровно три новых теста, ни один существующий не удалён.

### 7. Итог

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Цикл проверок: пункт 7, заход 3 — неделимость куска при правке количества
Итераций: 1 из 30        Вердикт: чистый свип
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Машинная приёмка:  typecheck OK · lint OK · dupes OK (8.93%) · format OK · unit OK (721)
                   test:audit OK (97) · vite build OK · e2e уровень 1 — 1027 passed
Вне гейта:         npm run audit — 0 high · deadcode — 56 экспортов, 24 типа
Линзы:             Л2, Л3, Л4, Л5, Л8, Л9, Л10 подтверждены; Л1, Л6, Л7 неприменимы
Найдено за прогон: 1       Починено: 1      Отклонено: 1 (карта wholePieces — не копия)
В bugs-file ушло:  0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Отметку ✅ в плане ставит не автор правки.

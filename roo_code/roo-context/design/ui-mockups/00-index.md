# UI Mockups Index

**Source:** `toDo/design/ui elements mockups png/`

PNG files are NOT copied to `roo-context/` (binary format). This index provides a text description of each mockup for AI context consumption. Reference the corresponding entries in [`01-ui-elements-map.md`](../01-ui-elements-map.md) for detailed DNA specs.

---

## 01 — Unit Switcher (`01_unit_switcher.png`)
**File:** `toDo/design/ui elements mockups png/01_unit_switcher.png`

A segmented control / toggle for switching between measurement units (mm, cm, m, inches). Used in product cards, warehouse operations, and sheet visualizer. Glassmorphism style with active state highlight.

---

## 02 — Color Palette (`02_color_palette.png`)
**File:** `toDo/design/ui elements mockups png/02_color_palette.png`

The Flexiron design system color palette: primary blue (#1890FF), semantic colors (green/success, red/danger, yellow/warning), neutral grays, and glassmorphism rgba values. Reference for all UI components.

---

## 03 — Input Fields (`03_input_fields.png`)
**File:** `toDo/design/ui elements mockups png/03_input_fields.png`

Standard input field styles: text, number, email, password, search. Glassmorphism backgrounds with white borders. States: default, focused (blue glow), error (red glow), disabled (reduced opacity).

---

## 04 — Nav Tabs (`04_nav_tabs.png`)
**File:** `toDo/design/ui elements mockups png/04_nav_tabs.png`

Tab navigation component for switching between related views (e.g., Table/Kanban on Suppliers List, different sections in Supplier Card). Active tab has blue underline + semi-transparent blue background.

---

## 05 — Tooltips & Context (`05_tooltips_context.png`)
**File:** `toDo/design/ui elements mockups png/05_tooltips_context.png`

Tooltip component with glassmorphism background, appearing on hover of icons, status badges, and truncated text. Also includes context menu patterns for right-click actions.

---

## 06 — Dashboard Cards (`06_dashboard_cards.png`)
**File:** `toDo/design/ui elements mockups png/06_dashboard_cards.png`

KPI metric cards for the Dashboard (05.3). Glassmorphism cards with: title (secondary text), large numeric value (white, bold), micro-trend arrow + percentage. 5 cards in a row: Warehouse Value, Debtor Debt, Weekly Sales, Active Orders, Deficit Items.

---

## 07 — Row Grouping / Batch Indicator (`07_row_grouping.png`)
**File:** `toDo/design/ui elements mockups png/07_row_grouping.png`

Table rows grouped by a common attribute (e.g., metal grade, supplier). Group header row with expand/collapse toggle. Batch indicator badge showing count of items in group.

---

## 08 — Nav Search (`08_nav_search.png`)
**File:** `toDo/design/ui elements mockups png/08_nav_search.png`

Global search component in the header. Glassmorphism input with search icon, dropdown results panel showing matches across modules (products, suppliers, orders, clients). Keyboard shortcut hint.

---

## 09 — Master Table (`09_master_table.png`)
**File:** `toDo/design/ui elements mockups png/09_master_table.png`

Primary data table component used across all list pages. Features: sortable columns, zebra striping, status badges, action buttons, row hover highlight, pagination. Glassmorphism header row.

---

## 10 — File Uploader (`10_file_uploader.png`)
**File:** `toDo/design/ui elements mockups png/10_file_uploader.png`

Drag-and-drop file upload zone with dashed border, upload icon, file list with progress bars, remove button. Used in supplier documents, product images, order attachments.

---

## 11 — Selection Controls (`11_selection_controls.png`)
**File:** `toDo/design/ui elements mockups png/11_selection_controls.png`

Checkbox, radio button, toggle switch components. Glassmorphism unchecked state (transparent with white border), filled accent blue for checked/active state.

---

## 12 — Status Indicators (`12_status_indicators.png`)
**File:** `toDo/design/ui elements mockups png/12_status_indicators.png`

Status badge components: Active (green), Suspended (yellow/gray), Blocked (red), Pending (blue), Draft (gray). Semi-transparent background with colored border. Used in supplier cards, orders, products.

---

## 13 — Typography System (`13_typography_system.png`)
**File:** `toDo/design/ui elements mockups png/13_typography_system.png`

Typography scale: headings (h1-h6), body text, captions, labels. Font family: system sans-serif for UI, JetBrains Mono for data/monospace. White text on glassmorphism backgrounds, secondary text at 65% opacity.

---

## 14 — Scanner Interface (`14_scanner_interface.png`)
**File:** `toDo/design/ui elements mockups png/14_scanner_interface.png`

Barcode/QR code scanner interface for warehouse operations. Camera viewport with scan overlay, manual input fallback, scanned items list with timestamps. Glassmorphism controls.

---

## 15 — Button System (`15_button_system.png`)
**File:** `toDo/design/ui elements mockups png/15_button_system.png`

Button variants: Primary (blue filled), Secondary (glassmorphism transparent), Success (green), Danger (red), Ghost (no background), Icon-only. Sizes: sm, md, lg. States: default, hover, active, disabled.

---

## 16 — Operational Modals (`16_operational_modals.png`)
**File:** `toDo/design/ui elements mockups png/16_operational_modals.png`

Modal/dialog component for confirmations, forms, and detailed views. Glassmorphism background with backdrop blur overlay. Header (title + close), body (content), footer (action buttons). Sizes: sm, md, lg, fullscreen.

---

## 17 — Pagination System (`17_pagination_system.png`)
**File:** `toDo/design/ui elements mockups png/17_pagination_system.png`

Pagination component for data tables: page numbers, prev/next buttons, page size selector, total count display. Glassmorphism style matching the design system.

---

## 18 — Production Stepper (`18_production_stepper.png`)
**File:** `toDo/design/ui elements mockups png/18_production_stepper.png`

Multi-step process indicator for production workflow. Horizontal stepper with numbered steps, active/completed/pending states, connecting lines. Used in order fulfillment and production tracking.

---

## 19 — Sheet Visualizer (`19_sheet_visualizer.png`)
**File:** `toDo/design/ui elements mockups png/19_sheet_visualizer.png`

Metal sheet visualizer for warehouse — graphical representation of metal sheets with dimensions, grade labels, cut marks, and remnant indicators. Interactive zoom and rotation.

---

## 20 — Sheet Detail View (`20_sheet_detail_view.png`)
**File:** `toDo/design/ui elements mockups png/20_sheet_detail_view.png`

Detailed view of a single metal sheet: dimensions, weight, grade, location (rack/bin), arrival date, cut history, linked orders. Glassmorphism card layout with data fields.

---

## 21 — Advanced Filters (`21_advanced_filters.png`)
**File:** `toDo/design/ui elements mockups png/21_advanced_filters.png`

Advanced filter panel with multiple filter criteria: text search, dropdown selects, date range picker, number range, multi-select tags. Apply/Clear buttons. Slide-out panel or dropdown overlay.

---

## 22 — Toast Notifications (`22_toast_notifications.png`)
**File:** `toDo/design/ui elements mockups png/22_toast_notifications.png`

Toast notification component: Success (green), Error (red), Warning (yellow), Info (blue). Glassmorphism background with icon, message text, close button, auto-dismiss timer. Positioned top-right.

---

## 23 — Sidebar Navigation (`23_sidebar_navigation.png`)
**File:** `toDo/design/ui elements mockups png/23_sidebar_navigation.png`

Sidebar navigation panel (05.2): logo at top, nav links with icons (Dashboard, Products, Warehouse, Sales/CRM, Supplying, Finance, Analytics), settings at bottom. Active state with blue left border + blue tint background.

---

## 24 — Icon System (`24_icon_system.png`)
**File:** `toDo/design/ui elements mockups png/24_icon_system.png`

Icon library for Flexiron: navigation icons, action icons (add, edit, delete, save), status icons, file type icons. SVG-based, white color with opacity variants for active/inactive states.

---

## 25 — Bulk Actions Bar (`25_bulk_actions.png`)
**File:** `toDo/design/ui elements mockups png/25_bulk_actions.png`

Bulk actions toolbar appearing when rows are selected in a table. Shows selected count + action buttons (Delete, Export, Change Status, Assign). Glassmorphism floating bar at bottom or fixed above table.

---

## 26 — Empty States (`26_empty_states.png`)
**File:** `toDo/design/ui elements mockups png/26_empty_states.png`

Empty state illustrations for pages with no data: illustration/icon, title, description text, CTA button (e.g., "Add first supplier"). Glassmorphism card style. Used across all list pages.

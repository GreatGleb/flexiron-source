# Журнал ночного прогона — 2026-09-12

Ветка: `fix/contract-bugs-2026-09-09` · дерево на старте чистое · гейт зелёный (`npm run verify`, exit 0).

## Замеры нулевой отметки

| замер | значение | команда |
|---|---:|---|
| `throw new Error(` в моках, всё | 210 | `grep -rc "throw new Error(" frontend_vue/src/services/mocks/*.ts \| awk -F: '{s+=$2} END{print s}'` |
| то же без `*.spec.ts` | **196** | `grep -ro "throw new Error(" frontend_vue/src/services/mocks/ \| grep -v '\.spec\.ts' \| wc -l` |
| решений владельца (уникальных П) | 75 | `grep -oE '^#+ *П[0-9]+' roo_code/plans/api/audit/00-решения-владельца.md \| grep -oE 'П[0-9]+' \| sort -u \| wc -l` |
| оплачиваемых токенов, база | 296 315 811 | `python3 /tmp/night-queue-briefs/расход.py` |
| cache_read, база | 10 690 046 778 | там же |
| оплачиваемых за 7 суток | 65 923 187 | `python3 /tmp/night-queue-briefs/неделя.py` |

Потолок ночи: **41 000 000** оплачиваемых токенов сверх базы 296 315 811, то есть стоп на
337 315 811. Расход считается разностью с базой, не по часам.

Дорожкам ниже раздаётся число **196**, а не 210: разница в 14 бросков сидит в спеках.

## Задачи

| задача | итог | доказательство | коммит |
|---|---|---|---|
| Преднастройка (гейт, замеры, журнал) | ✅ | `npm run verify` exit 0; 210 / 196 / 75 сошлись с ожиданием | — |
| класс 3: мок молчит вместо отказа | ✅ | pre-commit чисто (prettier+eslint+typecheck); спека `mocks/unknown-id-is-refused.spec.ts` | a563fba |
| закрыть дыры сторожа кодов | ✅ | pre-commit чисто; `apiErrorCode.guard.spec.ts` | fcf5c51 |
| сравнение кодов подстрокой → равенство | ✅ | pre-commit чисто; `apiErrorCode.spec.ts` | 3eaf11b |
- перетриаж группы В · закоммичено · 12 доменных bugs-файлов + перетриаж-группы-В.md · d193c52
- незаведённые дефекты делт · закоммичено · 7 доменных bugs-файлов дополнены · 987a58e
- дубль триажа · закоммичено · триаж.md удалён, ссылки в README/передаче/contractrefs/очереди правлены · 0754e17
- 13 битых ссылок · закоммичено · 12 файлов контракта и аудитов · 98c0fbb
- П33 в соглашения · закоммичено · 00-conventions.md + config.md, сняты четыре «не решено» · e04f553
- П34 settings.md · закоммичено · settings.md сведён, две врезки в warehouse.md сняты · 823fee2
- П30 uploads.md · закоммичено · uploads.md + правка 00-conventions.md · 9c3d098
- сквозное: rights · закоммичено · roo_code/plans/general/сквозное-rights-план.md · 0a9fc5e
- сквозное: tenancy · закоммичено · roo_code/plans/general/сквозное-tenancy-план.md · 318e1f7
- сквозное: audit-log · закоммичено · roo_code/plans/general/сквозное-audit-log-план.md · 829d244
- сквозное: notifications · закоммичено · roo_code/plans/general/сквозное-notifications-план.md · df929c4
- сквозное: idempotency · закоммичено · roo_code/plans/general/сквозное-idempotency-план.md · 8fe0abb
- сквозное: custom-fields · закоммичено · roo_code/plans/general/сквозное-custom-fields-план.md · 55315eb
- сводка сквозного · закоммичена · roo_code/plans/general/сквозное-сводка.md (8185 байт) · 19a33c7
- класс 4: null в query литералом · закоммичено · frontend_vue/src/services/api.ts + apiQuery.spec.ts · 4957cd3 (гейт перепроверен после коммита соседа)
- mocks/index.ts на ApiRequestError · закоммичено · frontend_vue/src/services/mocks/index.ts · b330a19 (там же)
- закрыть дыры сторожа кодов · закоммичено · frontend_vue/src/services/apiErrorCode.guard.ts + .spec.ts (pre-commit гейт чист) · 487fe04
- сравнение кодов подстрокой → равенство · закоммичено · roo_code/plans/bugs/contract-sync-conventions-bugs.md · db4407e
- перетриаж группы В · закоммичено · перетриаж-группы-В.md + contract-sync-uploads-bugs.md + contract-sync-services-bugs.md · d5a8eb4
- незаведённые дефекты делт · закоммичено · contract-sync-conventions-bugs.md (+63 строки) · a523990
- сквозное: rights · закоммичено · roo_code/plans/general/сквозное-rights-план.md · 738b39f
- сквозное: tenancy · закоммичено · roo_code/plans/general/сквозное-tenancy-план.md · 1db261f
- сквозное: audit-log · закоммичено · roo_code/plans/general/сквозное-audit-log-план.md · 5b2eeb1
- сквозное: notifications · закоммичено · roo_code/plans/general/сквозное-notifications-план.md · 39454dc
- сквозное: idempotency · закоммичено · сквозное-idempotency-план.md + roo_code/plans/services/services-backend-plan.md · 47bba83
- сквозное: custom-fields · закоммичено · roo_code/plans/general/сквозное-custom-fields-план.md · fe5f1fb
- сводка сквозного · сделано · roo_code/plans/general/сквозное-сводка.md обновлена и закоммичена · 74b23f9

---

# Итог прогона

**Гейт зелёный.** `cd frontend_vue && npm run verify` — exit 0: typecheck чист, eslint чист
(`--max-warnings=0`), prettier чист, jscpd без превышения порога, unit — **48 файлов, 938 тестов
прошло, 2 пропущено**. Строка контракта: `[контракт] сведено доменов: 17 · описано эндпоинтов:
175 из 175 · спроектировано впрок: 1`.

Но в том же выводе стоит строка, которую гейт не считает провалом и которую нельзя замолчать:
`[ссылки] документов 67 · ссылок 15522 · битых 824`. Ночью починены **13** битых ссылок
(коммит `98c0fbb`) — это 1,6 % от числа. Остальные 824 остались; резолвер их печатает, но не
краснеет. Чинить их ночь не бралась и не планировала.

**Дерево не чистое.** `git status --short` на момент отчёта:

- `M roo_code/plans/services/services-backend-plan.md`
- 15 непроиндексированных файлов `roo_code/plans/<домен>/<домен>-backend-plan.md`
  (analytics, audit-feed, auth, bcc, categories, clients, config, finance, orders, products,
  services, settings, suppliers, uploads, warehouse)

Это **не работа человека** — это остатки провалившейся фазы доменных планов (см. ниже). Файлы
написаны агентами прогона, скептиком не пропущены и потому не закоммичены. Трогать их руками до
разбора не нужно; удалять — тоже: в них 129 вопросов к владельцу, собранных ниже.

## Что сделано, по дорожкам очереди

**Дорожка «мок-долг» — 5 из 5.**

| задача | коммит |
|---|---|
| класс 3: мок молчит вместо отказа | `a563fba` |
| класс 4: `null` в query литералом | `4957cd3` |
| `mocks/index.ts` на `ApiRequestError` (13 мест) | `b330a19` |
| закрыть дыры сторожа кодов | `fcf5c51`, `487fe04` |
| сравнение кодов подстрокой → равенство | `3eaf11b`, `db4407e` |

**Дорожка «учёт» — 4 из 4.**

| задача | коммит |
|---|---|
| перетриаж группы В (12 доменных bugs-файлов) | `d193c52`, `d5a8eb4` |
| незаведённые дефекты дельт | `987a58e`, `a523990` |
| дубль триажа устранён (`триаж.md` удалён, ссылки правлены) | `0754e17` |
| 13 битых ссылок в контракте и аудитах | `98c0fbb` |

**Дорожка «контракт» — 3 из 3.**

| задача | коммит |
|---|---|
| П33 в соглашения, сняты четыре «не решено» в config | `e04f553` |
| П34: `settings.md` сведён, две врезки в `warehouse.md` сняты | `823fee2` |
| П30: закрыт открытый вопрос `uploads.md` | `9c3d098` |

**Дорожка «сквозные планы» — 6 из 6 плюс сводка.**

| сквозная тема | коммиты |
|---|---|
| rights | `0a9fc5e`, `738b39f` |
| tenancy | `318e1f7`, `1db261f` |
| audit-log | `829d244`, `5b2eeb1` |
| notifications | `df929c4`, `39454dc` |
| idempotency | `8fe0abb`, `47bba83` |
| custom-fields | `55315eb`, `fe5f1fb` |
| сводка сквозного (`roo_code/plans/general/сквозное-сводка.md`) | `19a33c7`, `74b23f9` |

Плюс инфраструктурный коммит `9b5ceda` — ворота пятичасового окна сессии: ночи отдано 60 %
окна, остальное оставлено человеку. Именно эти ворота ночь и уперлась (раздел «Расход»).

## Что провалено

**Фаза доменных планов — 15 из 15 провалены.** analytics, audit-feed, auth, bcc, categories,
clients, config, finance, orders, products, services, settings, suppliers, uploads, warehouse.

Каждый план **написан** (262–701 строки, 42–83 КБ на файл, лежат на диске непроиндексированными)
и каждый **отклонён скептиком**, потому и не закоммичен: правило фазы — план без прошедшего
скептика хуже отсутствия плана.

**Причину отказа назвать поимённо не могу, и это отдельный дефект прогона.** Скрипт
`night-queue.js` пишет вердикт скептика только в консоль (`log()`), файла прогона не ведёт, а
в структуру, отданную финалу, попадает лишь `status`, без поля `notes`. Реплика скептика
по каждому из пятнадцати доменов утеряна вместе с консолью. Выдумывать за него причину я не стал.

Что удалось перемерить самому, уже после факта (это не вердикт скептика, а независимый замер):
планы содержательны и по формальным линзам скептика не пусты — ссылка на сквозную сводку стоит
в каждом (3–8 раз), раздел «Требует решения владельца» есть в каждом. То есть отказ был не по
«план пустой», а по чему-то предметному, и разбирать его придётся заново.

**Две задачи фазы вообще не взяты — по потолку расхода:** `notifications`, `sales-crm`.
Это не «сделали всё»: два домена из семнадцати даже не начинали.

**Фаза «закрытие сквозного» не выполнялась** — до неё очередь не дошла.

**Раскрой общего контекста признан нечистым** (`раскройЧист: false`). Один изъян брифов остался
незакрытым: в `группа-В.md` таблица объявлена «дословным вырезом `триаж.md` строк 83–115 с
изъятыми двумя строками», но правка оказалась не только изъятием — шапка столбца переписана с
«ждёт строки» на «ждёт строки (устарело)»; про изъятие сказано, про правку шапки — нет. Данные
при этом дословны: все 29 строк совпали с источником побайтово.

## Что осталось человеку

**1. Пункт 2 очереди — подтверждение класса 2.** Прогон его не делает и сделать не может:
подтверждение требует человека, который посмотрит на поведение, а не тест, который его выведет.
Материалы лежат готовыми: `вердикт-скептика-класс-2.md`, `вердикт-скептика-класс-2-заход-2.md`,
`передача-класс-2.md`, `найдено-не-заведено-класс-2.md`.

**2. Пункты 18–19 (реализация) в прогон не входили** — ни строки бэкенда ночью не написано.
Ночь занималась контрактом, планами и мок-долгом.

**3. Пятнадцать доменных планов ждут повторного суда.** Файлы на диске, скептик их отклонил,
причина утеряна. Дешевле всего — прогнать по каждому шесть проверок скептика из `night-queue.js`
(счёт нереализованных против контракта, покрытие решений П, разделы «Обязанности сервера» и
«Чего в домене нет», шесть сквозных правил ссылкой на сводку, резолвер ссылок, догадка вместо
решения владельца) и либо принять, либо переписать названное.

**4. Сто двадцать девять вопросов владельцу — одним списком.** Ниже.

## Вопросы владельцу — 129 штук, подряд

Собраны из разделов «Требует решения владельца» пятнадцати доменных планов, чтобы отвечать
подряд, а не искать по файлам. Номер в скобках — номер вопроса внутри своего плана; полная
формулировка с доказательствами — там же.

### analytics — 6 · `roo_code/plans/analytics/analytics-backend-plan.md` §7
1. (7.1) Чем сервер подписывает величину — ключом перевода, кодом справочника или ничем.
2. (7.2) Кому принадлежит порог, превращающий число в вердикт (`trend`, `ageStatus`, `statusType`, `ratingType`, `errorType`).
3. (7.3) По какому принципу строится P&L — по оплате или по отгрузке.
4. (7.4) Что сервер отвечает на значение `page`, которого нет в перечне.
5. (7.5) Остаются ли отменённые страницы (`supply`, `logistics`) в реестре фич.
6. (7.6) Нужна ли отметка среза `asOf` там, где срез один, и возвращается ли она всегда.

### audit-feed — 7 · `roo_code/plans/audit-feed/audit-feed-backend-plan.md` §10
7. (В1) Два человека с одинаковым отображаемым именем сливаются в одну строку фильтра авторов — разделять и чем ключевать, или признать слияние законным.
8. (В2) Ставить ли защитный потолок `pageSize` и какой (П20 снял его как настройку).
9. (В3) Состав ролей по умолчанию для права «открыть ленту».
10. (В4) Видит ли пользователь в ленте строки о сущностях, на которые у него нет права `read`.
11. (В5) Чем заполнять `user_name_translations` при одноязычном имени — всеми тремя ключами или языком автора.
12. (В6) `ORDER_AUDIT_ENTRY_NOT_FOUND` содержит `AUDIT_ENTRY_NOT_FOUND` подстрокой вопреки §2 — переименовать заказный код или снять запрет.
13. (В7) Форма колонок `old_value`/`new_value` под П39 — JSONB со значением и типом или отдельная колонка вида.

### auth — 11 · `roo_code/plans/auth/auth-backend-plan.md`
14. Чем вход узнаёт арендатора (email уникален только парой «арендатор + email»).
15. Точное значение тайм-аута бездействия (П29 даёт вилку 15–30 минут, в код идёт одно число).
16. Сверяется ли CSRF (от ответа зависит существование слайса С7).
17. Кто отдаёт матрицу прав эндпоинтами — `auth` или `config`.
18. Чем сеять справочники новому арендатору (П25 говорит «миграцией», арендатор рождается не миграцией).
19. Остаётся ли `role` в ответах `login`/`register`/`me` после перехода на многоролевость.
20. Включаем ли лимит попыток входа (три ключа объявлены и не читаются).
21. Что значит `tenant_id = NULL` — superadmin или что-то другое.
22. Срок жизни ссылки-приглашения (сегодня секретная ссылка не истекает).
23. Судьба `auth-secret-link-plan.md`.
24. Каким кодом отвечать по живой сессии деактивированного пользователя.

### bcc — 6 · `roo_code/plans/bcc/bcc-backend-plan.md`
25. Что происходит с историей BCC при удалении поставщика (схема уносит каскадом).
26. В какой валюте хранится принятая цена.
27. Где живёт повтор отправки и кто его нажимает (П48).
28. Попадает ли перечень источников запроса на страницу настроек (П19).
29. Хранит ли сервер локаль отправленного письма.
30. Нужен ли инструменту свой эндпоинт журнала аудита (П36).

### categories — 7 · `roo_code/plans/categories/categories-backend-plan.md`
31. (В1) Считает ли `productCount` товары поддерева или только свои.
32. (В2) Требует ли смена родителя категории того же четырёхзначного кода подтверждения.
33. (В3) Что делает `DELETE` категории, чьи поля заполнены у товаров.
34. (В4) По какому языку сервер сортирует список категорий (П64).
35. (В5) Подтягивает ли поиск предков найденной категории.
36. (В6) Обязан ли товар видеть два одноимённых поля, если предок и потомок назвали поле одинаково.
37. (В7) Чем клиент называет убранное определение, чтобы вернуть его в набор (П73).

### clients — 7 · `roo_code/plans/clients/clients-backend-plan.md`
38. Чем сервер отвечает на поле вне белого списка `PATCH`.
39. Предел выдачи сводки счетов (ответ не пагинирован и растёт без границы).
40. Чем наполнять журнал у операции, меняющей несколько полей разом (П36).
41. Судьба взаимодействий при удалении клиента в свете П74.
42. Остаётся ли перечень типов взаимодействия в коде (П26).
43. Судьба `rejectionReason` у самой записи взаимодействия (П74).
44. Поиск по списку клиентов на сервере — по каким полям и как.

### config — 7 · `roo_code/plans/config/config-backend-plan.md` §7
45. (В-1) Какой признак «этого удалять нельзя» настоящий — колонка `is_builtin` или префикс `f-custom-`.
46. (В-2) Что делать с осиротевшими строками матрицы и нужна ли `permission_items.name_translations`.
47. (В-3) Какой из трёх путей «назвать сущность» верен (форма запроса `POST /api/config/sections`).
48. (В-4) Что происходит со значением при смене типа определения.
49. (В-5) Считается ли дублем совпадение имени только в одном языке.
50. (В-6) Страна поставщика (П67) — новый седьмой тип поля или `enum` с 249 вариантами.
51. (В-7) Читает ли админка реестр фич с сервера и каким эндпоинтом (П13; без ответа П65(б) неисполним).

### finance — 9 · `roo_code/plans/finance/finance-backend-plan.md`
52. Кто и по какому событию создаёт счёт поставщика, меняет его статус и удаляет запись.
53. Кто владеет справочником статусов платежа — настройки арендатора или код.
54. Кому принадлежит перечень типов документа архива.
55. Остаётся ли `url` хранимой колонкой у `payment_documents` и `document_archive_items`.
56. Кто наполняет архив документов (путей записи нет ни одного).
57. `counterparty_id` — FK на поставщика или свободная строка.
58. Неизвестный `fileId` в `PATCH` — отказ или тихая заготовка.
59. Чем сервер обнаруживает просрочку, если чтение больше не пишет (§10).
60. Какой из двух кодов уступает — `NOT_FOUND` или `PAYMENT_NOT_FOUND` (§2).

### orders — 6 · `roo_code/plans/orders/orders-backend-plan.md`
61. (В-1) Что делать с выпущенной корректировкой, оплаченной платежом, который удаляют.
62. (В-2) Платёж заказа и `finance_payments` — одна запись или две.
63. (В-3) Распространяется ли П43 (атомарный Save) на страницу создания заказа.
64. (В-4) Срок хранения записей журнала заказа (П38 оставил открытым).
65. (В-5) Кому принадлежит размер страницы списка заказов и три умолчания нового заказа (П19, П20).
66. (В-6) Считается ли «рабочий день» срока брони по календарю арендатора (П52).

### products — 7 · `roo_code/plans/products/products-backend-plan.md`
67. Уникален ли `sku` в пределах арендатора.
68. Форма валюты на проводе — идентификатор, код или и то и другое.
69. Средние цены при партиях в разных валютах (`avgCostPriceFor` складывает разные валюты).
70. Возврат из архива — П44 назвал дорогу туда, обратную не назвал.
71. Что у товара чувствительно и вырезается у роли без права видеть себестоимость.
72. Кто автор записи журнала товара (сегодня отображаемое имя, а не идентификатор).
73. Адресат правила §2 «ни один код не является подстрокой другого» — сквозной план соглашений, не владелец (требует подтверждения адресата).

### services — 7 · `roo_code/plans/services/services-backend-plan.md`
74. На каком языке сервер сортирует список услуг по имени.
75. Как на проводе выражается стирание `name` и `description`.
76. Уникально ли имя услуги внутри арендатора.
77. Нужен ли услугам свой вид сущности журнала (П36).
78. Что отвечает повторный `DELETE` уже архивной услуги.
79. Есть ли путь обратно из архива (§22 описывает только дорогу туда).
80. Какое выражение умолчания валюты главное — флаг или явное поле настроек.

### settings — 13 · `roo_code/plans/settings/settings-backend-plan.md` §8
81. (В1) Кто создаёт системные статусы заказа после первичного сида и может ли арендатор добавить свой системный.
82. (В2) Проверяет ли сервер границы финансовых констант и какие они.
83. (В3) Остаётся ли `constants.defaultCurrency` в ответе как производное.
84. (В4) Кто удаляет файл заменённой карты склада (П65(а)).
85. (В5) «Рабочий день» срока брони (П52) — производственный календарь страны или просто пн–пт.
86. (В6) Право видеть код подтверждения (П73) — новый вид элемента матрицы или элемент на поле.
87. (В7) Заводится ли право на вкладку настроек или одно право на весь раздел.
88. (В8) Часовой пояс компании (П62) — обязателен ли и что делать с пустым.
89. (В9) Список пользователей (П58) отдаёт `settings` или `auth`.
90. (В10) Уникально ли имя валюты, единицы и статуса внутри арендатора — или уникален только код.
91. (В11) Что сервер отвечает на запрос к выключенной фиче — 404, 403 или пустой ответ.
92. (В12) Чем именуется системный статус заказа на сервере и что делает статус системным.
93. (В13) Уезжают ли условия оплаты поставщика в настройки вместе с отсрочкой платежа клиента (П32).

### suppliers — 10 · `roo_code/plans/suppliers/suppliers-backend-plan.md`
94. `hasDeficit` — что значит «у поставщика дефицит».
95. П68 против П72 на `lastBccDate` — считать при чтении или хранить колонкой.
96. Набор колонок серверного экспорта и заголовок ответа (П65(в)).
97. Справочный вызов с `pageSize: 999` — оставить или завести отдельный эндпоинт справочника.
98. Категории поставщика при включённом `categorySupplierLinks` (П26).
99. `Supplier.country` рядом с `SupplierAddress.country` (П67 сделал кодом оба).
100. Форма правки заметок (П35): заметки становятся записями, а Save шлёт дельту.
101. Связан ли `statusReason` с быстрой сменой статуса.
102. Уникальность поставщика внутри арендатора.
103. Срок хранения записи журнала поставщика.

### uploads — 7 · `roo_code/plans/uploads/uploads-backend-plan.md`
104. На чём проверять право загрузки, если эндпоинт не знает сущности (П15).
105. Может ли один файл принадлежать двум сущностям.
106. Какой код отдавать пользователю без арендатора (сегодня 404 `NOT_FOUND`).
107. Оставляет ли след сама загрузка, без привязки (П36).
108. uploads против П72 — копировать колонкой и обновлять событием или нет.
109. Срок подписанной ссылки и письма BCC (П11 задал ~15 минут; вложения BCC живут дольше).
110. Чем запускать уборщик черновиков (планировщика в проекте нет, `lifespan` пуст).

### warehouse — 19 · `roo_code/plans/warehouse/warehouse-backend-plan.md` §7
111. Какие колонки в каждой из пяти выгрузок CSV.
112. Что делать с `purchaseOrderId` нехватки, который правится, но задаётся негде.
113. Как выразить на проводе `exceptLine` и `claimed` FIFO-стоимости.
114. Какие значения верны при ручном создании нехватки (БАГ-26).
115. Какой источник главнее для `status` куска — клиентский `PATCH` или серверное правило.
116. Что происходит с партией при удалении обрезка, чей материал списан движением (БАГ-19).
117. Что считается «рабочим днём» у срока брони (П52) и учитывать ли праздники арендатора.
118. Кто автор записи журнала, когда операцию выполнил не человек, а правило (П57, каскад).
119. Видит ли себестоимость партии кладовщик (П18 решил форму, не право).
120. Уникален ли номер партии внутри арендатора.
121. Что делает `DELETE /api/warehouse/movements/:movementId` и что при этом с партией.
122. Удаление поставщика молча стирает происхождение партии (`SET NULL`) — так и задумано (П44 про поставщика молчит).
123. Обратная половина П57 (порог опустили — нехватка закрывается пересчётом): подтвердить или отменить.
124. Снимок ли `supplierName` на партии и `productName` на нехватке — или соединение при чтении (П72).
125. Кто владеет перечнем `referenceType` движения и какой словарь главный (БАГ-13).
126. Что источник истины для четырёх чужих полей, правимых через `PATCH /api/warehouse/stock/:productId`.
127. Остаётся ли таблица `stock_items` вообще (П68 против производности восьми из четырнадцати полей).
128. Куда девается бронь, когда её срок истёк (П52) — снимается молча, с уведомлением или ждёт человека.
129. Обязан ли склад отдавать в отказ `RESTRICT` от `settings` число ссылающихся партий.

## Расход

| | |
|---|---:|
| оплачиваемых токенов за ночь | **30 957 119** |
| потолок ночи | 41 000 000 |
| доля потолка | **75 %** |
| база на старте | 296 315 811 |
| замер на финале | 327 272 930 |

Формула: `python3 /tmp/night-queue-briefs/расход.py` → первая строка минус база 296 315 811.

**Не взято из-за потолка: `notifications` и `sales-crm`** — два домена фазы доменных планов
даже не начинались. Плюс фаза «закрытие сквозного» не выполнялась. Это не «сделали всё»: потолок
и пятичасовое окно сессии обрезали объём. Окно на финале — 17 101 130 при разрешённых ночи
9 750 000 (60 % от 16 250 000), то есть окно ночь выела и вышла за свою долю; именно оно, а не
потолок токенов, было узким местом последних волн.

**cache_read за ночь: 796 439 807.** В потолок он не входит, но по замеру 2026-09-04 он примерно
в 26 раз больше оплачиваемых — и здесь соотношение то же (796 млн против 31 млн, ×25,7).
Владельцу это число надо видеть, даже если счёт по нему не идёт.

**Просьба владельцу: сверьте 30 957 119 с `/usage` и скажите, сошлось ли.** Пока сверки не было,
потолок 41 000 000 стоит на локальном замере по `.jsonl`-транскриптам, а не на настоящем лимите
аккаунта. Если локальный замер врёт — врёт и потолок, и все решения «взять/не взять домен»,
принятые ночью, приняты по неверному числу.
план: products · закоммичен · roo_code/plans/products/products-backend-plan.md · 2f25c01

## Доменные планы — приняты с описью 2026-09-13

- 16 планов в roo_code/plans/<домен>/<домен>-backend-plan.md · products принят скептиком (2f25c01), остальные 15 — с открытыми находками (faf6016)
- Опись открытых находок: roo_code/fix-run-2026-09-09/открытые-находки-доменных-планов.md
- sales-crm плана не имеет: до домена не дошёл ни один прогон
- Сходимость кругов: 4.3 → 2.6 → 2.5 находки на план; между 2 и 3 встала


---

# ЗАХОД 2026-09-13 (вечер) — журнал подготовки

Ниже — журнал, который агент подготовки записал ПОВЕРХ этого файла в 18:28.
Содержимое сохранено, старая часть восстановлена из git (коммит 711cd5d).
Это дефект прогона: подготовка перезаписывает журнал вместо дописывания.


Ветка: `fix/contract-bugs-2026-09-09` (содержит `main`, дерево на старте чистое).
Машинная приёмка до работы: `cd frontend_vue && npm run verify` — **зелёная** (exit 0,
48 файлов, 938 passed | 2 skipped; `[контракт] сведено доменов: 17 · 175 из 175`).

## Замеры на старте

| замер | команда | ожидание очереди | факт |
|---|---|---:|---:|
| `throw new Error(` в моках, всё | `grep -rc … mocks/*.ts` | 210 | **218** |
| то же без `*.spec.ts` | то же + `grep -v '\.spec\.ts'` | 196 | **204** |
| разница (сидит в спеках) | — | 14 | **14** |
| уникальных П в файле решений | `grep -oE '^#+ *П[0-9]+' … \| sort -u \| wc -l` | 75 | **75** |

Мок-долг раздаётся доменам по числу **204**, не 196 и не 218. Расхождение с очередью —
плюс 8 бросков, появившихся после замера 2026-09-11; структура (14 в спеках) не изменилась.

## Счётчик расхода

Потолок ночи: **16 994 500** оплачиваемых токенов (8,29 % от 205 000 000).
Нулевая отметка (все транскрипты, включая `subagents/agent-*.jsonl`):

- оплачиваемые (input + cache_creation + output): **363 597 268**
- cache_read: **12 185 339 789**

Расход ночи считается разностью с этими числами, не по часам.
Для справки: за последние 7 суток оплачиваемых — **133 204 897**.

## Таблица работ

| задача | итог | доказательство | коммит |
|---|---|---|---|
| Преднастройка (дерево, ветка, гейт, замеры, счётчик) | ✅ | этот файл | — |
| Закрыть дыры сторожа кодов (мок-долг) | ✅ | pre-commit зелёный: prettier + eslint + typecheck; apiErrorCode.guard.spec.ts обновлён | ee6c588 |
| Перетриаж группы В | ✅ | contract-sync-bcc-bugs.md + перетриаж-группы-В.md | b1e7ffc |
| Незаведённые дефекты дельт | ✅ | contract-sync-uploads-bugs.md | 691215e |
| 13 битых ссылок | ✅ | audit/warehouse.md, categories-bugs.md, warehouse-bugs.md | ff2c864 |
| П34: settings.md и две врезки в warehouse | ✅ | api/warehouse.md — владелец назван, врезки сняты | bce8dbd |

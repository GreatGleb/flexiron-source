# Журнал ночного прогона — 2026-09-12

Ветка: `fix/contract-bugs-2026-09-09` (содержит `main`, дерево на старте чистое).
Машинная приёмка до работы: `cd frontend_vue && npm run verify` → **EXIT=0**,
46 файлов спек, 901 тест пройден, 2 пропущено; `[контракт] сведено доменов: 17 ·
описано эндпоинтов: 175 из 175`.

Замеры на старте (команды — из задания, результат совпал с ожиданием 2026-09-11):

| замер | команда | значение |
|---|---|---|
| `throw new Error(` в моках | `grep -rc "throw new Error(" frontend_vue/src/services/mocks/*.ts \| awk -F: '{s+=$2} END{print s}'` | **210** |
| уникальных П в решениях владельца | `grep -oE '^#+ *П[0-9]+' roo_code/plans/api/audit/00-решения-владельца.md \| grep -oE 'П[0-9]+' \| sort -u \| wc -l` | **75** |

Счётчик расхода: `/tmp/night-queue-briefs/расход.py` (считает и субагентов —
`<сессия>/subagents/agent-*.jsonl`).
Нулевая отметка ночи: оплачиваемых токенов **283 246 687**, cache_read **10 457 258 060**.
За последние 7 суток на момент старта — **52 855 898** оплачиваемых токенов.
Потолок ночи: **32 000 000** оплачиваемых токенов (50 % от 64 000 000), считается
как разность с нулевой отметкой.

---

## Задачи

| задача | итог | доказательство | коммит |
|---|---|---|---|
| Пред-полётные проверки (дерево, ветка, приёмка, замеры, счётчик) | ✅ | `git status --short` пусто; `npm run verify` EXIT=0; замеры 210 / 75 | — |

# Журнал ночного прогона — 2026-09-12

Ветка: `fix/contract-bugs-2026-09-09` · дерево при старте чистое · гейт `npm run verify` зелёный (exit 0).

## Замеры при старте

| замер | команда | факт | ожидание очереди |
|---|---|---:|---:|
| `throw new Error(` в моках, включая спеки | `grep -rc … mocks/*.ts` | 210 | 210 |
| то же, **без** `*.spec.ts` — столько раздаётся доменам | `grep -ro … \| grep -v '\.spec\.ts' \| wc -l` | 196 | 196 |
| уникальных решений владельца (П) | `grep -oE '^#+ *П[0-9]+' … \| sort -u \| wc -l` | 75 | 75 |

Все три совпали с очередью. Домены считают по **196**, не по 210.

## Счётчик расхода

Потолок ночи: **41 000 000** оплачиваемых токенов (20 % от 205 000 000).

| | значение |
|---|---:|
| нулевая отметка, оплачиваемые (`input + cache_creation + output`) | 295 159 798 |
| нулевая отметка, `cache_read` | 10 658 534 641 |
| за последние 7 суток, оплачиваемые | 64 767 706 |

Расход ночи считается разностью с нулевой отметкой, не по часам.
Счётчик: `/tmp/night-queue-briefs/расход.py` (обходит и субагентов в `<сессия>/subagents/agent-*.jsonl`).

## Задачи

| задача | итог | доказательство | коммит |
|---|---|---|---|
| Преднастройка (проверки 1–9) | ✅ пройдена | см. замеры выше | — |

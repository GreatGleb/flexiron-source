# Прогон починки багов сверки контракта — 2026-09-09

Папка заведена, чтобы работа по починке кода **не трогала существующие файлы `roo_code/`**:
пока владелец принимает решения, их правит другая живая сессия, и общий файл — это
гарантированный конфликт.

> **Состояние на 2026-09-12: прогон закрыт.** Класс 2 починен, вердикт скептика подтверждён
> владельцем, запрет на `roo_code/` снят (правило 1). Папка остаётся как архив доказательств —
> триаж, делты, журнал починки, вердикты скептика. Новую работу сюда не класть: её место —
> `roo_code/plans/`.

## Правила этого прогона

1. ~~**Ничего не править вне этой папки внутри `roo_code/`.**~~ **ЗАПРЕТ СНЯТ владельцем
   2026-09-12.** Он держался ровно на одном условии — «пока владелец принимает решения, их
   правит другая живая сессия». Решения приняты, класс 2 закрыт и подтверждён, второй сессии
   в дереве нет. Писать в `roo_code/` можно обычным порядком.

   Что из этого следует немедленно: пометки «✅ починено» больше не копятся в
   [`журнал-починки.md`](журнал-починки.md), а **переносятся в баг-файлы** семи доменов —
   это пункт 3 очереди работ, и он был заблокирован только этим правилом.
2. **Код правится обычным порядком** — `frontend_vue/`, `backend/`. Это не `roo_code/`, и
   другая сессия его не касается.
3. ~~**Порт 5173 не занимать.**~~ **Отпало 2026-09-12:** чужой сессии в дереве нет,
   `ss -ltnp | grep 5173` — пусто. Правило держалось на замере 2026-09-09 (pid 908858),
   которого больше не существует. Перед прогоном проверить заново, а не верить этой строке.
4. **Порядок задачи** — скил [`fix-bugs.md`](../skills/fix-bugs.md): один баг за цикл.
   Проверка — цикл из [`verify.md`](../skills/verify.md).

## Что здесь лежит

| файл | что в нём |
|---|---|
| [`триаж.md`](триаж.md) | базовый триаж: 200 находок по группам А/Б/В |
| [`триаж-делта.md`](триаж-делта.md) | правки к нему после куста «транзакционность» (П43–П48), закрытого 2026-09-09 |
| [`журнал-починки.md`](журнал-починки.md) | что починено, чем доказано, какой коммит |

## Где что лежит физически

Прогон идёт **отдельным git worktree**, потому что вторая сессия сидит в основной папке и на
ветке `docs/owner-decisions-2026-09-07`; переключать под ней HEAD нельзя.

- рабочая папка прогона — `/home/greatgleb/PycharmProjects/flexiron-fixes`, ветка
  `fix/contract-bugs-2026-09-09`, отведена от `main`;
- ветвление от `main` законно: расхождение кода между `main` и веткой решений **нулевое**
  (`git diff --stat main...HEAD -- frontend_vue backend` → пусто), а все четыре сентябрьские
  починки уже в `main`;
- `frontend_vue/node_modules` — симлинк на основное дерево, ставить второй раз незачем.

## Задача на мерж ветвей: удалить дубль триажа

Триаж существует в **двух** экземплярах, и это надо разрешить сознательно, а не оставить как
получилось:

| путь | ветка | состояние |
|---|---|---|
| [`триаж.md`](триаж.md) | `fix/contract-bugs-2026-09-09` | **канонический** — рядом делты и журнал |
| `roo_code/plans/bugs/contract-sync-bugs-triage-2026-09-09.md` | `docs/owner-decisions-2026-09-07` | копия, приведена к тому же содержимому коммитом `e341667` |

Пути разные, поэтому git при мерже **не покажет конфликта** — после него просто останутся два
файла, и про это забудут. Удалять надо копию в `plans/bugs/`: канонический экземпляр живёт
здесь, вместе с делтами и журналом, и один документ должен лежать в одном месте.

**Хвост, оставленный в основном дереве.** Первая версия триажа была записана в
`roo_code/plans/bugs/contract-sync-bugs-triage-2026-09-09.md` до того, как правило появилось, и
чужой коммит `c245463` забрал её через `git add -A`. Мои поправки к четырём неверным числам
висели там несохранёнными, потому что `git checkout -- <файл>` в deny-листе (правило 8
[`fix-bugs.md`](../skills/fix-bugs.md), и запрет по делу: откат вернул бы неверные числа).
Закрыто коммитом `e341667` в ветке решений — числа там теперь верные, а дубль снимается при
мерже по задаче выше.

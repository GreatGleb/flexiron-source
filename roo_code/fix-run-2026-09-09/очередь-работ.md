# Очередь работ после класса 2 — составлена 2026-09-11

**Место этого файла временное.** Его дом — `roo_code/plans/general/`, но правило прогона
([`README.md`](README.md)) запрещает трогать `roo_code/` вне этой папки. Снимут запрет —
переносить `git mv`, чтобы не рвать историю.

Все числа ниже — замеры, команда приведена рядом. Числа без команды в этот файл не
попадают: у прогона уже был случай, когда четыре значения оказались неверными.

---

## Что изменилось в понимании очереди

Планировалось: класс 2 → класс 3 → класс 4 → и так до конца триажа. Оказалось, что
рядом с починкой находок идёт **вторая дорожка, которой ни у кого не было в плане** —
реализация решений владельца. Она больше первой:

| | замер |
|---|---|
| эндпоинтов в контракте | **175** (`contract-conformance.spec.ts` печатает `175 из 175`) |
| из них **без бэкенда** | **147** (`grep -rcE 'Бэкенд: (\*\*)?не реализован' roo_code/roo-context/api/*.md`) |
| вертикальных слайсов написано | **8** — auth ×4, products ×2, settings ×2 (`find backend/app/modules -path '*/features/*' -name action.py`) |
| решений владельца | **75** уникальных П |
| из них упомянуты в контракте | **71** |

То есть контракт сведён полностью, а сервера под ним почти нет. **Классы 3 и 4 при этом
никуда не сдвигаются:** класс 3 — «мок молчит вместо отказа» — это мок-сторона той же
монеты, что класс 2, и оба они и есть начало дорожки «привести мок к контракту».

---

## Дорожка 0 — закрыть класс 2

1. **Скептик** — вердикт по трём коммитам (`e0893b1`, `229c73c`, `365fc65`). Промпт
   составлен, единственный открытый вопрос к нему — не прячет ли откат `errorCode` на
   `message` то, что должно краснеть.
2. **Подтверждение человеком** — режим интерактивный, вердикт скептика его не заменяет.
3. **Пометки `✅` в баг-файлы** семи доменов + **13 битых ссылок**, разобранных поимённо
   в [`журнал-починки.md`](журнал-починки.md). Упирается только в запрет на `roo_code/`.

## Дорожка 1 — мок-долг

4. **Класс 3** — мок молчит вместо отказа, 8 находок, группа А.
5. **Класс 4** — `null` в query литералом, 2 находки, группа А.
6. **Мок бросает `ApiRequestError`** — 210 мест
   (`grep -rc "throw new Error(" frontend_vue/src/services/mocks/`). Осторожно: восемь
   спек утверждают отказ по **тексту** исключения (`toThrow('КОД')`) и покраснеют от
   смены конверта — их надо переписать на утверждение о поле `code`, а это улучшение, а
   не обход. Пока не сделано, откат на `message` в `errorCode` обязан жить.

## Дорожка 2 — привести учёт находок в порядок

7. **Перетриаж группы В.** Триаж писался при 42 решениях, сейчас 75. Из 200 находок в нём
   31 помечена «ждёт нерешённого»; сколько из них разблокировано — неизвестно, и в
   баг-файлах пометки старые. Пока не сделано, любая оценка объёма завышена.
8. **Семь дефектов из [`триаж-делта-2.md`](триаж-делта-2.md)** — названы решениями
   владельца и не заведены ни в один баг-файл. Два из них про потерю несохранённых данных.
9. **Дубль триажа.** `триаж.md` здесь и `roo_code/plans/bugs/contract-sync-bugs-triage-2026-09-09.md`
   в ветке решений — при мерже git конфликта не покажет, пути разные. Удалять копию в
   `plans/bugs/`.

## Дорожка 3 — добить контракт

10. **П33** — «новый элемент матрицы: по умолчанию админ, плюс правило 90 %». В контракте
    нет: `grep -niE "90 ?%|новый элемент" 00-conventions.md config.md` → пусто. Место —
    §6 соглашений.
11. **П34** — «ширина реза уезжает в настройки, переопределяется в каждом резе». В
    контракте нет: `grep -niE "ширина реза|kerf" settings.md warehouse.md` → пусто.
    Место — `settings.md` и раздел раскроя в `warehouse.md`.

П30 и П60 внесения **не требуют** и в список не входят: суть П30 (лимит 20 МБ, белый
список MIME) в `uploads.md` уже описана, а П60 — ссылка на П54, который внесён. Не
внесены только номера, а не содержание.

## Дорожка 4 — планы реализации по доменам

**Семнадцать планов, по файлу на домен.** Разбивать надо **по доменам контракта, а не по
решениям**: иначе П44 окажется в одном плане, а `DELETE /api/products/:id`, который он
переписывает, — в другом.

В план домена входит три источника, а не один:

- **нереализованные эндпоинты** его файла контракта — форма запроса и ответа там уже есть;
- **решения владельца**, которые этот домен касаются;
- **названные дыры** — разделы «Обязанности сервера» и «Чего в домене нет» есть во всех
  17 файлах, и они перечисляют то, чего не делает никто.

Почему план нужен и под старый контракт, а не только под решения: контракт написан **по
коду** — он описывает, что есть, и честно называет, чего нет, но не говорит, кто прав в
конфликте схемы и контракта, каким кодом отказывать там, где сегодня не отказывает никто,
и в каком порядке это строить. Пример на одном эндпоинте, `POST /api/categories`: `name`
на схеме `String(255)`, то есть трёхъязычное имя хранить нечем; существование `parentId`
не проверяет никто и кода отказа для этого нет; уникальности имени не требует ни мок, ни
схема; бэкенда нет. Четыре задачи из одного эндпоинта из 175.

Порядок доменов — по объёму без бэкенда:

| домен | эндпоинтов | без бэкенда |
|---|---:|---:|
| warehouse | 37 | 37 |
| orders | 33 | 33 |
| settings | 31 | 7 |
| config | 12 | 12 |
| clients | 10 | 10 |
| suppliers | 8 | 8 |
| products | 7 | 5 |
| bcc | 7 | 7 |
| categories | 6 | 6 |
| services | 5 | 5 |
| finance | 5 | 5 |
| auth | 5 | 1 |
| notifications | 4 | 4 |
| audit-feed | 2 | 2 |
| analytics | 1 | 1 |
| uploads · sales-crm | 2 | 0 |

## Дорожка 5 — сквозное, что не ложится ни в один домен

Эти шесть идут поперёк всех семнадцати, и если их не спланировать отдельно, каждый домен
изобретёт своё:

12. **Права (§6)** — «модель решена 2026-09-07, **не реализована нигде**»: единственная
    функция проверки — заглушка `return True`.
13. **Мультиарендность (§4)** — `tenant_id` есть в моделях всех десяти модулей, значит
    фильтр обязателен в **каждом** запросе; линза Б3 прямо говорит, что ни один тест этого
    не поймает, потому что тестов нет.
14. **Аудит-лог (§9)** — одна таблица, девять видов сущности; на бэкенде таблицы нет вовсе.
15. **Уведомления (§10)** — восемь типов становятся пятнадцатью (П51), два канала (П54).
16. **Идемпотентность и оптимистичная блокировка (§11)**.
17. **Кастомные поля (§8)** — определения в двух местах, значения в одном, жизненного
    цикла нет нигде; П73 задал жизненный цикл значения.

## Дорожка 6 — реализация

18. **Фронт по контракту.** Делать его раньше бэкенда законно — при 8 слайсах из 175 фронт
    и есть референсная реализация. Менять надо не очерёдность, а **источник**: не мок, а
    контракт. Поэтому дорожка 1 идёт раньше: фронт, построенный по моку, который врёт про
    отказы, изготовит класс 2 заново — только в масштабе 175 эндпоинтов.
19. **Бэкенд по тому же контракту**, слайсами, по планам дорожки 4.

## Отложенное, без места в дорожках

20. **Причина падений e2e на длинном прогоне не установлена.** Замерено: два тяжёлых
    прогона дали 9 и 5 падений с пустым пересечением, в покое — 1078/1078. Прогон
    2026-09-11 был чистый, то есть машина была спокойна, а не причина исчезла.
21. **`products/БАГ-01` перекрыт П44.** Ветка `PRODUCT_IN_USE` умрёт вместе с удалением
    товара, когда архивирование реализуют. Снимать её вместе с П44, а не отдельно.
22. **Три кода справочников** — `UOM_IN_USE`, `CURRENCY_IN_USE`, `CURRENCY_IS_DEFAULT` —
    назначены контрактом (`api/settings.md:76-78`) и не разбираются ни одним потребителем.
    Класс тот же, что `finance/БАГ-06`: код, брошенный сервером и никем не прочитанный.

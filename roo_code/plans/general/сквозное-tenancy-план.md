# Сквозное: мультиарендность (§4)

**Тема идёт поперёк всех семнадцати доменов.** Этот файл существует ровно затем, чтобы
правило про арендатора было выведено **один раз**, а не семнадцать раз по-своему. Домен не
толкует правило и не выбирает вариант — он применяет §3 дословно, копирует себе §4 и
показывает зелёный прогон §5.

Измерения в §2 сняты **2026-09-12** и **перепроверены целиком 2026-09-13** на ветке
`fix/contract-bugs-2026-09-09`. Команды, которыми они сняты, приведены рядом с числами: число,
снятое иначе, — это другое число. Свип §2.3 воспроизвёлся строка в строку (37 / 22 / 15,
`/tmp/proof-tenancy-sweep.txt`); правки второго замера — три, и все три названы там, где стоят:
перепись моделей §2.1, решение П62 в §1 и снятая ложная находка в §2.6.

Источник правила — [`roo_code/roo-context/api/00-conventions.md`](../../roo-context/api/00-conventions.md),
§4 «Мультиарендность — одно правило на все домены» (строки 116–170) и §4.1 (строки 148–170).
Этот план ничего в §4 не меняет: он его **измеряет, доводит до исполнимого вида и снабжает
сторожем**.

---

## 1. Что решено владельцем

Куст «Мультиарендность» отсеялся почти целиком: соглашения задали правило раньше, чем куст
дошёл до владельца. Дословно, из `audit/00-решения-владельца.md` (раздел «куст „настройки и
арендаторы“, 2026-09-10»):

> Мультиарендность отсеялась почти целиком — §4 соглашений уже задал правило на все домены, —
> и от неё осталась одна строка, про пользователей.

Поэтому владельческих решений по теме **три**, и ни одно из них не про «фильтровать ли»:
фильтрация решена схемой, а не владельцем.

### П58 · Второй человек в компании заводится ссылкой-приглашением

> Владелец **сначала заводит запись**: пишет имя и выбирает роль. Затем получает
> **ссылку-приглашение** и передаёт её как считает нужным — письмом (П53) или скопировав и
> отправив коллеге любым способом. Приглашённый переходит по ссылке и **ставит себе пароль
> сам**.

И следствие, названное там же дословно:

> Уникальность почты считается парой с арендатором (§4 выше), то есть один и тот же адрес
> может быть приглашён в две разные компании.

Это единственное решение, которое **меняет** правило §4: раньше «email уникален» можно было
читать как глобальное свойство, теперь нельзя — и это ломает два места входа (см. §6, пункт 4).

### П12 · Матрица прав хранится на бэкенде, по арендатору

> Фронтовая копия — только для мок-режима. На бэкенде матрица хранится отдельно для каждого
> арендатора и редактируется владельцем и админом.

Значение для темы: право — тоже арендаторская таблица, и §3 распространяется на неё так же,
как на товар. Схема под это уже сделана парой с арендатором —
`UniqueConstraint("tenant_id", "item_id", "role", name="uq_role_permission")`
([`backend/app/modules/auth/shared/models.py:197`](../../../backend/app/modules/auth/shared/models.py)).

### П25 · Справочники нового арендатора сеются миграцией

> Валюты, единицы измерения, правила пересчёта и статусы заказов — всё, что нужно для работы с
> самого начала, — создаётся **миграцией**. Статусы заказа системные: их создаёт система,
> удалить их нельзя.

Значение для темы: у нового арендатора справочники обязаны появиться **не пустыми**, иначе
первый же экран показывает пустой список валют. Механизм назван (миграция); **состав** —
не назван и здесь назван быть не может (§6, пункт 1).

### П62 · У компании есть часовой пояс, и сервер режет сутки и месяцы по нему

> Поле заводится в настройках компании; **сервер** определяет по нему границу суток у фильтров
> «с даты по дату», границу месяца в сводке продаж и периоды отчётов.

Добавлено вторым замером: первая редакция этого файла решение пропустила. Значение для темы:
часовой пояс — **свойство арендатора запроса**, то есть всякий запрос с датами сначала берёт
пояс того арендатора, которым уже сужен, и только потом режет сутки. Ни пояс сервера, ни пояс
другого арендатора заменой не служат, и это второе место после справочников (П25), где чужая
строка портит ответ, ничего не «утекая»: цифры просто окажутся не за тот месяц.

---

## 2. Что есть в коде СЕЙЧАС

### 2.1. Схема: колонка есть везде, и это не обсуждается

`tenant_id` объявлен в моделях **всех десяти** модулей. Снято командой
`for m in $(ls backend/app/modules); do grep -c tenant_id backend/app/modules/$m/shared/models.py; done`:

| модуль | вхождений `tenant_id` в `shared/models.py` |
|---|---|
| auth | 7 |
| bcc | 2 |
| billing | 6 |
| finance | 3 |
| notifications | 1 |
| products | 4 |
| services | 1 |
| settings | 7 |
| suppliers | 10 |
| warehouse | 6 |

Модулей с `shared/models.py` ровно десять — остальные элементы `ls` это `__init__.py` и
`__pycache__`. Значит **фильтр по арендатору обязателен в каждом запросе**, и это свойство
проекта, а не выбор домена.

Счёт вхождений говорит, что колонка есть; он не говорит, у каких таблиц её **нет**. Второй
замер снял и это — разбором `ast` по всем `app/**/models.py` (`/tmp/proof-tenancy-models.txt`):

```
ВСЕГО моделей: 45; без tenant_id: 6; с tenant_id: 39
```

Шесть без колонки — весь список исключений схемы на сегодня, и он закрытый:

| таблица | класс | где объявлен | почему без колонки |
|---|---|---|---|
| `tenants` | `Tenant` | [`backend/app/modules/auth/shared/models.py:14`](../../../backend/app/modules/auth/shared/models.py) | сам реестр арендаторов |
| `user_roles` | `UserRole` | [`backend/app/modules/auth/shared/models.py:87`](../../../backend/app/modules/auth/shared/models.py) | арендатор наследуется через `user_id` |
| `sessions` | `Session` | [`backend/app/modules/auth/shared/models.py:111`](../../../backend/app/modules/auth/shared/models.py) | арендатор наследуется через `user_id` |
| `plans` | `Plan` | [`backend/app/modules/billing/shared/models.py:11`](../../../backend/app/modules/billing/shared/models.py) | платформенный каталог тарифов |
| `plan_features` | `PlanFeature` | [`backend/app/modules/billing/shared/models.py:75`](../../../backend/app/modules/billing/shared/models.py) | платформенный каталог тарифов |
| `feature_definitions` | `FeatureDefinition` | [`backend/app/modules/billing/shared/models.py:137`](../../../backend/app/modules/billing/shared/models.py) | платформенный реестр ключей фич |

Отсюда три категории таблицы, и они нужны, чтобы правило можно было применить к таблице,
которой ещё нет (Т8):

- **А — своя колонка.** Данные арендатора. `tenant_id` обязателен:
  `ForeignKey("tenants.id", ondelete="CASCADE")`, `nullable=False`, `index=True`. Сегодня 39.
- **Б — арендатор наследуется** через единственный обязательный FK на строку категории А или
  на `users`. Своей колонки не заводит. Сегодня две: `user_roles`, `sessions`.
- **В — платформенная**, одна на всех арендаторов. Сегодня четыре: `tenants`, `plans`,
  `plan_features`, `feature_definitions`.

### 2.2. Механизма получения арендатора нет — есть две копии одной функции

Общего места, которое дало бы запросу арендатора, **не существует**. Доказательство:
`backend/app/core/middleware/` содержит один файл `cors.py` (15 строк, вхождений `tenant` — 0);
`backend/app/modules/auth/shared/dependencies.py` обещает докстрингом «get_current_user,
permission checkers, tenant isolation» и не содержит ни строки кода — это записано прямо в
шапке существующего сторожа
([`backend/tests/test_route_auth.py:38`](../../../backend/tests/test_route_auth.py)).

Вместо общего места — две копии, обе резолвят арендатора через пользователя токена:

- [`backend/app/core/uploads/action.py:62`](../../../backend/app/core/uploads/action.py) — `_get_tenant_id`, свой `select(User)` прямо в слое роута;
- [`backend/app/modules/settings/features/crud/action.py:131`](../../../backend/app/modules/settings/features/crud/action.py) — `_get_tenant`, который зовёт репозиторную `get_tenant_id_for_user` ([`backend/app/modules/settings/features/crud/repository.py:21`](../../../backend/app/modules/settings/features/crud/repository.py)).

Вызывающих у второй — два файла, оба внутри `settings`
(`grep -rl get_tenant_id_for_user backend/app/` → `crud/action.py`, `crud/repository.py`).
То есть переиспользования нет: каждый новый слайс сегодня пишет третью копию.

### 2.3. Сколько запросов сужено, а сколько нет — точный счёт

Замер снят разбором AST по всем `backend/app/**/*.py` (скрипт и вывод —
`/tmp/proof-tenancy-sweep-baseline.txt`): функция считается запросом, если в её теле есть
`select(`, `update(` или `db.delete(`; суженной — если в теле есть `tenant_id ==`.

```
всего функций с запросом: 37
фильтруют по tenant_id: 22
НЕ фильтруют:           15
```

Пятнадцать несуженных разбираются на три разные вещи, и смешивать их нельзя. **Арифметика
сходится нацело и проверяется глазом: 7 (а) + 1 (б) + 7 (в) = 15.** Это не украшение: список
несуженных, который не сходится с разбором, означает, что одну функцию забыли отнести
куда-нибудь, и она тихо живёт вне обоих списков сторожа.

**(а) Семь законно несуженных — это резолверы личности, то есть запросы, чья работа и есть
«узнать арендатора». Сузить их нечем: арендатор ещё не известен.**

| место | функция | почему законно |
|---|---|---|
| [`backend/app/core/uploads/action.py:62`](../../../backend/app/core/uploads/action.py) | `_get_tenant_id` | резолвер |
| [`backend/app/modules/settings/features/crud/repository.py:21`](../../../backend/app/modules/settings/features/crud/repository.py) | `get_tenant_id_for_user` | резолвер |
| [`backend/app/modules/auth/features/me/repository.py:11`](../../../backend/app/modules/auth/features/me/repository.py) | `get_user_by_id` | `user_id` взят из токена |
| [`backend/app/modules/settings/features/profile/repository.py:11`](../../../backend/app/modules/settings/features/profile/repository.py) | `get_user_by_id` | `user_id` взят из токена |
| [`backend/app/modules/auth/features/magic_link/repository.py:9`](../../../backend/app/modules/auth/features/magic_link/repository.py) | `get_user_by_secret_link` | секрет глобально уникален |
| [`backend/app/modules/auth/features/register/repository.py:24`](../../../backend/app/modules/auth/features/register/repository.py) | `get_tenant_by_slug` | до арендатора |
| [`backend/app/modules/auth/features/login/repository.py:15`](../../../backend/app/modules/auth/features/login/repository.py) | `get_user_by_email` | вход: арендатора ещё нет по построению — **но какую из двух записей эта функция вернёт после П58, не определено; см. §6, пункт 4** |

Последняя строка таблицы — единственная, которая законна по механизму и при этом
недоопределена по решению: Т4 её покрывает, П58 её ломает. В `TENANTLESS` сторожа она входит,
но причиной у неё стоит не «резолвер», а ссылка на открытый вопрос — чтобы починка §6 п.4 не
прошла мимо неё.

**(б) Одна несужена по форме, а не по сути:** `get_company_info`
([`backend/app/modules/settings/features/crud/domain.py:64`](../../../backend/app/modules/settings/features/crud/domain.py))
читает саму таблицу арендаторов — `select(Tenant).where(Tenant.id == tenant_id)`. Сужение
здесь пишется как `Tenant.id ==`, а не `tenant_id ==`. Сторож §5 обязан знать этот случай,
иначе покрасит верный код.

**(в) Семь настоящих дыр. Каждая — чужие данные по одному `id`:**

| место | что течёт | уже записано как |
|---|---|---|
| [`backend/app/modules/products/features/get_product_detail/repository.py:13`](../../../backend/app/modules/products/features/get_product_detail/repository.py) — `get_product_by_id` | товар чужого арендатора по UUID | products БАГ-14 |
| [`backend/app/modules/products/features/get_product_detail/repository.py:25`](../../../backend/app/modules/products/features/get_product_detail/repository.py) — `get_category_by_id` | категория чужого арендатора | products БАГ-14 |
| [`backend/app/core/uploads/service.py:39`](../../../backend/app/core/uploads/service.py) — `get_file_by_id` | **файл** чужого арендатора | новая, не записана |
| [`backend/app/core/uploads/service.py:51`](../../../backend/app/core/uploads/service.py) — `delete_file` | удаление чужого файла | новая, не записана |
| [`backend/app/modules/settings/features/profile/repository.py:17`](../../../backend/app/modules/settings/features/profile/repository.py) — `update_user` | **запись** по одному `id` | новая, не записана |
| [`backend/app/modules/settings/features/profile/repository.py:32`](../../../backend/app/modules/settings/features/profile/repository.py) — `get_user_by_email` | проверка уникальности глобально | auth БАГ-13 |
| [`backend/app/modules/auth/features/register/repository.py:35`](../../../backend/app/modules/auth/features/register/repository.py) — `get_user_by_email` | то же на регистрации | auth БАГ-13 |

Две из этих семи (`uploads`) лежат **вне** `features/*/repository.py` — в `app/core/`. Это
единственная причина, по которой сторож §5 обязан обходить все файлы, а не только репозитории:
сторож, ограниченный `repository.py`, дыру в файлах не увидел бы вовсе.

### 2.4. Арендатор захардкожен заглушкой в обоих роутах `products`

```
tenant_id = uuid.UUID("00000000-0000-0000-0000-000000000001")  # placeholder
```

[`backend/app/modules/products/features/get_product_detail/action.py:35`](../../../backend/app/modules/products/features/get_product_detail/action.py) и
[`backend/app/modules/products/features/create_product/action.py:34`](../../../backend/app/modules/products/features/create_product/action.py).
То есть слайс **создаёт товар в чужом арендаторе** и читает из него же — вне зависимости от
того, кто вошёл. Оба файла уже стоят в `KNOWN_GAPS` существующего сторожа
([`backend/tests/test_route_auth.py:58`](../../../backend/tests/test_route_auth.py)).

### 2.5. Межмодульный вход отдаёт несуженные getters наружу

`products/internal_api/interface.py` объявлен «the ONLY way other modules should access product
data» и **ре-экспортирует ровно те две функции из (в)**:
[`backend/app/modules/products/internal_api/interface.py:21`](../../../backend/app/modules/products/internal_api/interface.py) — `get_product_by_id`,
`:33` — `get_category_by_id`. Соседние функции того же файла арендатора принимают:
`count_products_by_currency` (`:43`) фильтрует `Product.tenant_id == tenant_id` (`:50`).
Значит внутри одного файла два разных правила.

### 2.6. Уникальность: парой — уже норма, два исключения — находки

Тенантских таблиц с уникальностью одиннадцать. Парой с арендатором сделаны, например:
[`backend/app/modules/settings/shared/models.py:82`](../../../backend/app/modules/settings/shared/models.py) — `uq_currencies_tenant_code`,
[`backend/app/modules/suppliers/shared/models.py:265`](../../../backend/app/modules/suppliers/shared/models.py) — `uq_field_definitions_tenant_name`,
[`backend/alembic/versions/3a0b5d31bde7_phase_1_tenants_auth_users_sessions.py:54`](../../../backend/alembic/versions/3a0b5d31bde7_phase_1_tenants_auth_users_sessions.py) — `ix_users_tenant_id_email`.

Нарушение ровно одно:

- [`backend/app/modules/warehouse/shared/models.py:213`](../../../backend/app/modules/warehouse/shared/models.py) — `product_id` объявлен `unique=True` в одиночку (warehouse №28). Два арендатора, купившие один товар, делят одну строку остатка: вторая не запишется вовсе.

**Поправка второго замера.** Первая редакция считала вторым нарушением
`uq_product_field_value` по `(product_id, field_id)`
([`backend/app/modules/products/shared/models.py:211-215`](../../../backend/app/modules/products/shared/models.py)).
Это неверно, и разница — не придирка: `product_id` — FK на `products`, таблицу категории А,
поэтому два товара разных арендаторов одного `product_id` иметь не могут, и пара уже
тенантская. Тем же устроен `uq_user_role` по `(user_id, role_name)`
([`backend/app/modules/auth/shared/models.py:104`](../../../backend/app/modules/auth/shared/models.py)).
Правило Т6 переписано так, чтобы обе формы были законны машинно, а не по усмотрению
читающего: иначе домены начали бы «чинить» верные констрейнты миграциями.

### 2.7. Фронт про арендатора почти не знает — и «почти» здесь важно

Бриф утверждал: «Ни в одном типе, ни в одном сервисе нет ни `tenantId`, ни заголовка
арендатора». **Это неверно, проверено грепом.**
`grep -rn 'tenantId\|tenant_id\|X-Tenant' frontend_vue/src --include=*.ts --include=*.vue` →
6 вхождений в 5 файлах, из них **четыре строки кода в трёх файлах**, остальные два — текст
комментариев ([`frontend_vue/src/services/contractRefs.ts:92`](../../../frontend_vue/src/services/contractRefs.ts)
и [`frontend_vue/src/services/api.ts:23`](../../../frontend_vue/src/services/api.ts)):

- [`frontend_vue/src/types/auth.ts:11`](../../../frontend_vue/src/types/auth.ts) и `:52` — поле `tenant_id` в `UserInfo` и в ответе регистрации;
- [`frontend_vue/src/composables/useAuth.ts:138`](../../../frontend_vue/src/composables/useAuth.ts) — оно же переписывается в кэш пользователя;
- [`frontend_vue/src/services/mocks/index.ts:988`](../../../frontend_vue/src/services/mocks/index.ts) — `tenant-mock-001` в моке.

Разница принципиальная и её надо назвать словами: это **эхо ответа сервера, а не параметр
запроса**. Ни один из 175 вызовов `tenant_id` не **шлёт** — ни телом, ни query, ни заголовком.
Правило §3 запрещает отправку, а не наличие поля в ответе; формулировка «нет нигде» запретила
бы верный код.

### 2.8. Сторож частично уже написан — и это главное, что надо знать

Утверждение задачи «тестов на это нет» **неверно**. В
[`backend/tests/test_route_auth.py:156`](../../../backend/tests/test_route_auth.py) живёт
`TenantScopedGetterTest`, и он проверяет ровно то правило, которое здесь выводится: функция
обязана принять `tenant_id` **и отфильтровать по нему** — проверяется подстрока `tenant_id`
на [`:195`](../../../backend/tests/test_route_auth.py), а не одна сигнатура. Там же записан
довод, ради которого проверка сделана разбором AST, а не через `router.routes`: FastAPI и
SQLAlchemy в этом окружении не установлены, а проверка, которую нельзя запустить, — не проверка.

Прогон сегодня зелёный (`/tmp/proof-tenancy-unittest.txt`):

```
cd backend && python3 -m unittest discover -s tests -t .
Ran 14 tests in 0.027s
OK
```

**Но охват у него — один файл и двенадцать имён, выписанных руками:**
`REPOSITORY = "app/modules/settings/features/crud/repository.py"`
([`backend/tests/test_route_auth.py:164`](../../../backend/tests/test_route_auth.py)), четыре
геттера ([`:165`](../../../backend/tests/test_route_auth.py) — `get_conversion` и соседи) и восемь писателей. Из 37 функций с запросом он
смотрит на 12, то есть **на треть**, и ни одной из семи дыр §2.3(в) не видит: все семь лежат
вне его файла.

Значит работа этого плана — **не изобрести сторожа, а снять с него список имён**. Форма,
которую надо повторить, в том же файле уже отработана на аутентификации: закрытый список
исключений с причиной (`PUBLIC_ROUTES`), закрытый список известных дыр
(`KNOWN_GAPS`, [`:58`](../../../backend/tests/test_route_auth.py)), пол охвата
(`assertGreaterEqual(counted, 25)`) и проверка, что исключение не пережило починку.

---

## 3. Правило — одно, для всех семнадцати доменов

Домен не выбирает вариант и не оценивает применимость. Ниже восемь пунктов; каждый либо
выполнен, либо нет, и каждый проверяется машиной из §5.

**Т1. Арендатор берётся только из токена.**
Ни тело, ни query, ни путь, ни заголовок арендатора не несут. Роут, принявший `tenant_id`
параметром, — дефект независимо от намерений: клиент подставит чужой. Единственный источник —
пользователь сессии.

**Т2. Каждая функция, выполняющая `select` / `update` / `delete` по тенантской таблице,
принимает `tenant_id` параметром и сужает им запрос.**
Не «репозиторий сужает» — **функция**, где бы она ни лежала: `repository.py`, `domain.py`,
`service.py`, `action.py`, `internal_api/interface.py`. Две из семи сегодняшних дыр лежат в
`app/core/uploads/`, и правило, ограниченное репозиторием, их разрешает.

**Т3. Писатель сужает сам, а не полагается на чтение перед собой.**
`get` → проверка → `patch` двумя запросами — это окно: между ними строка может сменить
владельца. `update()` и `delete()` несут `tenant_id ==` в своём собственном `where`. Довод
записан в существующем стороже ([`backend/tests/test_route_auth.py:206-211`](../../../backend/tests/test_route_auth.py)).

**Т4. Исключение бывает ровно одного вида: запрос, чья работа — узнать арендатора.**
Это резолвер по токену, поиск по глобально уникальному секрету и обращение к самой таблице
`tenants`. Больше исключений нет. «Здесь и так безопасно, потому что id мы получили из
предыдущего запроса» исключением **не является**: предыдущий запрос — другой запрос.
Исключение попадает в закрытый список сторожа **с причиной строкой**, а не молча.

**Т5. Ответ, сшитый из нескольких источников, сужает каждый источник отдельно.**
Лента аудита из девяти логов, сводка CRM из двух хранилищ, реестр входящих по заказам — один
пропущенный фильтр течёт именно туда, где это заметно меньше всего. Сшивающая функция
обязана передать `tenant_id` в каждый вызов, а не в первый.

**Т6. Уникальность считается парой с арендатором — в одной из двух форм.**
Всякое `UniqueConstraint` / `unique=True` на таблице категории А законно, если выполнено
одно из двух:
(1) набор колонок начинается с `tenant_id` — `uq_role_permission`, `ix_users_tenant_id_email`;
(2) набор колонок содержит FK на строку **другой** таблицы категории А, то есть пара уже
тенантская через владельца — `uq_product_field_value` через `product_id`, `uq_user_role`
через `user_id`.
Вне обеих форм — дефект: голый `unique=True` на собственной колонке таблицы категории А
означает «эта строка может быть только у одного арендатора во всём мире» (ровно warehouse
№28). Единственное именованное исключение — глобально уникальный секрет
(`secret_link_token`), и оно тоже в закрытом списке. Миграция и модель правятся вместе:
модель без миграции — это уникальность, которой в базе нет.

**Т8. У новой таблицы категория назначается явно, и по умолчанию она А.**
Категории — §2.1: А (своя колонка), Б (наследует через единственный FK), В (платформенная).
Таблица, заведённая без `tenant_id` и без строки-причины в списке сторожа, — провал приёмки,
а не «наверное, платформенная». Правило нужно именно доменам без бэкенда: у семи из
семнадцати таблиц сегодня нет ни одной, и категорию они назначают **при проектировании
схемы**, а не после миграции.

**Т7. Фронт про арендатора не знает и не начинает знать.**
Сервис не шлёт ни поле `tenantId`/`tenant_id`, ни заголовок вида `X-Tenant-*`. Поле
`tenant_id` в `UserInfo` — **чтение ответа `/api/auth/me`**, и оно остаётся; отправка
запрещена. Домен, которому «нужен арендатор на фронте», ошибся: ему нужен сервер, который
уже сузил.

---

## 4. Что обязан сделать каждый домен

Блок ниже копируется в доменный план целиком, без правок кроме подстановки имени домена.
Он не содержит выборов — только проверяемые действия. Домены без модуля на бэкенде
(`orders`, `clients`, `categories`, `config`, `audit-feed`, `analytics`, `sales-crm`)
отвечают по Д1, Д5, Д6, Д9, Д10 — то есть проектно; остальные пункты у них пустые
по построению, и так и записываются.

```
### Мультиарендность (сквозное правило, §3 файла roo_code/plans/general/сквозное-tenancy-план.md)

Д1. Перечислить в разделе плана все таблицы домена, у которых объявлен tenant_id.
    Команда: grep -n tenant_id backend/app/modules/<модуль>/shared/models.py
    Нет модуля на бэкенде — так и записать: «таблиц нет, правило вступит вместе со слайсом».

Д2. Для КАЖДОЙ функции домена, выполняющей select/update/delete по этим таблицам,
    записать: принимает ли tenant_id и сужает ли им запрос. Строкой файл:строка.
    Проверять не только features/*/repository.py — также domain.py, action.py,
    internal_api/interface.py и всё, что домен трогает в app/core/.

Д3. Каждую несужённую функцию отнести к одному из двух:
      — исключение по Т4 (резолвер личности / глобальный секрет / таблица tenants) —
        тогда строка уходит в TENANTLESS сторожа с причиной;
      — дефект — тогда строка уходит в баг-файл домена и в KNOWN_GAPS сторожа
        с номером бага.
    Третьего варианта нет. «Пока оставим» — это дефект без номера.

Д4. Роуты домена: ни один не принимает tenant_id параметром (Т1). Проверить сигнатуры
    всех @router.* в action.py домена.

Д5. Уникальности домена (Т6): каждое UniqueConstraint/unique=True на тенантской таблице
    включает tenant_id. Нарушение — правка модели И миграции одним действием.

Д6. Фронт домена (Т7): сервис не шлёт tenant_id/tenantId/X-Tenant.
    Команда: grep -n 'tenantId\|tenant_id\|X-Tenant' frontend_vue/src/services/<домен>Service.ts
    Ожидание: пусто.

Д7. Новый слайс домена берёт арендатора ТОЛЬКО из токена и передаёт его вниз по слоям
    в порядке schemas → repository → domain → action: tenant_id приходит в action из
    резолвера токена, action передаёт его в domain, domain — в repository, repository
    ставит его в where. Схемы (schemas.py) поля tenant_id НЕ содержат: то, чего нет в
    схеме, клиент не подставит.

Д8. Приёмка домена по этому пункту — зелёный прогон сторожа:
    cd backend && python3 -m unittest discover -s tests -t .
    Домен, добавивший строку в TENANTLESS без причины, красит сторож.

Д9. Графа «Мультиарендность» раздела «Обязанности сервера» в
    roo_code/roo-context/api/<домен>.md заполнена и ССЫЛАЕТСЯ на §4 соглашений,
    а не пересказывает его. Пустая графа = домен не закрыт (скил api-contract, шаг 4).
    Проверка — §5.6 сквозного плана.

Д10. Таблицам домена назначены категории А/Б/В (Т8) — в том числе таблицам, которых
    ещё нет. Ответ «модуля на бэкенде нет, поэтому нечего назначать» НЕ принимается:
    схему проектирует доменный план, и категория стоит дешевле до миграции, чем после.
```

---

## 5. Машинная приёмка

Без неё §3 — декларация. Проверок семь: §5.1–§5.3 и §5.5 — новые тесты в одном файле,
§5.4 — новая спека фронта, §5.6 — греп по контракту, §5.7 — резолвер по этому файлу.

### 5.1. Сторож сужения (расширение существующего)

**Файл:** `backend/tests/test_tenant_scope.py` — отдельным файлом, а не правкой
`test_route_auth.py`: тот про аутентификацию, и смешивать два правила в одном файле значит
получить один список исключений на две разные вещи.

**Что обходит:** все `backend/app/**/*.py` (кроме `__pycache__`) разбором `ast`. Функция
считается запросом, если в `ast.unparse` её тела есть `select(`, `update(` или `db.delete(`.
Разбором, а не импортом — по доводу, уже записанному в
[`backend/tests/test_route_auth.py:3-8`](../../../backend/tests/test_route_auth.py): FastAPI и
SQLAlchemy в этом окружении не установлены.

**Что требует:** в теле функции есть `tenant_id ==` **или** `Tenant.id ==` (второе — случай
§2.3(б), обращение к самой таблице арендаторов).

**Три списка, все закрытые, все с причиной строкой:**

- `TENANTLESS` — исключения по Т4. Стартовый состав — **семь** строк §2.3(а), причём у седьмой
  (вход, `login/repository.py:15`) причиной стоит ссылка на открытый вопрос §6 п.4, а не слово
  «резолвер».
- `KNOWN_GAPS` — измеренные дыры с номером бага. Стартовый состав — семь строк §2.3(в).
- `TENANT_TABLES_EXEMPT` — функции, читающие таблицы категорий Б и В. Состав категорий
  **измерен** (§2.1) и потому записывается сразу, а не «когда появится первая функция»:
  `tenants`, `plans`, `plan_features`, `feature_definitions`, `sessions`, `user_roles` —
  шесть, и седьмая строка обязана прийти вместе с новой моделью и причиной (Т8).

**Четыре теста, и каждый закрывает свой способ соврать:**

1. `test_every_query_is_tenant_scoped` — несужённых, кроме перечисленных, нет.
2. `test_sweep_floor` — `assertGreaterEqual(counted, 37)`, число снято 2026-09-12.
   Экстрактор, переставший находить запросы, выдаёт зелёный отчёт и **стоит меньше, чем
   отсутствующая проверка: он ещё и врёт**. Формулировка и приём — с
   [`backend/tests/test_route_auth.py:121-126`](../../../backend/tests/test_route_auth.py).
3. `test_exception_lists_are_not_stale` — каждый файл из обоих списков существует.
4. `test_known_gaps_really_lack_scope` — функция из `KNOWN_GAPS`, которую уже починили,
   красит сторож: иначе исключение переживает починку и прячет место навсегда.

**Шестой тест, добавлен вторым замером — `test_every_model_has_a_category`:** пройти все
`app/**/models.py`, и класс модели без `tenant_id` в теле обязан стоять в
`TENANT_TABLES_EXEMPT` с причиной (Т8). Пол — `assertGreaterEqual(models_seen, 45)`, число
снято 2026-09-13. Без этого теста правило Т8 не проверяется ничем: сторож §5.1 смотрит на
функции, а таблица без колонки не порождает несужённой функции до тех пор, пока к ней не
напишут первый запрос, — то есть дефект обнаружился бы на слайс позже, чем возник.

**Порог, ниже которого сторож бесполезен:** `TENANTLESS` длиннее десяти строк означает, что
список стал свалкой. Проверяется седьмым тестом `assertLessEqual(len(TENANTLESS), 10)`.

### 5.2. Заглушек арендатора в коде нет

```
grep -rn '00000000-0000-0000-0000-000000000001' backend/app --include=*.py | wc -l
```

Ожидание после починки products БАГ-14 — `0`. Сегодня — 2
(§2.4). Оформляется тестом в том же файле: захардкоженный арендатор — это не «временно», это
работающий обход всего правила.

### 5.3. Роут не принимает арендатора

Разбором AST по всем `action.py`: среди аргументов ни одного обработчика `@router.*` нет
`tenant_id`. Ожидание — пусто, и сегодня оно уже выполняется (проверено: `tenant_id` в
`products/*/action.py` — локальная переменная, не параметр). Тест фиксирует состояние, чтобы
первый же слайс, решивший «принять арендатора из тела», покраснел.

### 5.4. Фронт не шлёт арендатора

Спека `frontend_vue/src/services/tenant-not-sent.spec.ts` внутри `npm run test:unit`:
по всем `src/services/**/*.ts` и `src/composables/**/*.ts` не встречается ни `X-Tenant`,
ни присваивание `tenant_id` в тело запроса. Разрешённый список — три места §2.7
(`types/auth.ts`, `useAuth.ts`, мок), и они разрешены **как чтение ответа**.

### 5.5. Уникальность парой

Разбором AST по всем `shared/models.py` и `app/core/uploads/models.py`: у класса категории А
каждое `UniqueConstraint` и каждый `unique=True` удовлетворяет одной из двух форм Т6 —
начинается с `tenant_id` **либо** содержит FK на строку другой таблицы категории А. Вторая
форма проверяется машинно: имя колонки оканчивается на `_id` и у той же модели объявлен
`ForeignKey` на таблицу, стоящую в списке категории А.

Стартовые исключения — два: `secret_link_token` (глобально уникальный секрет,
[`backend/app/modules/auth/shared/models.py:57-59`](../../../backend/app/modules/auth/shared/models.py))
и warehouse №28 с номером находки. `uq_product_field_value` и `uq_user_role` в исключения
**не входят**: они законны по второй форме, и тест обязан пропускать их молча — иначе домен
получит красный на верном коде и «починит» его миграцией (§2.6, поправка второго замера).

### 5.6. Графа «Мультиарендность» заполнена во всех семнадцати доменах

Бэкендная приёмка §5.1–§5.5 не видит семь доменов: модуля у них нет, функций нет, красить
нечего. Их вклад в тему — раздел контракта, и проверяется он по документам:

```
cd roo_code/roo-context/api && for f in analytics audit-feed auth bcc categories clients \
  config finance notifications orders products sales-crm services settings suppliers \
  uploads warehouse; do
  printf '%s %s\n' "$f" "$(grep -cE '^[-*#[:space:]]*(\*\*)?([0-9]+\.[[:space:]]*)?Мультиарендность' "$f.md")"
done
```

Замер 2026-09-13 (`/tmp/proof-tenancy-graph6.txt`): `1` у всех семнадцати, доменов без
графы — 0. Регулярное выражение терпит все три формы заголовка, которые в контракте уже
встречаются (`**Мультиарендность.**`, `- **Мультиарендность.**`, `### 6. Мультиарендность`)
и не ловит упоминания слова внутри абзаца и в таблицах — проверено на `services.md` и
`warehouse.md`, где такие упоминания есть.

### 5.7. Приёмка самого этого файла

```
cd frontend_vue && CONTRACT_REFS=roo_code/plans/general/сквозное-tenancy-план.md \
  npx vitest run src/services/contractRefs.spec.ts
```

Ожидание — «битых 0».

---

## 6. Что здесь НЕЛЬЗЯ решить до доменных планов

**Механизм не блокирован ничем.** Т1–Т8 выведены полностью, исключения перечислены поимённо,
сторож описан до состава списков — домен может применить правило, не дожидаясь ни одного
другого домена. Соглашения сами говорят, что это свойство проекта, а не домена, и замер §2
это подтвердил: правило одинаково читается для всех семнадцати.

Блокированы **не правила, а три перечня плюс один вопрос**. Ни один из них не нужен домену,
чтобы выполнить §4: они собираются *из* доменных планов, а не *до* них.

**1. Состав сеяния справочников нового арендатора (П25).**
Решено, что сеет миграция. Что именно она сеет — список валют, единиц, правил пересчёта и
статусов заказа — не назначено нигде: `op.bulk_insert` во всех миграциях ровно один, и это
фичи тарифов. Эталон состава существует только в моке и только по статусам (15 штук). Список
для остальных трёх справочников физически не существует до планов `settings` и `config` —
выдумать его здесь значит записать в контракт данные, которых владелец не видел.

**2. Перечень уникальностей, обязанных стать парой с арендатором.**
Правило Т6 полное. Перечень — нет: сегодня тенантских таблиц с уникальностью одиннадцать, а
доменов семнадцать, и девять из них бэкенда не имеют вовсе (`orders` 33 эндпоинта из 33 без
бэкенда, `warehouse` 37 из 37, `clients` 10 из 10, `config` 12 из 12). Естественные ключи
этих таблиц — свойство домена: что уникально у заказа, знает план заказов. Два известных
нарушения названы в §2.6; полный список собирается из Д5 семнадцати планов.

**3. Пополнение закрытых списков за пределами сегодняшних 37 функций и 45 моделей.**
Стартовый состав измерен и записан — и `TENANTLESS`/`KNOWN_GAPS` по функциям, и
`TENANT_TABLES_EXEMPT` по таблицам. Дальнейшие строки появляются по одной вместе со слайсом
или таблицей, которых ещё нет, и каждая требует причины — то есть решения домена, а не общего
правила. Отдельно это касается категории В: сегодня платформенных таблиц четыре, все в
`billing` и `auth`, а будет ли платформенной, скажем, общий справочник стран или валют мира —
решает план того домена, который его заведёт. Т8 даёт **правило выбора**; результат выбора
он дать не может. Это не дыра плана, а его устройство: закрытый список, который **может**
пополняться только осознанно, и есть способ не потерять исключение.

**4. Как вход выбирает арендатора, когда одна почта заведена в двух компаниях.**
Это единственное место, где решение владельца П58 **опережает** механизм и ломает его.
П58 прямо назначает: «один и тот же адрес может быть приглашён в две разные компании». Тогда
`get_user_by_email` на входе ([`backend/app/modules/auth/features/login/repository.py:15`](../../../backend/app/modules/auth/features/login/repository.py))
возвращает одну запись из двух — и какую, не определено ни решением, ни контрактом. Т4
исключением это не покрывает: у запроса на входе арендатора нет по построению, но и
однозначного ответа у него теперь тоже нет. Варианты (выбор компании вторым шагом входа;
почта уникальна глобально вопреки П58; вход по паре «почта + компания») — это форма экрана
входа, то есть предмет плана домена `auth`, а не сквозного правила. **Здесь этот вопрос
называется и оставляется открытым; догадка была бы хуже дыры.**

---

## Задачи (самостоятельные, для автономного прогона)

| № | Задача | Зависит от | Приёмка |
|---|---|---|---|
| С-1 | Написать `backend/tests/test_tenant_scope.py` по §5.1 со стартовыми списками из §2.3 | ничего | `cd backend && python3 -m unittest discover -s tests -t .` — зелено, счётчик ≥ 37 |
| С-2 | Добавить в тот же файл проверки §5.2, §5.3, §5.5 | С-1 | тот же прогон |
| С-3 | Написать `frontend_vue/src/services/tenant-not-sent.spec.ts` по §5.4 | ничего (независима от С-1) | `cd frontend_vue && npm run test:unit` |
| С-4 | Завести три неоформленные находки §2.3(в) — `uploads/get_file_by_id`, `uploads/delete_file`, `settings/profile/update_user` — в баг-файлы соответствующих доменов | ничего | строки есть в баг-файлах, номера подставлены в `KNOWN_GAPS` |
| С-5 | Добавить в тот же файл `test_every_model_has_a_category` (§5.1, шестой тест) со списком `TENANT_TABLES_EXEMPT` из §2.1 | С-1 | тот же прогон, пол `models_seen ≥ 45` |
| С-6 | Вставить блок §4 в семнадцать доменных планов копией | существование доменных планов | `grep -rl "Мультиарендность (сквозное правило" roo_code/plans` → 17 файлов |

С-1 и С-3 независимы и гонятся параллельно. Починка семи дыр §2.3(в) в этот план **не
входит**: план ставит сторожа и перечисляет дыры номерами; чинит их доменный прогон, и
сторож покраснеет, если починку заявят, не сделав.

---

## Чем проверен этот файл

Второй замер, 2026-09-13. Каждое число §2 снято командой заново, не перенесено из первой
редакции:

| что | чем | где вывод |
|---|---|---|
| 37 / 22 / 15 функций с запросом | разбор `ast` по `backend/app/**/*.py` | `/tmp/proof-tenancy-sweep.txt` |
| 45 моделей, 6 без `tenant_id` | разбор `ast` по `app/**/models.py` | `/tmp/proof-tenancy-models.txt` |
| сторож зелёный, 14 тестов | `cd backend && python3 -m unittest discover -s tests -t .` | `/tmp/proof-tenancy-backend-tests.txt` |
| графа 6 у 17 доменов | цикл §5.6 | `/tmp/proof-tenancy-graph6.txt` |
| ссылки этого файла | `CONTRACT_REFS=… npx vitest run src/services/contractRefs.spec.ts` | «битых 0» |

Свип воспроизвёлся строка в строку — те же 37, те же 22, те же пятнадцать имён в том же
разбиении 7 + 1 + 7. Правки второго замера три, и каждая названа на месте: перепись моделей
и три категории (§2.1, Т8, шестой тест §5.1), решение П62 (§1), снятая ложная находка
`uq_product_field_value` (§2.6, Т6). Ложная находка — единственная правка, которая что-то
**убирает**: первая редакция требовала бы от `products` миграции, чинящей верный констрейнт.

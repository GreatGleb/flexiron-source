# Сквозное: идемпотентность и оптимистичная блокировка (§11)

**Тип:** сквозной план. Не план страницы и не план домена — один свод правил, который
семнадцать доменных агентов **применяют**, а не выводят каждый по-своему.
**Тема:** §11 общих соглашений — `Idempotency-Key`, версия записи, граница транзакции Save,
и примыкающие к ним решения П43, П46–П50.
**Написан:** 2026-09-12, на ветке `fix/contract-bugs-2026-09-09`. Все замеры ниже сделаны в этот
день командами, которые приведены рядом с числом.
**Перепроверен:** 2026-09-13, теми же командами — вывод в `/tmp/proof-idempotency.txt`. Сошлось
всё: пять вызовов с ключом, 40 блоков `POST`, 56/22 по кнопкам, ноль упоминаний идемпотентности и
`If-Match` на бэкенде. Три места дополнены — 2.6, 2.7 и самопроверка теста 3.1.
**Код этим планом не меняется ни строкой.** План даёт правило, форму записи и гейт.

---

## 0. Как этим пользоваться

Домен читает разделы 3–5 и копирует раздел 4 в свой план. Раздел 6 говорит, чего в этом
плане нет и почему — не потому, что забыли, а потому, что до доменных планов такого
перечня не существует.

Три вещи, которые домен **не решает сам**, даже если «в его случае логичнее иначе»: срок
жизни ключа, область действия ключа и политика версии. Расхождение в них между доменами —
это не гибкость, а второй экземпляр правила, который молча разойдётся с первым.

---

## 1. Что решено владельцем

Источник — `roo_code/plans/api/audit/00-решения-владельца.md`, куст «транзакционность»
от 2026-09-09. Цитаты дословные; сверено побайтово с источником (`md5sum` блоков П43, П46–П50
совпал, расхождение только в заголовке, которым блок обрывается).

### П43 · Атомарность — выборочно: заказ и склад целиком, справочники и настройки как есть

> Save применяется одной транзакцией там, где полумера опасна, — **заказ и склад**. Справочники и
> настройки остаются как сегодня: череда независимых запросов, частичное сохранение возможно, и
> контракт называет это прямо, а не умалчивает.

> Следствие для контракта: у заказа и склада Save обязан принимать всё одним запросом и применяться
> целиком либо не применяться вовсе; у справочников и настроек контракт пишет прямо, что порядок не
> задан и частичное сохранение возможно.

### П46 · Ключ идемпотентности — живёт сутки, область действия «ключ + операция»

> Срок хранения — **24 часа**. Ключ считается использованным **для конкретной операции**, а не
> вообще: один и тот же ключ на отгрузке и на платеже — два разных случая.

> Замер: кэш ключей у мока — `Map` на процесс без срока и без привязки к пути
> (`frontend_vue/src/services/mocks/index.ts:259-269`), то есть сегодня повтор ключа на другой
> операции вернул бы чужой ответ. Правило «необратимый `POST` требует ключ, остальные — нет»
> уже записано (§11 соглашений); П46 закрывает два его пробела.

### П47 · Двойная отправка гасится и на клиенте: кнопка заморожена до ответа сервера

> Правило владельца, заданное как сквозное и обязательное **везде на всех страницах**: как только
> фронтенд отправил данные, кнопка замораживается и второй раз нажать нельзя, пока не придёт ответ
> сервера.

> **Когда её делать — решено 2026-09-09: позже.** Откладывается **детальный план реализации** — тот,
> что требует отдельного разбора кода по каждой из отправок; его пишут, когда кусты решений
> кончатся. Само правило откладыванию не подлежит: оно записано в контракт наравне с остальными
> решениями (§15 соглашений), потому что несёт обязанность сервера — ответ обязан быть достаточным,
> чтобы клиент разморозил кнопку и обновил экран без второго запроса.

### П48 · BCC: сначала запись, потом отправка; ответ несёт созданные строки

> Порядок — **сначала записать, потом отправить**. Упала отправка — запись остаётся, помеченная как
> неотправленная, и её можно повторить. Обратный порядок хуже: письмо не вернёшь.

> Второй вопрос того же места (БАГ-11) решён так же прямо: сервер **возвращает созданные строки
> прямо в ответе** — на один запрос меньше, и список обновляется мгновенно, без перечитывания
> истории.

### П49 · Карточка клиента и финансовая запись атомарными не становятся

> Заданы отдельно, ответ на обе один — **как есть**, без общей транзакции. Тем самым перечень
> атомарного закрыт: атомарны ровно заказ и склад, всё остальное — череда независимых запросов,
> частичное сохранение возможно, и контракт называет это прямо.

> Что остаётся дефектом и П49 не оправдывает: `load()` после ошибки не вызывается у клиента
> (`useClientCard.ts:314-316`), поэтому экран молча расходится с базой, — а у карточки исходящего
> платежа ровно наоборот, `catch { load() }` разрушает несохранённое
> (`OutgoingPaymentCardPage.vue:83-85`).

### П50 · Повтор файла отсеивает фронт, сервер о нём не знает

> Повторная загрузка того же файла **ошибкой сервера не является**. Отсев делает клиент: браузер
> различает файл, поданный в одну форму дважды, и второй экземпляр в отправку не идёт. На сервере
> ничего не меняется — ни хеша содержимого, ни проверки дубля, ни нового кода ошибки.

> `File` несёт **имя, размер и дату изменения**, содержимого до чтения не видно.
> Граница действия: **одна форма, один сеанс**.

### Снято прежними решениями, владельцу не задавалось

Дословно из того же куста — таблица «Снято этим кустом»:

| Строка | Чем закрыта |
|---|---|
| нужен ли `Idempotency-Key` в настройках, у поставщиков, на загрузке файла | §11 соглашений: необратимый `POST` требует ключ, остальные — нет; создание валюты или поставщика обратимо |
| нужна ли карточке поставщика проверка версии `If-Match` | §11: версия спрашивается только в заказах, в остальных шестнадцати доменах last-write-wins |

И вывод куста, тоже дословно:

> **Куст закрыт целиком:** 18 строк аудита, восемь решений владельца (П43–П50), шесть строк снято
> прежними решениями, одна признана работой.

---

## 2. Что есть в коде сейчас

Замеры 2026-09-12. Числа §11 соглашений и куста решений местами **разошлись со строками**: файлы
сдвинулись после решения. Ниже — собственные замеры этого плана, и ссылки в нём указывают на
сегодняшние строки, а не на те, что записаны в соглашениях.

### 2.1. Ключ идемпотентности на клиенте — пять вызовов, все `POST`

Генератор один: `newIdempotencyKey` (`frontend_vue/src/services/api.ts:301-307`) — `crypto.randomUUID()`
с запасной ветвью для мока. Шлют его пять мест, и только они:

| вызов | где |
|---|---|
| отправка письма BCC | `frontend_vue/src/services/bccService.ts:43` |
| запись письма в историю BCC | `frontend_vue/src/services/bccService.ts:64` |
| создание отгрузки | `frontend_vue/src/services/ordersService.ts:298` |
| создание платежа | `frontend_vue/src/services/ordersService.ts:355` |
| возврат по заказу | `frontend_vue/src/services/ordersService.ts:389` |

Команда замера и её вывод — `/tmp/proof-idem-now.txt`, строка «вызовов с ключом: 5».

### 2.2. Ключ у мока — кэш без срока и без привязки к операции

`withIdempotency` (`frontend_vue/src/services/mocks/index.ts:320-330`) хранит ответы в
`idempotencyCache` — `Map` на процесс (`:321`). Из этого следуют ровно два пробела, которые и
закрывает П46: **срока у записи нет** и **ключ не связан ни с методом, ни с путём**, то есть один
ключ, посланный на отгрузку и на платёж, сегодня вернул бы первый ответ на оба. Вызывают обёртку
пять веток мока — `:1011`, `:1018`, `:1135`, `:1142`, `:1151`, — столько же, сколько мест шлёт ключ.

### 2.3. Ключ на бэкенде — нет нигде

Доказательство: `grep -rni "idempotenc" backend/` → **0 совпадений**. Ни таблицы ключей, ни
middleware, ни чтения заголовка. То же и с оптимистичной блокировкой: `grep -rni "if-match\|if_match\|etag" backend/` → **0**.
Вывод обеих команд — `/tmp/proof-idem-now.txt`.

Это значит: §11 сегодня — правило **для фронта и мока**, у сервера его нет даже в зачатке. Любой
доменный раздел, который напишет «сервер возвращает закэшированный ответ», описывает будущую
обязанность, а не измеренное поведение, и обязан это назвать.

### 2.4. Версия записи — один домен из семнадцати

- Тип несёт поле: `frontend_vue/src/types/order.ts:535-547` — `version`, необязательное, и
  отсутствие означает «клиент версии не видел», а не «версия ноль».
- Заголовок ставит `ifMatch` (`frontend_vue/src/services/ordersService.ts:41-43`); шлют его шесть
  удалений заказа — `:116`, `:168`, `:190`, `:198`, `:214`, `:398`.
- Мок читает заголовок в `ifMatchVersion` (`frontend_vue/src/services/mocks/index.ts:1540-1545`) и
  подставляет его в разбор `DELETE` (`:1548`); **нет заголовка — проверки нет**.
- Отказ один: `ORDER_VERSION_CONFLICT` — бросает мок (`frontend_vue/src/services/mocks/orders.ts:1947`),
  переводит `frontend_vue/src/services/orderLineEdits.ts:374`, обрабатывает карточка
  (`frontend_vue/src/composables/useOrderCard.ts:542`): тост, сброс очереди, перезагрузка.
- **У одного из шести удалений версия не шлётся, и это не дефект.** Удаление записи истории
  вызывают двое: карточка заказа — с версией (`frontend_vue/src/composables/useOrderCard.ts:342`,
  `deleteOrderAuditEntry(id, entryId, atVersion())`), общая лента — без неё
  (`frontend_vue/src/services/auditFeedService.ts:52`, та же функция двумя аргументами против
  трёх у объявления `frontend_vue/src/services/ordersService.ts:193-199`). §11 соглашений называет
  это правилом, а не упущением: «удаление записи истории из общей ленты от проверки версии
  освобождено» (`roo_code/roo-context/api/00-conventions.md:885-886`) — тот, кто заказа не
  открывал, версии заявить не может.

В остальных шестнадцати доменах версии нет ни во фронте, ни в схеме — поведение last-write-wins.

### 2.5. Атомарность Save — сегодня её нет нигде, включая два домена, которым она назначена

| домен | Save сегодня | где |
|---|---|---|
| orders | `1 + N + M + K + L` последовательных запросов, начиная с `saveFormFields` | `frontend_vue/src/composables/useOrderCard.ts:419-532` |
| warehouse | два запроса без общей границы: `patchBatch` (`frontend_vue/src/composables/useWarehouseBatch.ts:256`), следом `createMovement` (`:263`) | там же |
| clients | `1 + N + M`, начиная с `clientDelta` | `frontend_vue/src/composables/useClientCard.ts:273-304` |
| settings | до десятка параллельных — `Promise.all` | `frontend_vue/src/composables/useSettings.ts:518` |
| categories | два параллельных — `Promise.all` | `frontend_vue/src/composables/useCategoryCard.ts:102-112` |
| finance | один PATCH плюс независимый аплоад до Save; после ошибки зовётся `load()` | `frontend_vue/src/views/admin/finance/OutgoingPaymentCardPage.vue:90-91` |

Два дефекта на противоположных краях, названные П49 и **не оправданные** им: у клиента после
ошибки `load()` не зовётся вовсе — в `catch` только `toast.error` (`frontend_vue/src/composables/useClientCard.ts:314-316`), а у
исходящего платежа `catch` зовёт `load()` и разрушает несохранённое (`OutgoingPaymentCardPage.vue:90-91`).

### 2.6. Заморозка кнопки (П47) — больше половины мест не защищены

Замер 2026-09-12 своими командами (они в `/tmp/proof-idem-now.txt`):

| что мерялось | команда | сегодня |
|---|---|---|
| файлов с `<button>` | `grep -rl "<button" src --include=*.vue \| wc -l` | 56 |
| из них морозят кнопку флагом занятости | тот же список, `:disabled` со `saving\|loading\|busy\|pending\|submitting` | 22 |
| композаблов (`use*.ts`, без спек) | `find src -name "use*.ts" ! -name "*.spec.ts"` | 44 |
| из них заводят флаг `ref(false)` | тот же список с `grep -l "ref(false)"` | 34 |

Числа соглашений («22 из 56» и «18 из 51») по кнопкам сошлись, по композаблам — нет: у них другой
счёт файлов и другой шаблон флага. Перепроверка 2026-09-13 объяснила расхождение наполовину:
**числитель 18 воспроизводится точно** тем шаблоном, который назван в §15 соглашений
(`grep -l 'saving\|isSaving\|submitting\|busy'` по `src/composables/*.ts` без спек → 18), а
знаменатель 51 не воспроизводится **ни одним** из двух счётов — без спек их 44, со спеками 52.
То есть старое число получено третьим способом, и какой это способ, восстановить нечем.

Это расхождение **не чинится этим планом**: П47 отложен
владельцем как отдельная работа. Здесь оно зафиксировано, чтобы доменный агент не принял старое
число за измеренное сегодня.

### 2.7. Отсев повторного файла (П50) — нет вовсе

`handleFiles` грузит всё, что дали, одним `Promise.all`
(`frontend_vue/src/components/admin/ui/DropZone.vue:33-43`): два одинаковых файла дают две записи.
Место правки одно на весь проект, а вот потребителей у компонента **тринадцать**, не десять, как
записано в решении: `grep -rl 'DropZone' frontend_vue/src --include=*.vue | grep -v 'ui/DropZone.vue' | wc -l`
→ `13` (замер 2026-09-13). На объём работы это не влияет — правка одна, — но доменный агент,
который пойдёт искать десять мест, найдёт тринадцать и решит, что ошибся файлом.

### 2.8. Разметки в контракте нет ни одной

| маркер | сколько раз встречается в `roo_code/roo-context/api/*.md` и контракте заказов |
|---|---|
| `**Ключ идемпотентности:**` | 0 |
| `**Атомарность Save:**` | 0 |
| `**Версия записи:**` | 0 |
| `**Ответ достаточен для разморозки:**` | 0 |

Блоков эндпоинтов в контракте — **176**, из них `POST` — **40**, и **ни один** не помечен
(`/tmp/proof-idem-blocks.txt`). Абзац «Транзакционность и идемпотентность» есть во всех 17 доменных
файлах — то есть тема везде затронута прозой, но **машинно не проверяется ничем**.

---

## 3. Правило, единое для всех доменов

Формулировки ниже — законченные. Домен их **применяет**: подставляет свои эндпоинты и получает
однозначный ответ. Где ответ неоднозначен, это сказано прямо, и такое место уходит в раздел 6.

### 3.1. Тест необратимости — решающий, а не обсуждаемый

Операция **необратима**, если выполняется хотя бы одно:

1. она двигает остаток на складе или выпускает складское движение;
2. она двигает деньги: платёж, возврат, счёт, корректировка счёта;
3. она отправляет наружу то, что не отзывается: письмо, документ клиенту;
4. она создаёт запись, которую **интерфейс не даёт удалить** (в домене нет `DELETE` на неё и нет
   действия отмены).

Иначе операция **обратима**: повтор виден человеку как лишняя строка, и он её удаляет.

Три уточнения, снимающие спор до того, как он начнётся:

- «Обратима» — свойство **контракта**, а не удобства. Если `DELETE` есть, но правами закрыт для
  роли, которая делает `POST`, — для неё операция необратима.
- Создание записи справочника (валюта, единица, поставщик, категория, товар, услуга) **обратимо**:
  так решено в кусте, и домен это решение не пересматривает.
- Сомнение решается в пользу «необратима». Лишний ключ стоит одной строки заголовка; пропущенный —
  второй отгрузки.

**Самопроверка теста (2026-09-13).** Тест обязан воспроизводить те вердикты, которые известны
независимо от него, — иначе он не тест, а мнение. Известных вердиктов семь, и тест даёт ровно их:

| эндпоинт | какой признак 3.1 срабатывает | известный вердикт | совпало |
|---|---|---|---|
| `POST /api/bcc/send` | 3 — письмо не отзывается | ключ шлётся (`frontend_vue/src/services/bccService.ts:43`) | да |
| `POST /api/bcc/log` | 4 — разделов удаления в bcc.md ноль | ключ шлётся (`frontend_vue/src/services/bccService.ts:64`) | да |
| `POST /api/orders/:id/shipments` | 1 — движение склада | ключ шлётся (`frontend_vue/src/services/ordersService.ts:298`) | да |
| `POST /api/orders/:id/payments` | 2 — деньги | ключ шлётся (`frontend_vue/src/services/ordersService.ts:355`) | да |
| `POST /api/orders/:id/returns` | 1 и 2 | ключ шлётся (`frontend_vue/src/services/ordersService.ts:389`) | да |
| `POST /api/orders/:id/shipments/:id/cancel` | 1 — двигает склад | БАГ-09: ключа нет, это нарушение | да |
| `POST /api/bcc/events/:eventId/response` и `.../no-response` | 4 — удалить строку нечем | БАГ-03: ключа нет, это нарушение | да |

Семь из семи. На двух последних тест даёт «необратима» там, где код ключа не шлёт, — и это ровно
те два места, которые прогон уже завёл как баги, то есть тест воспроизводит находки, а не спорит с
ними. Замер «`DELETE`-блоков в `bcc.md` ноль»: `grep -c '^### DELETE ' roo_code/roo-context/api/bcc.md` → `0`.

### 3.2. Ключ: как едет, чем является, сколько живёт

- **Транспорт** — заголовок `Idempotency-Key`. Другого места у него нет: ни поля в теле, ни query.
- **Значение** — UUID v4, который генерирует клиент на каждый **пользовательский жест**, а не на
  каждую попытку: повтор после обрыва связи шлёт **тот же** ключ. Генератор один на проект —
  `newIdempotencyKey` (`frontend_vue/src/services/api.ts:301-307`), своих домен не заводит.
- **Срок жизни записи — 24 часа** от момента, когда сервер её принял (П46). Ни один домен не
  назначает себе другой срок.
- **Чем запись адресуется** — четвёркой `(tenant_id, Idempotency-Key, HTTP-метод, путь запроса)`.
  Арендатор в ключе обязателен по §4: он берётся из токена, как `_get_tenant_id`
  (`backend/app/core/uploads/action.py:62-76`), и в теле запроса его нет ни у одного из вызовов.
- **Тело в адресацию не входит.** Отсюда прямое следствие, которое домен обязан знать: тот же ключ
  с другим телом на том же методе и пути получает **первый** ответ, а не ошибку.

### 3.3. Что сервер делает с повтором

1. Ключа нет, а операция помечена «обязателен» → **отказ**, `VALIDATION_ERROR`, 422
   (`backend/app/core/exceptions.py:23-27`). Молча выполнять нельзя: правило, которое можно
   обойти, не написав заголовка, — это не правило.
2. Ключ есть, записи нет → операция выполняется, ответ **и его статус** сохраняются под четвёркой
   из 3.2 на 24 часа.
3. Ключ есть, запись есть и завершена → возвращается **сохранённый ответ дословно**: тот же статус,
   то же тело. Операция не выполняется второй раз. Отказ кэшируется наравне с успехом — повтор
   отказавшей операции обязан отказать так же, иначе «повтор» превращается в «вторая попытка».
4. Ключ есть, запись есть, но первая операция **ещё не завершилась** → `CONFLICT`, 409
   (`backend/app/core/exceptions.py:44-48`). См. уточнение Р3 ниже: это дорисовка прогона, а не
   решение владельца, и меняется она в одном месте — здесь.
5. Ключ прислан на операцию, которой он не нужен, → **принимается и игнорируется**. Отказывать
   нельзя: клиент вправе слать ключ шире, чем требует контракт.

### 3.4. Версия записи: один домен ведёт, шестнадцать не ведут

- **Заказы.** Версия обязательна на каждой мутации, сделанной **из карточки заказа**. Транспорт:
  у запроса с телом — поле `version` в теле; у `DELETE` тела нет, и версия едет в `If-Match`
  (`frontend_vue/src/services/ordersService.ts:41-43`).
  Отсутствие того и другого означает «клиент версии не читал» и проверку **не включает**
  — так читает заголовок `ifMatchVersion` (`frontend_vue/src/services/mocks/index.ts:1540-1545`).
  Несовпадение версий → `ORDER_VERSION_CONFLICT`, 409, и **не пишется ничего**.
- **Единственное исключение, и оно уже названо §11: общая лента истории.**
  `DELETE /api/orders/:id/audit/:entryId`, вызванный из общей ленты, версии не несёт
  (`frontend_vue/src/services/auditFeedService.ts:52`) и обязан выполниться. Сервер поэтому
  заголовок **не требует**: у мутации заказа он не «обязательный параметр запроса», а
  предусловие, которое заявляет тот, кто заказ читал. Домен `audit-feed` пишет строку Д4 в форме
  «не ведётся» и добавляет к ней эту фразу; домен `orders` называет исключение в блоке этого
  эндпоинта. Расширять исключение на другие вызовы нельзя: список закрыт одной строкой §11
  (`roo_code/roo-context/api/00-conventions.md:885-886`).
- **Остальные шестнадцать.** Версии нет, `If-Match` не шлётся и сервером не читается, поведение —
  last-write-wins, и доменный файл называет это **прямо**, а не умалчивает.
- Домен **не заводит** себе версию по своей инициативе. Нашёл место, где потеря правки стоит
  дорого, — пишет находку в свой баг-файл и оставляет решение владельцу. Контракт при этом
  по-прежнему говорит last-write-wins.

### 3.5. Атомарность Save: перечень закрыт, и он состоит из двух доменов

- **`orders` и `warehouse`:** Save обязан приходить **одним запросом** и применяться целиком либо
  не применяться вовсе (П43, дословно).
- **Все остальные пятнадцать:** череда независимых запросов, **порядок не задан**, частичное
  сохранение возможно — и доменный файл это называет прямо (П43, П49).
- Перечень **закрыт**. Домен в него себя не дописывает и из него себя не вычёркивает.
- «Как есть» относится к **границе транзакции**, а не к поведению экрана после отказа. Отсутствие
  `load()` после ошибки и `catch { load() }`, разрушающий несохранённое, — дефекты в обе стороны
  (П49), и они остаются дефектами.

### 3.6. Обязанность ответа (серверная половина П47)

Ответ на любую отправку данных обязан быть **достаточен, чтобы клиент разморозил кнопку и обновил
экран без второго запроса**. Практически это значит: ответ несёт созданную или изменённую сущность
целиком в той форме, в какой её показывает экран, — а не `204`, не `{ ok: true }` и не один
идентификатор, за которым придётся идти `GET`-ом.

Частный случай, решённый поимённо: BCC — **сначала запись, потом отправка**, и ответ **несёт
созданные строки** (П48).

Сам обход всех отправок во фронте владельцем отложен. Отложен **разбор кода и план правки**, а не
обязанность сервера: она пишется в контракт сейчас.

### 3.7. Повторный файл — дело клиента

Сервер про дубликаты файлов не знает: ни хеша, ни проверки, ни кода отказа (П50). Отсев делает
клиент по тройке **имя + размер + дата изменения**, в границах «одна форма, один сеанс», в
единственном месте — `frontend_vue/src/components/admin/ui/DropZone.vue:33-41`.
Доменный контракт **не описывает** отказ на повторный файл, потому что его нет.

### 3.8. Чего домену делать нельзя

1. Назначать свой срок жизни ключа, отличный от 24 часов.
2. Заводить версию записи вне заказов или слать `If-Match` из другого домена.
3. Вводить серверную проверку дубликата файла или код отказа на неё.
4. Записывать в контракт как измеренное то, чего на бэкенде нет: ключа там нет вовсе (2.3).
5. Объявлять свой Save атомарным (или неатомарным) вопреки 3.5.
6. Заводить доменный код отказа, который является подстрокой другого кода, — фронт местами
   сравнивает код подстрокой (§2 соглашений).

### 3.9. Уточнения прогона: что дорисовано и где это менять

Четыре места, где решение владельца не доходит до однозначного поведения. Они дорисованы **здесь**,
в одном экземпляре, и домен их не варьирует. Владелец захочет иначе — правится одна строка этого
раздела и ни одной в семнадцати доменных файлах.

| № | Что дорисовано | Из чего выведено |
|---|---|---|
| Р1 | «Операция» в П46 читается как пара «метод + путь запроса», с идентификаторами. | Пример владельца («отгрузка и платёж — два разных случая») выполняется при обоих чтениях. Различие видно только у клиента, который шлёт один ключ на два разных заказа, а наш клиент так не умеет: ключ рождается на вызов (`frontend_vue/src/services/api.ts:301-307`). Выбрано более узкое чтение — оно не может вернуть чужой ответ. |
| Р2 | Отсутствие обязательного ключа → `VALIDATION_ERROR`, 422. | Каталог кодов §2 закрыт пятью кодами ядра; отсутствующий обязательный заголовок — отказ входного разбора, а это `ValidationError` (`backend/app/core/exceptions.py:23-27`). Нового кода тема не вводит. |
| Р3 | Повтор, пришедший до завершения первого, → `CONFLICT`, 409. | П46 задаёт кэш **ответов**; про повтор без ответа он молчит. Владелец при этом прямо отдаёт двойной клик клиенту (П47), то есть серверу остаётся редкий случай гонки. 409 — единственный код каталога, означающий «состояние сейчас этого не позволяет» (`backend/app/core/exceptions.py:44-48`). |
| Р4 | Кэшируются и отказы, не только успехи. | П46 говорит «кэш ответов». Отказ — тоже ответ; иначе повтор после обрыва выполнял бы операцию, которая первый раз была отклонена. |

---

## 4. Что обязан сделать каждый домен

Копируется в доменный план как есть. «Домен» здесь — один файл `roo_code/roo-context/api/<домен>.md`.
У заказов вторая половина контракта живёт в `roo_code/plans/orders/orders-backend-contract.md`, но
блоков вида `### POST /api/...` там **ноль** (замер в `/tmp/proof-idem-now.txt`): все 13 `POST`
заказов размечаются в `roo_code/roo-context/api/orders.md`. Второй файл остаётся местом
рассуждения (§3, §6) и в гейт Г1 включён на будущее — чтобы заголовок, если он там появится, не
проехал мимо разметки.

### Д1. Пометить каждый `POST` своего файла

В блоке каждого `### POST /api/...` — ровно одна строка, дословно в одной из двух форм:

```
**Ключ идемпотентности:** обязателен — <что произойдёт при повторе, с файл:строка>
**Ключ идемпотентности:** не требуется — операция обратима (<чем именно: DELETE, отмена, правка>)
```

Решение принимается тестом 3.1, а не вкусом. Не-`POST` блоки не помечаются.

### Д2. Обосновать каждое «обязателен» ценой повтора

Не «на всякий случай», а что именно случится: вторая отгрузка и второе движение склада, второй
платёж, второе письмо. С ссылкой `файл:строка` на то место мока или бэкенда, где это видно.

### Д3. Написать одну строку про атомарность Save

Ровно одна на файл, дословно в одной из двух форм:

```
**Атомарность Save:** одной транзакцией — весь Save приходит одним запросом и применяется целиком либо не применяется (П43)
**Атомарность Save:** отдельными запросами — порядок не задан, частичное сохранение возможно (П43, П49)
```

Первая форма — только у `orders` и `warehouse`. У остальных пятнадцати — вторая. У домена без
Save-формы (`analytics`, `audit-feed`, `sales-crm`) пишется вторая форма с добавкой «Save-формы в
домене нет».

### Д4. Написать одну строку про версию записи

```
**Версия записи:** ведётся — версия обязательна на каждой мутации, несовпадение → `ORDER_VERSION_CONFLICT`, 409 (§11)
**Версия записи:** не ведётся — last-write-wins, `If-Match` не шлётся и не читается (§11)
```

Первая форма — только у `orders`. У остальных шестнадцати — вторая.

Две добавки, обе обязательные, потому что иначе домен начнёт их выводить сам (3.4):

- `orders` — в блоке `### DELETE /api/orders/:id/audit/:entryId` дописывает строку
  `**Версия записи:** не спрашивается при вызове из общей ленты (§11)`;
- `audit-feed` — к своей строке «не ведётся» дописывает «удаление записи истории версию не шлёт
  и от проверки освобождено (§11)».

### Д5. Проверить достаточность ответа у каждой отправки

По каждому `POST`/`PATCH`/`PUT`/`DELETE` домена — хватает ли ответа, чтобы разморозить кнопку и
обновить экран без второго запроса (3.6). Достаточен — ничего не пишется. Недостаточен — строка:

```
**Ответ достаточен для разморозки:** нет — <чего не хватает>; сегодня клиент добирает <чем> (файл:строка)
```

Это наблюдение, а не работа: правку П47 владелец отложил.

### Д6. Свести с тем, что уже написано в разделе «Транзакционность и идемпотентность»

Абзац с таким заголовком уже есть во всех 17 файлах. Если он противоречит разметке Д1–Д4 —
правится абзац, а не разметка. Числа в нём, привязанные к строкам файлов, перепроверяются:
часть ссылок §11 и доменных абзацев **уже сдвинулась** (2.2, сноска в начале раздела 2).

### Д7. Нарушения — в баг-файл, не в контракт

Место, где код расходится с правилом, описывается как есть, а находка уходит в
`roo_code/plans/bugs/contract-sync-<домен>-bugs.md`. Уже заведённые по этой теме:

- `roo_code/plans/bugs/contract-sync-orders-bugs.md`, БАГ-09 — отмена отгрузки не шлёт ключ;
- `roo_code/plans/bugs/contract-sync-bcc-bugs.md`, БАГ-03 — `response` и `no-response` создают
  строки без ключа.

Новые нарушения нумеруются дальше по своему файлу; эти два не дублируются.

### Д8. Ничего из раздела 3.8 не делать

### Распределение по доменам — сколько `POST` кому размечать

Замер `grep -cE "^### POST /api/" roo_code/roo-context/api/<домен>.md` на 2026-09-12:

| домен | `POST` | атомарность Save | версия |
|---|---|---|---|
| orders | 13 | одной транзакцией | ведётся |
| settings | 6 | отдельными запросами | не ведётся |
| warehouse | 5 | одной транзакцией | не ведётся |
| bcc | 4 | отдельными запросами | не ведётся |
| auth | 3 | отдельными запросами | не ведётся |
| clients | 2 | отдельными запросами | не ведётся |
| config | 2 | отдельными запросами | не ведётся |
| categories | 1 | отдельными запросами | не ведётся |
| products | 1 | отдельными запросами | не ведётся |
| services | 1 | отдельными запросами | не ведётся |
| suppliers | 1 | отдельными запросами | не ведётся |
| uploads | 1 | отдельными запросами | не ведётся |
| analytics, audit-feed, finance, notifications, sales-crm | 0 | отдельными запросами | не ведётся |

Итого размечаемых блоков — **40**. Строк Д3 и Д4 — по **17** (по одной на доменный файл).
Обратите внимание: `warehouse` ведёт Save одной транзакцией и при этом версии **не ведёт** — это
не противоречие, а два разных решения (П43 и §11), и домен обязан написать обе строки как есть.

---

## 5. Машинная приёмка

Без этого раздела правило не выполняется, а декларируется. Все команды запускаются из корня
репозитория, кроме Г4.

### Г1 — каждый `POST` помечен ровно один раз

```bash
awk '
  FNR==1 { if (h!="") { printf "%s\t%s\t%d\n", f, h, m; h="" } }
  /^### (GET|POST|PUT|PATCH|DELETE) \/api\// { if (h!="") printf "%s\t%s\t%d\n", f, h, m; f=FILENAME; h=$0; m=0; next }
  /\*\*Ключ идемпотентности:\*\*/ { m++ }
  END { if (h!="") printf "%s\t%s\t%d\n", f, h, m }
' roo_code/roo-context/api/*.md roo_code/plans/orders/orders-backend-contract.md > /tmp/idem-blocks.txt
echo "POST-блоков: $(grep -cP '\t### POST ' /tmp/idem-blocks.txt)"
echo "из них помечены ровно один раз: $(grep -P '\t### POST ' /tmp/idem-blocks.txt | awk -F'\t' '$3==1' | wc -l)"
echo "непомеченные:"; grep -P '\t### POST ' /tmp/idem-blocks.txt | awk -F'\t' '$3!=1'
```

Зелено, когда второе число равно первому и третья команда молчит.
**База 2026-09-12: 40 и 0** — то есть сегодня гейт красный по построению, и это правильно: работа
не начата.

### Г2 — строка про атомарность есть в каждом доменном файле ровно одна

```bash
for f in roo_code/roo-context/api/*.md; do
  case "$(basename "$f")" in 00-conventions.md|README.md) continue;; esac
  n=$(grep -c '^\*\*Атомарность Save:\*\*' "$f")
  [ "$n" = "1" ] || echo "$f → $n"
done
```

Зелено, когда вывод пуст. Дополнительно: форма «одной транзакцией» встречается **ровно дважды** по
всем файлам, и только в `orders.md` и `warehouse.md`:

```bash
grep -l '^\*\*Атомарность Save:\*\* одной транзакцией' roo_code/roo-context/api/*.md
```

### Г3 — строка про версию есть в каждом доменном файле ровно одна

```bash
for f in roo_code/roo-context/api/*.md; do
  case "$(basename "$f")" in 00-conventions.md|README.md) continue;; esac
  n=$(grep -c '^\*\*Версия записи:\*\*' "$f")
  [ "$n" = "1" ] || echo "$f → $n"
done
grep -l '^\*\*Версия записи:\*\* ведётся' roo_code/roo-context/api/*.md
```

Зелено: первый цикл молчит, вторая команда печатает ровно `roo_code/roo-context/api/orders.md`.

### Г4 — ссылки размеченных блоков не битые

Каждый домен, дописав разметку, прогоняет резолвер по своему файлу:

```bash
cd frontend_vue && CONTRACT_REFS=roo_code/roo-context/api/<домен>.md npx vitest run src/services/contractRefs.spec.ts
```

Зелено — «битых 0». Это ловит ровно тот дефект, который у обоснований Д2 самый вероятный: ссылка
на строку, которая сдвинулась (см. 2.2).

### Г5 — фронт и контракт не разошлись

```bash
cd frontend_vue && npm run test:unit
```

Строка `[контракт] сведено доменов: …` обязана остаться зелёной: разметка не добавляет эндпоинтов
и не убирает их, и если этот счётчик покраснел — правка задела не то.

### Г6 — гейт становится постоянным (отдельная задача, кодом в этом плане не пишется)

Г1–Г3 — команды, которые кто-то должен помнить. Их следует превратить в спеку
`frontend_vue/src/services/idempotency-contract-conformance.spec.ts` внутри `npm run test:unit`,
по образцу `frontend_vue/src/services/contract-conformance.spec.ts`. Утверждений ровно четыре:

1. каждый `### POST /api/...` во всех доменных файлах несёт ровно одну строку `**Ключ идемпотентности:**`;
2. каждый доменный файл несёт ровно одну строку `**Атомарность Save:**`, и форма «одной транзакцией»
   встречается ровно в двух файлах — `orders.md` и `warehouse.md`;
3. каждый доменный файл несёт ровно одну строку `**Версия записи:**`, и форма «ведётся» — ровно в
   одном, `orders.md`;
4. строка `**Ключ идемпотентности:** обязателен` не встречается ни в одном блоке, кроме `POST`.

Задача самостоятельна и от доменных планов **не зависит**: она проверяет форму, а не содержание.
Её можно и нужно делать **до** доменных планов — тогда домен видит красный гейт и знает объём.

---

## 6. Что здесь нельзя решить до доменных планов — разобрано 2026-09-13, закрыто не всё

Тема делится надвое чисто. **Механизм** — разделы 3 и 4 — написан целиком и доменов не ждёт: срок
ключа, область действия, транспорт, поведение на повторе, коды отказа, политика версии, перечень
атомарного, обязанность ответа, запреты. Владелец решил каждый из этих вопросов явно, и П49
отдельно закрыл перечень атомарного словами «перечень атомарного этим закрыт».

**Перечни и сводки** — не написаны и до доменных планов не существуют. Их четыре, и выдумать их
здесь значило бы подменить работу семнадцати агентов догадкой одного.

> **Состояние на 2026-09-13.** Доменные планы написаны у **шестнадцати** доменов из семнадцати
> (нет `sales-crm`), и собранное из них лежит в разделе **6а**. Итог: Б4 закрыт, Б1 закрыт на
> 29 блоков из 40, Б2 — на 5 обоснований из 14, Б3 — на один домен из шестнадцати, Б5 остаётся
> открытым целиком. Что именно не закрылось и почему — в 6а, по пункту на каждый Б; перечень
> **не полон и называет себя неполным**, потому что перечень, дописанный по памяти, выглядит
> готовым и этим хуже названной дыры.

### Б1. Какие из 40 `POST` необратимы — ЗАКРЫТ ЧАСТИЧНО (29 из 40, раздел 6а.1)

Тест дан (3.1) и решает однозначно, но применять его надо к семантике каждого эндпоинта: что
именно операция делает, есть ли у неё `DELETE` или отмена, кому она видна. Сегодня помечено кодом
**5 вызовов**, ещё **2** известны как нарушения (Д7), остальные **33 блока** не разбирал никто.
Перечень «эндпоинт → обязателен/не требуется» появляется только из семнадцати проходов и
собирается обратно гейтом Г1, а не этим планом.

### Б2. Цена повтора у каждого необратимого эндпоинта — ЗАКРЫТ ЧАСТИЧНО (5 из 14, раздел 6а.2)

Обоснование Д2 — это `файл:строка` на место, где видно, что повтор создаёт вторую сущность. Для
отгрузки и платежа оно уже написано в контракте заказов; для остальных его нет, и взять его
неоткуда, кроме чтения мока и бэкенда своего домена.

### Б3. Перечень ответов, недостаточных для разморозки — ЗАКРЫТ ЧАСТИЧНО (один домен из 16, раздел 6а.3)

Серверная половина П47 (3.6) — правило; список эндпоинтов, которые ему сегодня не удовлетворяют, —
сводка по 176 блокам ответа. Ни один общий замер её не даёт: «достаточно ли ответа» зависит от
того, что показывает экран после этой операции, а это знает только домен.

### Б4. Новые доменные коды отказа, если они понадобятся — ЗАКРЫТ (не понадобились, раздел 6а.4)

Тема своих кодов не вводит: ключа хватает `VALIDATION_ERROR` и `CONFLICT` из ядра (Р2, Р3), версии
— `ORDER_VERSION_CONFLICT`, уже существующего. Но если домен, применив 3.1, найдёт необратимую
операцию, у которой отказ сегодня назвать нечем, код придётся завести — и какие это будут коды,
до Б1 неизвестно.

### Б5. Форма единого Save-запроса у заказа и склада — ОТКРЫТ (раздел 6а.5)

П43 требует, чтобы Save у `orders` и `warehouse` приходил **одним запросом**. Что в этом запросе
лежит — какие поля, позиции, услуги, файлы, движения и в каком виде — описывается в контракте этих
двух доменов, а не здесь: сегодня Save размазан на `1 + N + M + K + L` вызовов
(`frontend_vue/src/composables/useOrderCard.ts:419-532`) и на два вызова склада
(`frontend_vue/src/composables/useWarehouseBatch.ts:256`, `:263`), и свести их в одно тело —
работа доменного плана.

### Что блокировкой не является, хотя похоже

- **Политика версии в шестнадцати доменах.** Кажется, что нужно сначала узнать, где бывает
  одновременная правка. Не нужно: владелец решил last-write-wins для всех, кроме заказов, и домену
  оставлено только записать это и, если найдёт дорогую потерю, завести находку (3.4).
- **Перечень атомарного.** Закрыт П49 дословно. Домены его не пополняют.
- **Кнопки П47 во фронте.** Работа отложена владельцем целиком; она не ждёт доменных планов, она
  ждёт решения «пора».
- **Отсев файла П50.** Одно место на весь проект (`DropZone.vue`), доменам делать нечего.

---

## 6а. Собрано по доменным планам

Раздел собран **2026-09-13** и только из доменных планов. У каждой строки есть `файл:строка` на
тот план, который вердикт вынес; строки без источника здесь нет ни одной — перечень, дописанный
по памяти, выглядит готовым и этим хуже честно названной дыры.

**Источник неполон, и вот чем именно.** Планов **шестнадцать из семнадцати**: нет `sales-crm`.
По раскладке раздела 4 у него **ноль** `POST`, поэтому перечень Б1 он не пополняет ни одной
строкой — счёт «из 40» остаётся верным. Но строки Д3 и Д4 он должен как всякий доменный файл, и
здесь их нет: **Д3/Д4 собраны 16 из 17**.

### 6а.1 (Б1). Эндпоинт → обязателен / не требуется — 29 блоков из 40

| эндпоинт | вердикт плана | на чём | источник |
|---|---|---|---|
| `POST /api/warehouse/batches` | обязателен | двигает склад (признак 1) | `roo_code/plans/warehouse/warehouse-backend-plan.md:199` |
| `POST /api/warehouse/movements` | обязателен | двигает склад | `roo_code/plans/warehouse/warehouse-backend-plan.md:199` |
| `POST /api/warehouse/cutting` | обязателен | двигает склад | `roo_code/plans/warehouse/warehouse-backend-plan.md:199` |
| `POST /api/warehouse/offcuts` | обязателен | двигает склад | `roo_code/plans/warehouse/warehouse-backend-plan.md:199` |
| `POST /api/warehouse/deficit` | обязателен | двигает склад | `roo_code/plans/warehouse/warehouse-backend-plan.md:200` |
| `POST /api/bcc/send` | обязателен | шлёт наружу неотзываемое (признак 3) | `roo_code/plans/bcc/bcc-backend-plan.md:282` |
| `POST /api/bcc/log` | обязателен | создаёт запись, неудаляемую из интерфейса (признак 4, П61) | `roo_code/plans/bcc/bcc-backend-plan.md:282` |
| `POST /api/bcc/events/:eventId/response` | обязателен | то же, признак 4 | `roo_code/plans/bcc/bcc-backend-plan.md:282` |
| `POST /api/bcc/events/:eventId/no-response` | обязателен | то же, признак 4 | `roo_code/plans/bcc/bcc-backend-plan.md:282` |
| `POST /api/auth/register` | обязателен | создаёт арендатора, пользователя, роль, сессию и карточку компании, а удаления арендатора из интерфейса нет (признак 4) | `roo_code/plans/auth/auth-backend-plan.md:187` |
| `POST /api/auth/login` | не требуется | обратима выходом | `roo_code/plans/auth/auth-backend-plan.md:189` |
| `POST /api/auth/logout` | не требуется | повтор на отозванной сессии состояния не меняет | `roo_code/plans/auth/auth-backend-plan.md:190` |
| `POST /api/orders/:id/shipments` | обязателен | движение склада | `roo_code/plans/orders/orders-backend-plan.md:420`, `:520` |
| `POST /api/orders/:id/shipments/:id/cancel` | обязателен — **впервые**, сегодня ключа нет (БАГ-09) | отмена идёт обратными движениями, то есть тоже двигает склад | `roo_code/plans/orders/orders-backend-plan.md:222`, `:476` |
| `POST /api/orders/:id/payments` | обязателен | деньги | `roo_code/plans/orders/orders-backend-plan.md:420` |
| `POST /api/orders/:id/returns` | обязателен | товар на полку и корректировка счёта | `roo_code/plans/orders/orders-backend-plan.md:420` |
| `POST /api/clients` | не требуется | обратима из интерфейса, `DELETE /api/clients/:id` | `roo_code/plans/clients/clients-backend-plan.md:655` |
| `POST /api/clients/:id/interactions` | не требуется | обратима, `DELETE /api/clients/:id/interactions/:entryIndex` | `roo_code/plans/clients/clients-backend-plan.md:658` |
| `POST /api/config/fields` | не требуется | повтор даёт лишнюю строку библиотеки, а не лишнюю отгрузку | `roo_code/plans/config/config-backend-plan.md:402`, `:254` |
| `POST /api/config/sections` | не требуется | то же | `roo_code/plans/config/config-backend-plan.md:429`, `:254` |
| `POST /api/settings/currencies` | не требуется | создал валюту — удалил | `roo_code/plans/settings/settings-backend-plan.md:381` |
| `POST /api/settings/uoms` | не требуется | создал единицу — удалил | `roo_code/plans/settings/settings-backend-plan.md:381` |
| `POST /api/settings/conversions` | не требуется | создал правило — удалил | `roo_code/plans/settings/settings-backend-plan.md:381` |
| `POST /api/settings/order-statuses` | не требуется | создал статус — удалил | `roo_code/plans/settings/settings-backend-plan.md:381` |
| `POST /api/categories` | не требуется | созданную категорию удаляет `DELETE` | `roo_code/plans/categories/categories-backend-plan.md:158` |
| `POST /api/products` | не требуется | правка карточки и `DELETE /api/products/:id`, который под П44 архивирует | `roo_code/plans/products/products-backend-plan.md:356` |
| `POST /api/services` | не требуется — **условно** | прямая цитата 3.1 про справочник; станет «обязателен», если владелец ответит «пути обратно из архива нет» (вопрос 6 раздела 8 того плана) | `roo_code/plans/services/services-backend-plan.md:271`, `:282` |
| `POST /api/suppliers` | не требуется | созданный поставщик удаляется из интерфейса | `roo_code/plans/suppliers/suppliers-backend-plan.md:219` |
| `POST /api/uploads` | не требуется | П50; строка закрывает БАГ-05 | `roo_code/plans/uploads/uploads-backend-plan.md:183` |

**Одиннадцать блоков вердикта не получили — они и есть остаток Б1.**

| блок | что сказал план | чего не хватает |
|---|---|---|
| `POST /api/settings/change-password` | накрыт общим «каждый `POST` домена получает строку „не требуется“» (`roo_code/plans/settings/settings-backend-plan.md:381`), но в скобках разобраны только четыре справочника | своего разбора нет: смена пароля справочником не является, и признак 4 к ней надо применить отдельно |
| `POST /api/settings/mail/test` | тот же общий оборот | признак 3 теста 3.1 (шлёт наружу письмо) на него ложится прямо, а план его поимённо не разбирал — см. 6а.6, пункт 2 |
| `POST /api/orders` | план перечисляет «обязателен» четырьмя эндпоинтами (`roo_code/plans/orders/orders-backend-plan.md:420`); остальные девять в перечне отсутствуют | вердикта «не требуется» с обоснованием план не пишет ни одному из девяти |
| `POST /api/orders/:id/items` | то же | то же |
| `POST /api/orders/:id/items/:id/split` | то же | то же |
| `POST /api/orders/:id/items/:id/correct` | то же | корректировка замороженной строки живёт в слайсе счетов (`roo_code/plans/orders/orders-backend-plan.md:478`) — то есть рядом с деньгами, и признак 2 к ней надо применять явно |
| `POST /api/orders/:id/allocate-total` | то же | то же |
| `POST /api/orders/:id/services` | то же | то же |
| `POST /api/orders/:id/reserve` | то же | резерв двигает удержание на партии (`roo_code/plans/orders/orders-backend-plan.md:128`) — признак 1 требует явного разбора |
| `POST /api/orders/:id/invoices` | то же | признак 2 называет **счёт** дословно — см. 6а.6, пункт 2 |
| `POST /api/orders/:id/files` | то же | то же |

Итог Б1: **29 из 40**. Оставшиеся 11 — два блока `settings` и девять блоков `orders`.

### 6а.2 (Б2). Цена повтора у «обязателен» — 5 обоснований из 14

«Обязателен» стоит у **14** эндпоинтов (5 склада, 4 BCC, 1 auth, 4 заказа). Обоснование Д2 —
`файл:строка` на место, где видно вторую сущность, — план прислал у пяти.

| эндпоинт | что создаёт повтор | источник обоснования |
|---|---|---|
| `POST /api/orders/:id/shipments` | две отгрузки, 6 единиц с полки на запрос о 3, два движения `sale`, `shippedQuantity` 6 | `roo_code/plans/orders/orders-backend-contract.md:93` |
| `POST /api/orders/:id/payments` | два платежа по 500 дают `paid 1000` | `roo_code/plans/orders/orders-backend-contract.md:93` |
| `POST /api/orders/:id/returns` | товар возвращается на полку дважды и выписываются две корректировки | `roo_code/plans/orders/orders-backend-contract.md:248` |
| `POST /api/warehouse/batches` | вторая партия: уникальности номера нет ни в моке, ни на схеме (`backend/app/modules/warehouse/shared/models.py:33` — просто `String(100)`) | `roo_code/plans/warehouse/warehouse-backend-plan.md:93` |
| `POST /api/auth/register` | второй арендатор с пользователем, ролью, сессией и карточкой компании (`backend/app/modules/auth/features/register/domain.py:97-151`), а удаления арендатора из интерфейса нет | `roo_code/plans/auth/auth-backend-plan.md:187` |

Без `файл:строка` остались девять: `POST /api/orders/:id/shipments/:id/cancel` (план называет
механику — обратные движения, `roo_code/plans/orders/orders-backend-plan.md:476` — но не место, где
видна вторая пара); `POST /api/warehouse/movements`, `/cutting`, `/offcuts`, `/deficit` (общий довод
«двигают склад» на пятерых, `roo_code/plans/warehouse/warehouse-backend-plan.md:198-200`); четыре `POST`
BCC (у отправки цена названа словами — «второе письмо живому поставщику»,
`roo_code/plans/bcc/bcc-backend-plan.md:252` — но без ссылки; у трёх остальных не названа вовсе).

### 6а.3 (Б3). Ответы, недостаточные для разморозки — разметку прислал один домен из шестнадцати

Дословные строки Д5 есть только у `products`
(`roo_code/plans/products/products-backend-plan.md:387-389`):

| отправка | чего не хватает | чем клиент добирает сегодня |
|---|---|---|
| `POST /api/products` | `201` несёт четыре поля против пятнадцати принимаемых (`backend/app/modules/products/features/create_product/schemas.py:33-38`) | переходом в карточку и полным `GET /api/products/:id` (`frontend_vue/src/views/admin/products/ProductsPage.vue:216`) |
| `PATCH /api/products/:id` | журнал в ответе прежний: записи, которые породил сам `PATCH` (П36), в него не попадают (`frontend_vue/src/services/mocks/products.ts:14217`) | полным перезапросом карточки (`frontend_vue/src/composables/useProductCard.ts:256-258`) |
| `DELETE /api/products/:id` | ответ пуст (`frontend_vue/src/services/productsService.ts:120-122`) | перезапросом всего списка (`frontend_vue/src/composables/useProducts.ts:48-50`) |

Четыре домена объявили свои ответы **достаточными** — это тоже результат Д5, и записывать в
контракт по правилу нечего:

- `clients` — `POST` отдаёт созданного клиента целиком, `PATCH` — клиента после правки (`roo_code/plans/clients/clients-backend-plan.md:208`);
- `bcc` — все четыре операции достаточны после переворота порядка П48 (`roo_code/plans/bcc/bcc-backend-plan.md:437`);
- `finance` — `PATCH` отдаёт запись целиком (`roo_code/plans/finance/finance-backend-plan.md:506-508`);
- `suppliers` — `PATCH /status` отвечает `void`, и П47 этим **не** нарушен: канбан знает новое значение до запроса (`roo_code/plans/suppliers/suppliers-backend-plan.md:494`).

Наблюдение `warehouse`, которое строкой Д5 не является и на неё не переводится: у пяти `POST`
домена и Save обеих карточек правило признано применимым, и «пять из пяти вызывающих
`POST /movements` ответа не читают вовсе» (`roo_code/plans/warehouse/warehouse-backend-plan.md:258`).
Это про клиента, а не про недостаточность ответа, и вердикта Д5 не заменяет.

Молчат одиннадцать доменов: `orders`, `settings`, `config`, `categories`, `services`, `auth`,
`uploads`, `notifications`, `analytics`, `audit-feed` и `warehouse` (наблюдение выше вердиктом не
является). Б3 закрыт **на один домен из шестнадцати**.

### 6а.4 (Б4). Новых кодов отказа ключу не понадобилось ни в одном из шестнадцати планов

Ответ отрицательный, и он подтверждён поимённо, а не молчанием:

- `config` — «чего в этом списке намеренно нет: `CONFLICT` 409 по `Idempotency-Key`» (`roo_code/plans/config/config-backend-plan.md:254`);
- `finance` — `CONFLICT` не заводит никто в домене, `POST` нет ни одного (`roo_code/plans/finance/finance-backend-plan.md:180`);
- `audit-feed` — то же, необратимых операций нет (`roo_code/plans/audit-feed/audit-feed-backend-plan.md:143`);
- `notifications` — «кода, которого здесь намеренно нет: `CONFLICT` 409» (`roo_code/plans/notifications/notifications-backend-plan.md:207`);
- `warehouse` — пять необратимых `POST` обходятся `VALIDATION_ERROR` и `CONFLICT` **ядра**, своих не заводит (`roo_code/plans/warehouse/warehouse-backend-plan.md:88`).

Один новый код рядом с темой всё же появился, и он **не про отсутствие ключа**:
`REQUEST_ALREADY_SENT`, 409, слайс `retry_send` домена BCC — повтор допустим только для записи,
помеченной неотправленной, а повтор успешной отправки есть второе письмо живому поставщику
(`roo_code/plans/bcc/bcc-backend-plan.md:252`). Он закрывает повторную **отправку**, тогда как
ключ закрывает повторный **запрос**; в §3.3 он ничего не меняет.

Соседний код, который темой ошибочно считать нельзя: `BATCH_NUMBER_DUPLICATE` склада —
идемпотентностью он **не** закрывается, это вопрос владельца об уникальности номера партии
(`roo_code/plans/warehouse/warehouse-backend-plan.md:317`).

Б4 **закрыт**: новых доменных кодов тема не вводит.

### 6а.5 (Б5). Форма единого Save — не собрана ни у одного из двух доменов

- `warehouse` объявил, что Save карточки партии применяется одной транзакцией, и назвал
  сегодняшнюю россыпь — `patchBatch()` и следом `createMovement()` двумя запросами без общей
  границы, с заглушённым провалом второго
  (`roo_code/plans/warehouse/warehouse-backend-plan.md:201`, `:469`). **Состава тела нет**: какие
  поля партии, какое движение и в каком виде едут одним запросом, план не пишет.
- `orders` держит это слайсом С11 — «одно изменение — один запрос, одна транзакция, один шаг
  версии» (`roo_code/plans/orders/orders-backend-plan.md:480`, `:223`). **Состава тела тоже нет**, и
  сверх того сам план держит открытым вопрос владельцу **В-3**: распространяется ли П43 на
  страницу создания заказа, где сегодня та же цепочка `1 + N + M`
  (`roo_code/plans/orders/orders-backend-plan.md:759`).

Б5 **остаётся открытым**. Причина названа планами, а не выведена здесь: оба домена решили
**границу** транзакции и не решили **тело** запроса, а у заказа тело зависит ещё и от неотвеченного
В-3.

### 6а.6 Что доменные планы прислали сверх Б1–Б5

Это не перечни темы, но оно приехало из тех же шестнадцати планов и молчанием не закрывается.

**1. Дословные строки Д1–Д4 несут три плана из шестнадцати.** Замер:
`grep -rc '\*\*Атомарность Save:\*\*' roo_code/plans/*/[a-z]*-backend-plan.md` даёт ненулевое
только у `config`, `clients` и `products`; по `**Версия записи:**` — те же три. Остальные тринадцать
пересказывают смысл своими словами, а гейты Г2 и Г3 грепают **маркер**, а не смысл. Четыре
пересказа уже разошлись с обеими формами Д3:

- `suppliers` — «один `PATCH`, и он же пишет записи аудита одной транзакцией» (`roo_code/plans/suppliers/suppliers-backend-plan.md:219`);
- `services` — «Save атомарен: один Save — ровно один вызов `patchService`» (`roo_code/plans/services/services-backend-plan.md:275-276`);
- `bcc` — «не применимо: у домена нет режима Save» (`roo_code/plans/bcc/bcc-backend-plan.md:282`); третью форму — вторую форму Д3 с припиской «Save-формы в домене нет» — раздел 4 отдаёт трём другим доменам, и BCC среди них нет;
- `finance` — «атомарность Save **не заводится**» (`roo_code/plans/finance/finance-backend-plan.md:503`).

Два плана строки не назвали вовсе: `auth` — про атомарность Save
(`grep -c 'Атомарность\|атомарн' roo_code/plans/auth/auth-backend-plan.md` → `0`), `uploads` — про
версию записи (`grep -n 'Версия записи\|last-write-wins' roo_code/plans/uploads/uploads-backend-plan.md`
— пусто). Работа отсюда доменная: строку пишет домен в свой файл контракта, и гейты Г2/Г3 её ловят.

**2. Два места, где тест 3.1 и вердикт домена расходятся.** Ни одно из них не решается здесь —
оба уходят обратно в свой домен:

- `POST /api/settings/mail/test` — признак 3 («шлёт наружу то, что не отзывается») ложится прямо,
  а план накрыл его общим «каждый `POST` … не требуется», разобрав в скобках четыре справочника
  (`roo_code/plans/settings/settings-backend-plan.md:381`). Пометка 3.1 «сомнение решается в пользу
  „необратима“» велит домену разобрать его отдельно, а не оставить под общим оборотом.
- `POST /api/orders/:id/invoices` — признак 2 называет **счёт** дословно, а перечень «обязателен»
  плана заказов его не содержит (`roo_code/plans/orders/orders-backend-plan.md:420`). Рядом та же
  развилка у `POST /api/orders/:id/items/:id/correct` — корректировка замороженной строки живёт в
  денежном слайсе С9 (`roo_code/plans/orders/orders-backend-plan.md:478`).

**3. Один вердикт условен.** `POST /api/services` помечен «не требуется» при сегодняшнем ответе на
вопрос 6 раздела 8 плана услуг; ответ «пути обратно из архива нет» делает его необратимым, и тогда
мок-хранилище ключа к этому не готово — `Map` на процесс без срока и без привязки к пути
(`frontend_vue/src/services/mocks/index.ts:259-269`), то есть оба параметра П46 в нём отсутствуют
(`roo_code/plans/services/services-backend-plan.md:283`).

---

## 7. Что осталось владельцу

Решениями куста не закрыто и доменами не закрывается — четыре дорисовки Р1–Р4 (3.9). Они не
блокируют работу: домен применяет их как правило. Но если владелец прочтёт «операцию» П46 как
маршрут без идентификаторов (Р1) или захочет другое поведение на гонке (Р3) — правится один абзац
здесь, и ни один доменный файл переписывать не придётся. Ради этого они и собраны в одном месте.

---

## Источники

- `roo_code/plans/api/audit/00-решения-владельца.md` — куст «транзакционность», П43, П46–П50.
- `roo_code/roo-context/api/00-conventions.md` — §2 коды ошибок, §4 мультиарендность, §11 эта тема,
  §15 Save UX и П47.
- `roo_code/plans/orders/orders-backend-contract.md` — §3 версия и ключ у заказов, §6 каталог кодов.
- `roo_code/plans/bugs/contract-sync-orders-bugs.md`, `roo_code/plans/bugs/contract-sync-bcc-bugs.md` —
  два известных нарушения.
- Замеры 2026-09-12: `/tmp/proof-idem-now.txt`, `/tmp/proof-idem-blocks.txt`.
- Шестнадцать доменных планов, из которых собран раздел 6а (нет `sales-crm`):
  `roo_code/plans/warehouse/warehouse-backend-plan.md`, `roo_code/plans/orders/orders-backend-plan.md`,
  `roo_code/plans/settings/settings-backend-plan.md`, `roo_code/plans/config/config-backend-plan.md`,
  `roo_code/plans/clients/clients-backend-plan.md`, `roo_code/plans/suppliers/suppliers-backend-plan.md`,
  `roo_code/plans/products/products-backend-plan.md`, `roo_code/plans/bcc/bcc-backend-plan.md`,
  `roo_code/plans/categories/categories-backend-plan.md`, `roo_code/plans/services/services-backend-plan.md`,
  `roo_code/plans/finance/finance-backend-plan.md`, `roo_code/plans/auth/auth-backend-plan.md`,
  `roo_code/plans/notifications/notifications-backend-plan.md`, `roo_code/plans/audit-feed/audit-feed-backend-plan.md`,
  `roo_code/plans/analytics/analytics-backend-plan.md`, `roo_code/plans/uploads/uploads-backend-plan.md`.
- Замер сбора 2026-09-13: `/tmp/proof-idem-domains.txt`, `/tmp/proof-razmoroz.txt`.

# Решения владельца — сверка контракта

Сюда фаза аудита сносит строки, у которых источник «нигде»: обязанность сервера, которой
нет ни в моке, ни на бэкенде, ни константой во фронте. Прогон их **не решает** — решение
принимает владелец, и до тех пор раздел контракта помечается `**Статус:** спроектировано`.

Формат строки: `<домен> · <графа> · что именно неизвестно · где смотрел`.

---

## Засеяно до прогона — то, что уже найдено и решать некому, кроме владельца

### 1. Фичи и тарифы: откуда админка берёт матрицу

**Где смотрел.** Бэкенд: `backend/app/modules/billing/shared/models.py` — `Plan` (тариф = набор
фич), `PlanFeature` (`feature_key` + разрешено ли), `TenantFeatureOverride` (переопределение на
арендатора), `FeatureDefinition` с комментарием «Registry of all known feature flag keys — single
source of truth»; ключи сеются миграцией `alembic/versions/8cf3bfa380dd_phase_12_plans_multi_role.py`.
Фронт: `frontend_vue/src/config/featureFlags.ts` — 52 флага константой плюс переопределения из
`localStorage`. Эндпоинтов между ними ноль (инвентарь `scanCode()`), роутов у модуля `billing` ноль.

**Что решить.** Читает ли админка матрицу фич с сервера, и каким эндпоинтом. Совпадает ли словарь
52 ключей фронта с `feature_definitions`. Что сервер отвечает на запрос к выключенной фиче — 404,
403 или пустой ответ. Переопределение арендатора сильнее тарифа или наоборот.

**Почему не решает прогон.** Это не наблюдение, а проектное решение: обе стороны реализованы, но
по разным замыслам, и выбрать между ними может только владелец.

### 2. Кастомные поля: жизненный цикл значения

**Где смотрел.** Определения — `types/config.ts:5` (`FieldDefinition`), CRUD `/api/config/fields`,
привязка к категории `PUT /api/categories/:id/fields`. Значения — `types/product.ts` и
`mocks/products.ts` (`fieldValues`). В `mocks/config.ts` о судьбе значений при удалении определения
**ни строки**.

**Что решить.** Что происходит со значениями, когда определение удалили: удаляются каскадом,
остаются сиротами, блокируют удаление. Кто валидирует тип значения при записи. Что происходит со
значением при смене типа определения.

**Почему не решает прогон.** Правила нет ни в моке, ни на бэкенде — выдумать его значит записать в
контракт догадку.

---

## categories — аудит 2026-09-04

Формат строк ниже — по шапке файла: `<домен> · <графа> · что именно неизвестно · где смотрел`.
Аудит целиком: [`categories.md`](categories.md).

- categories · События и уведомления · рождает ли уведомление создание/удаление категории и, главное, замена набора её полей — операция меняет карточку каждого товара категории и всего её поддерева · `grep -n "notify" frontend_vue/src/services/mocks/categories.ts` — пусто; `grep -rn "categor" frontend_vue/src/services/mocks/notifications.ts` — пусто, ни один из семи триггеров категорий не касается
- categories · Запись в аудит-лог · остаётся ли след у создания, удаления, смены родителя и замены набора полей категории, и кто его автор · `grep -n "auditLog" frontend_vue/src/services/mocks/categories.ts` — пусто; аудит в проекте есть только на товаре (`frontend_vue/src/types/product.ts:108`) и отдельным доменом `audit-feed`
- categories · Права — в какой функции проверяются · какое право требуется на создание, правку и удаление категории, и одно ли оно с правом на товары · во фронте гейт только фича-флагами `adminCategories` / `categoryFieldReorder` / `categorySupplierLinks` (`frontend_vue/src/router/index.ts:232,238`, `frontend_vue/src/config/featureFlags.ts:19,36,37`); в матрице прав `frontend_vue/src/services/mocks/config.ts` категорий нет (`grep -rn "categor"` даёт только `f-categories` — поле карточки поставщика, `:42`, `:126`)
- categories · Значения по умолчанию и их владелец · кто владеет перечнем типов поля категории — код или настройки арендатора · константа продублирована в двух местах, `frontend_vue/src/types/category.ts:4` и `frontend_vue/src/views/admin/products/CategoryCardPage.vue:97-105`; справочника типов поля в `settings` нет (`grep -rn "fieldType\|field_type" frontend_vue/src/types/settings.ts` — пусто)
- categories · Кастомные поля · что делать со значениями у товаров, когда `PUT /api/categories/:id/fields` удалил определение, и что делать со значениями **унаследованных** полей, когда сменился родитель категории · мок товаров при этом не трогается вовсе (`frontend_vue/src/services/mocks/categories.ts` — ни одного обращения к товарам), а схема удаление запрещает: `product_field_values.field_id` объявлен `ondelete="RESTRICT"` (`backend/alembic/versions/25245d4bf874_phase_3_categories_products.py:78`). Расширяет засеянный пункт 2 выше случаем наследования, которого там нет
- categories · Транзакционность и идемпотентность · обязаны ли `PATCH /api/categories/:id` и `PUT /api/categories/:id/fields` применяться одной транзакцией — сейчас это два независимых параллельных запроса · `Promise.all([patchCategory, putCategoryFields])`, `frontend_vue/src/composables/useCategoryCard.ts:102-112`; при падении одного второй остаётся применённым, и `load()` после ошибки не вызывается (`:113` стоит до `catch`)
- categories · Коды ошибок · каким кодом сервер отвергает смену родителя на собственного потомка (цикл) и удаление категории, чьи поля уже заполнены у товаров · подходящего кода в домене нет: мок знает только `CATEGORY_NOT_FOUND`, `CATEGORY_HAS_PRODUCTS`, `CATEGORY_HAS_CHILDREN` (`frontend_vue/src/services/mocks/categories.ts:1479-1481`); проверки цикла нет ни в моке, ни во фронте (`:1468-1473`, `CategoryCardPage.vue:91`)
- categories · Форма ответа для справочников · чем отдавать полный список категорий выбору родителя и выбору категории товара — отдельным лёгким эндпоинтом или явным большим `pageSize` · сейчас один справочник берётся двумя разными способами: `getCategories({ search: '' })` по дефолту в 25 записей (`frontend_vue/src/views/admin/products/CategoryCardPage.vue:63`) и `getCategories({ search: '' }, 1, 999)` (`frontend_vue/src/composables/useProductCard.ts:132`)

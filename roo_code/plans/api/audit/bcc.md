# Аудит контракта — bcc

Эндпоинтов в коде: **7**. Реализовано бэкендом: **0**.

Источник истины по эндпоинту: бэкенд → мок+клиент → замысел. Пустая графа = задача не закрыта.
Утверждение без `файл:строка` не записывается. Код не правится: место, где он выглядит
неверным, — находка в `roo_code/plans/bugs/contract-sync-bcc-bugs.md`.

> **Отдельно про «Бэкенд: нет»: у `bcc` это значит не то же, что у соседей.** Роутов ноль —
> `grep -rn "@router\." backend/app/modules/bcc --include=*.py` не даёт ни одного попадания,
> `internal_api/interface.py` и `shared/dependencies.py` — по одной строке докстроки
> (`backend/app/modules/bcc/internal_api/interface.py:1`, `backend/app/modules/bcc/shared/dependencies.py:1`).
> **Но, в отличие от `suppliers` и `notifications`, у `bcc` есть написанный доменный слайс:**
> `backend/app/modules/bcc/features/send_request/domain.py` (125 строк — `build_bcc_envelope` `:78-107`,
> `send_bcc_request` `:110-125`, `is_mail_configured` `:58-65`, два исключения `:23-38`),
> `.../transport.py` (`smtp_transport` `:20-43`, `send_via_smtp` `:46-54`) и тест под них
> (`backend/tests/modules/bcc/test_send_request.py`, 169 строк, три класса `:76`, `:105`, `:142`).
> Вызывающих в `backend/app` у этого слайса **ноль**:
> `grep -rn "send_bcc_request\|send_via_smtp\|build_bcc_envelope\|MailServerConfig(" backend --include=*.py`
> даёт только объявления и тринадцать строк самого теста. То есть правило «одно письмо, все адреса
> в BCC» на сервере **написано и не подключено**: HTTP-слоя, схем и репозитория у слайса нет.
> По К5 старшинство «бэкенд» не наступило ни у одного из семи эндпоинтов, но по **правилу
> отправки** и по **схеме хранения** бэкенд уже старше фронта, и оба расхождения отмечены ниже.
>
> **Таблицы есть обе:** `backend/app/modules/bcc/shared/models.py:11-41` (`BccCategory`,
> `__tablename__ = "bcc_categories"`, `:14`) и `:43-89` (`BccEvent`, `"bcc_events"`, `:46`);
> миграция `backend/alembic/versions/f96e6fb2d5cf_phase_8_bcc.py:24-52`.

## Эндпоинты

### GET /api/bcc/categories
- Вызывающий: `src/services/bccService.ts:7`
- Бэкенд: **нет**
- Мок: `mocks/index.ts:353`
- Форма запроса: пусто — ни query, ни тела, ни заголовков: `apiGet<BccCategory[]>('/api/bcc/categories')` вызывается одним аргументом (`src/services/bccService.ts:6-8`), а `options?.headers` — единственный источник заголовков у GET (`src/services/api.ts:144-158`). Мок ветку разбирает без параметров (`mocks/index.ts:353`).
- Форма ответа: **голый массив** `BccCategory[]`, не `PaginatedResponse` — подпись `src/services/bccService.ts:6`, тип `{ id: string; name: TranslatedString; productCount: number; children?: BccCategory[] }` (`src/types/bcc.ts:3-8`). Дерево ровно двухуровневое: мок отдаёт `MOCK_BCC_CATEGORIES` как есть (`mocks/bcc.ts:225-227`) — пять корней с `children` и без `children` у листьев (`mocks/bcc.ts:7-118`). `productCount` заполнен только у корней (`4`, `2`, `3`, `3`, `3` — `:11,38,55,77,99`), у всех пятнадцати листьев он `0` (`:16,21,26,31,44,49,60,65,70,82,87,92,104,109,114`). Конверт общий: `unwrap()` снимает `{ success, data }` для любого тела с ключом `success` (`src/services/api.ts:129-138`), значит на проводе `ApiResponse<BccCategory[]>`. Мок возвращает **ссылку на константу**, а не копию (`mocks/bcc.ts:226`) — в отличие, например, от `mocks/notifications.ts:424`, где стоит `structuredClone`.
- Коды ошибок: **ни одного**. `mockGetBccCategories` — одна строка `return MOCK_BCC_CATEGORIES` (`mocks/bcc.ts:225-227`), `throw` в ней нет. Каталога бэкенда у домена нет: своих `@router.` ноль, а общие исключения ядра (`backend/app/core/exceptions.py:13-48` — `NOT_FOUND`, `VALIDATION_ERROR`, `UNAUTHORIZED`, `FORBIDDEN`, `CONFLICT`) на этом пути никем не брошены. Клиент кладёт **текст** исключения в `error` (`src/composables/useBccRequest.ts:97-99`), разбора `ApiRequestError.code` (`src/types/api.ts`, заполнение `src/services/api.ts:118-124`) в домене нет нигде: `grep -c "ApiRequestError" frontend_vue/src/composables/useBccRequest.ts frontend_vue/src/services/bccService.ts` → 0 в обоих.
- Save-режим: чтение, один раз на монтировании. Единственный триггер — `onMounted` страницы (`src/views/admin/suppliers/BccRequestPage.vue:522-523` → `loadCategories`, `src/composables/useBccRequest.ts:92-102`). Перезапроса нет: ни фильтр, ни поиск, ни пагинация таблицы товаров запроса не делают — всё считается на клиенте из уже загруженного дерева (`BccRequestPage.vue:105-140`).
- Пробел контракта: старый раздел (`roo_code/roo-context/03-api-contract.md:536-543`) описывает форму верно (корни с nested `children`), но: **(а)** пример ответа (`:541`) даёт `"name": "Sheets"` **строкой**, тогда как в типе это `TranslatedString` (`src/types/bcc.ts:5`) и мок отдаёт объект трёх локалей (`mocks/bcc.ts:10`); **(б)** обещание «кешируется (ETag)» (`:543`) кодом не поддержано ничем — `grep -rn "ETag\|If-None-Match" frontend_vue/src/services/api.ts frontend_vue/src/services/bccService.ts` пусто; **(в)** обещание «видимость товара фильтруется по товарным правам» (`:543`) — прав в домене нет ни одного (см. графу «Права»); **(г)** не сказано главного: **листья этого дерева фронт считает товарами**, а схема — категориями. `productOptions` берёт `cat.children` и кладёт их `id` в `productIds` запроса (`BccRequestPage.vue:77-86`, `:397-410`), тогда как таблица называется `bcc_categories` и `parent_id` ссылается на неё же (`backend/app/modules/bcc/shared/models.py:23-28`), а `bcc_events.product_id` — FK на **`products.id`** (`:63-67`). Идентификаторы листьев (`sheet-2mm`, `beam-i20`, …) не встречаются в проекте больше нигде, кроме мока BCC и его спеки: `grep -rn "sheet-2mm" frontend_vue/src` даёт `mocks/bcc.ts` и `mocks/bcc-envelope.spec.ts:32,77`; товары мока — `prod-001`… (`mocks/products.ts:31`), категории — `cat-1`… (`mocks/categories.ts:11`). Находка БАГ-06; чем является дерево — отдельным каталогом BCC или проекцией `products`/`categories` — строка владельцу.
- Источник истины: мок + клиент (реализации бэкенда нет). Форма взята из `src/services/bccService.ts:6-8`, `src/types/bcc.ts:3-8`, `mocks/bcc.ts:7-118,225-227`. Схема учтена как ограничение, а не как форма: `backend/app/modules/bcc/shared/models.py:11-41`.

### GET /api/bcc/history
- Вызывающий: `src/services/bccService.ts:21`
- Бэкенд: **нет**
- Мок: `mocks/index.ts:358`
- Форма запроса: два query-ключа, и клиент шлёт **оба всегда** — `page` и `pageSize`, сериализованные через `String()` с дефолтами `1` и `25` (`src/services/bccService.ts:17-21`). Ни фильтров, ни сортировки, ни поиска: третьего ключа в объекте нет. Заголовков клиент не шлёт (`:21` — вызов без третьего аргумента `apiGet`, `src/services/api.ts:144-158`) → находка БАГ-09. Единственный вызывающий передаёт литералы `{ page: 1, pageSize: 25 }` (`src/composables/useBccRequest.ts:106`), то есть дефолты клиента недостижимы. Мок читает ровно эти два ключа со своими дефолтами `1` и `25` (`mocks/index.ts:359-360`).
- Форма ответа: `PaginatedResponse<BccRequest>` — подпись `src/services/bccService.ts:14-16`, конверт `{ items, total, page, pageSize, totalPages }` собирается в `mockGetBccHistory` (`mocks/bcc.ts:248-267`). Элемент — `BccRequest`: `id`, `requestId`, `date`, `supplierId`, `supplierName: TranslatedString`, `productId`, `productName: TranslatedString`, `source: TranslatedString`, `status: BccEventStatus`, `price?: number`, `unit?: string` (`src/types/bcc.ts:21-34`). `BccEventStatus` — три значения: `sent`, `responded`, `no_response` (`:18`). Это event-sourcing-строка, что записано в типе прямо (`:20` — «one product × one supplier. Grouped by requestId»). Мок отдаёт `slice` **исходного массива**, а не копию (`mocks/bcc.ts:261`). Сортировки при чтении нет вовсе: порядок — это порядок массива, который держится `unshift`-ами (`:364`, `:386`) поверх сида, уложенного по убыванию даты (`:143` 2026-04-05, `:177` 2026-04-02, `:202` 2026-03-20).
- Коды ошибок: **ни одного**. `mockGetBccHistory` не содержит ни одного `throw` (`mocks/bcc.ts:248-267`). Клиент ошибку **глотает молча**: `catch { /* history is optional — silent fail */ }` (`src/composables/useBccRequest.ts:108-110`), то есть сорванная загрузка истории выглядит как пустая история. Каталога бэкенда нет (роутов ноль).
- Save-режим: чтение, один раз на монтировании (`BccRequestPage.vue:524` → `loadHistory`, `src/composables/useBccRequest.ts:104-111`). **Пагинации в интерфейсе нет:** таблица рисует всё, что пришло, — `v-for="evt in history"` (`BccRequestPage.vue:890`), а ни одного другого вызывающего у `getBccHistory` нет (`grep -rn "getBccHistory" frontend_vue/src` → объявление `bccService.ts:14`, импорт и вызов `useBccRequest.ts:6,106`). Дальше массив правит только клиент: новые строки после отправки и логирования (`BccRequestPage.vue:292`, `:439`), после принятия ответа (`:359`) и отметки «не ответил» (`:370`).
- Пробел контракта: старый раздел (`03-api-contract.md:555-563`) верен по форме конверта и по природе строк (event-sourcing, `id` уникален, `requestId` группирует), но: **(а)** «Когда: `onMounted` → `loadHistory`, **смена `page`/`pageSize`**» (`:557`) неверно — второго вызова нет, страница ленты не листает; **(б)** пример ответа (`:561`) даёт `supplierName`, `productName`, `source` **строками**, тогда как в типе все три — `TranslatedString` (`src/types/bcc.ts:26,28,30`); **(в)** «сортировка DESC по создания» (`:563`) сервером не выражена: мок не сортирует ничего (`mocks/bcc.ts:258-266`), порядок держится тем, что новая строка кладётся в начало массива, а поля времени у строки нет вовсе — есть только `date` формата `YYYY-MM-DD` (`src/types/bcc.ts:24`, значения `mocks/bcc.ts:145`, `:354`), то есть события одного дня не упорядочены ничем; **(г)** «пагинация обязательна — tenant за год может набрать 50k+ event-rows» (`:563`) — при том, что срока жизни строки и удаления в домене нет ни одного (`DELETE` среди семи путей отсутствует), строка владельцу; **(д)** не сказано, что «текущее состояние пары (запрос, поставщик, товар)» — это **производное от порядка**, и считает его сегодня клиент (`isLatestEvent`, `BccRequestPage.vue:492-496`).
- Источник истины: мок + клиент. Формы — `src/services/bccService.ts:14-22`, `src/types/bcc.ts:18-34`, `mocks/bcc.ts:140-223,248-267`. Схема хранения — бэкенд (`backend/app/modules/bcc/shared/models.py:43-89`), и она расходится с типом фронта по пяти полям (см. «Правила домена», п. 6).

### GET /api/bcc/recipients
- Вызывающий: `src/services/bccService.ts:11`
- Бэкенд: **нет**
- Мок: `mocks/index.ts:354`
- Форма запроса: один query-ключ `products` — **CSV-строка** идентификаторов, а не повторяющийся параметр: `apiGet('/api/bcc/recipients', { products: productIds.join(',') })` (`src/services/bccService.ts:10-12`). Пустой выбор превращается в пустую строку, и мок читает её как «ничего не выбрано»: `params?.products ? params.products.split(',') : []` (`mocks/index.ts:355`). Заголовков нет (`bccService.ts:11`).
- Форма ответа: голый массив `BccRecipient[]` — `{ id, company: TranslatedString, email, contactPerson: TranslatedString, selected: boolean }` (`src/types/bcc.ts:10-16`), подпись `src/services/bccService.ts:10`. Список **всегда полный**: мок отображает весь `MOCK_SUPPLIERS` и никого не отсекает (`mocks/bcc.ts:239-245`), причина записана в коде (`:230-233` — «matches float to the top without hiding anyone»). `selected` — подсказка авто-отметки: она истинна, когда хотя бы одна категория поставщика покрывает хотя бы один выбранный товар (`mocks/bcc.ts:234-244`), причём сопоставление идёт **по имени категории строкой** через захардкоженную карту `PRODUCT_CATEGORY` из пятнадцати пар (`mocks/bcc.ts:122-138`) и свободные строки `supplier.categories` (тип — см. аудит suppliers, графа «Настройки, которых мок не отслеживает»). Ни один поставщик не скрыт: `total`/пагинации у ответа нет.
- Коды ошибок: ни одного — `mockGetBccRecipients` не бросает (`mocks/bcc.ts:229-246`). Клиент кладёт текст исключения в `error` (`src/composables/useBccRequest.ts:119-121`), страница его печатает; кода не разбирает никто.
- Save-режим: чтение. Триггеров два: `onMounted` после каталога и истории (`BccRequestPage.vue:526`) и `watch(selectedProductIds, refreshRecipients, { deep: true })` (`src/composables/useBccRequest.ts:124`), то есть каждое переключение чекбокса товара — отдельный запрос; дебаунса нет. Есть замок: `recipientsLocked` (`:90`, проверка `:114`) — предусмотрен под сценарий «получатель задан снаружи», но **никем не взводится**: `grep -rn "recipientsLocked" frontend_vue/src` даёт только объявление (`useBccRequest.ts:90`), проверку (`:114`) и экспорт (`:187`). Ответ страница не берёт напрямую — она пересобирает свой `selectedRecipientIds` из флага `selected` каждой перезагрузки списка (`BccRequestPage.vue:513-520`).
- Пробел контракта: старый раздел (`03-api-contract.md:545-553`) описывает механику верно (CSV, пустое → все, `selected` как подсказка UI), но: **(а)** «`selected: true` для тех, у кого **в профиле** есть хотя бы один из запрошенных товаров» (`:549`) — в коде связи «поставщик ↔ товар» нет: сопоставляются **категории**, и не идентификаторами, а совпадением строк (`mocks/bcc.ts:236-244`); **(б)** пример (`:551`) даёт `company` и `contactPerson` строками, в типе оба — `TranslatedString` (`src/types/bcc.ts:12,14`); **(в)** не сказано, что ответ **непагинирован**, а страница режет его сама по 5 (`BccRequestPage.vue:194`, `:224-227`); **(г)** не сказано, что `id` получателя — это `id` поставщика из домена suppliers (`mocks/bcc.ts:240`), и потому наследует его расхождение пространств (см. аудит suppliers, БАГ-02) — а вместе с ним и БАГ-05 ниже.
- Источник истины: мок + клиент. Формы — `src/services/bccService.ts:10-12`, `src/types/bcc.ts:10-16`, `mocks/bcc.ts:229-246`. Хранилища получателя у домена нет вовсе — это проекция `suppliers`, поэтому таблицы под неё в `models.py` нет.

### POST /api/bcc/events/:id/no-response
- Вызывающий: `src/services/bccService.ts:77`
- Бэкенд: **нет**
- Мок: `mocks/index.ts:933`
- Форма запроса: путь плюс **пустой объект телом** — `apiPost<BccRequest>(\`/api/bcc/events/${eventId}/no-response\`, {})` (`src/services/bccService.ts:76-78`). Тело сериализуется всегда (`src/services/api.ts:175`), то есть на провод уходит `{}` с `Content-Type: application/json` (`:174`). Заголовков нет ни одного: **`Idempotency-Key` не шлётся**, хотя вызов создаёт новую строку (`bccService.ts:77` против `:43`, `:64` у send/log) → находка БАГ-03. Мок ловит путь регуляркой `/^\/api\/bcc\/events\/([^/]+)\/no-response$/` (`mocks/index.ts:933`) и тело не читает вовсе (`:935-936`).
- Форма ответа: `BccRequest` — **новая** строка со `status: 'no_response'`, исходная не меняется (`src/services/bccService.ts:76`, `mocks/bcc.ts:372-388`). Новая запись копирует у исходной `requestId`, `supplierId`, `supplierName`, `productId`, `productName`, `source` (`:377-383`), получает собственный `id` вида `evt-${Date.now()}` (`:376`) и сегодняшнюю дату `new Date().toISOString().slice(0, 10)` (`:378`); `price` и `unit` в ней отсутствуют. Мок кладёт её в начало ленты сам (`:386`) **и** возвращает клиенту, который кладёт её туда же ещё раз в свою копию (`BccRequestPage.vue:370`).
- Коды ошибок: **ни одного, и это хуже, чем ноль**. `mockMarkNoResponse` на неизвестном `eventId` возвращает `null` (`mocks/bcc.ts:373-374`), ветка отдаёт этот `null` как значение типа `BccRequest` (`mocks/index.ts:933-937`), и страница кладёт его в ленту без проверки: `history.value.unshift(newEvt)` (`BccRequestPage.vue:370`) → находка БАГ-04. Ни `EVENT_NOT_FOUND`, ни любого другого кода в домене нет: `grep -rn "EVENT_NOT_FOUND\|BCC_" frontend_vue/src backend` — пусто.
- Save-режим: quick-action, уходит сразу по крестику в строке `sent` (`BccRequestPage.vue:949` — `bcc-request-history-cancel-btn` → `markNoResponse`, `:367-374`). Save bar не участвует; ошибка показывается общим тостом `msg.status_error` (`:372`).
- Пробел контракта: старый раздел (`03-api-contract.md:609-614`) верен по форме (пустое тело, новая строка, «не мутирует исходник»), но: **(а)** не сказано, **кто и когда** отмечает молчание — сегодня это только ручное действие человека, автоматического срока «поставщик не ответил за N дней» в проекте нет (`grep -rn "no_response" frontend_vue/src | grep -v spec` даёт лишь тип, пилюлю, мок и обработчик страницы); **(б)** не сказано, что операция **не идемпотентна**: два клика — две строки, и `Idempotency-Key` не шлётся (см. выше); **(c)** не сказано, что уведомления это событие **не рождает** — и что так задумано: причина записана в коде рядом с обратным случаем (`mocks/bcc.ts:365-367` — «a feed that reports silence as news would fill up with things that did not happen»); **(г)** не сказано, что новая строка наследует `source` исходной (`mocks/bcc.ts:383`), то есть «не ответил на телефонный запрос» останется с источником `Телефон`.
- Источник истины: мок + клиент. Формы — `src/services/bccService.ts:76-78`, `mocks/bcc.ts:372-388`, `mocks/index.ts:933-937`. Схема хранения — бэкенд: строка ложится в `bcc_events` со `status`/`source` как `String(50)` без enum (`backend/app/modules/bcc/shared/models.py:68-73`).

### POST /api/bcc/events/:id/response
- Вызывающий: `src/services/bccService.ts:73`
- Бэкенд: **нет**
- Мок: `mocks/index.ts:925`
- Форма запроса: путь плюс `{ price: number; unit: string }` (`src/services/bccService.ts:69-74`). Заголовков нет — **`Idempotency-Key` не шлётся** при том, что вызов создаёт строку (`:73`) → находка БАГ-03. `unit` приходит из константы страницы: `UNIT_OPTIONS = ['kg', 'm', 'piece', 'ton']` (`BccRequestPage.vue:333`), дефолт `'kg'` (`:331`, `:340`), значение подставляется в модалке (`:1049`). Это **не** идентификаторы справочника единиц: справочник владеет ими в настройках (`AppSettings.uoms`, `src/types/settings.ts:240`; сид — `uom-t`, `uom-kg`, … `mocks/settings.ts:89-101`), и склад строит свой список именно из него (`src/views/admin/warehouse/WarehousePage.vue:423-437`, потребители `:440-444`) → находка БАГ-07. `price` клиент приводит `Number()` и проверяет только на `NaN` (`BccRequestPage.vue:349`), нуля и отрицательного не отсекает.
- Форма ответа: `BccRequest` — **новая** строка со `status: 'responded'`, `price` и `unit` из тела (`src/services/bccService.ts:72`, `mocks/bcc.ts:345-370`). Остальные шесть полей копируются у исходного события (`:353-359`), `id` — `evt-${Date.now()}` (`:352`), `date` — сегодняшняя (`:354`). Мок кладёт строку в ленту сам (`:364`), возвращает её же, и страница кладёт её в свою копию (`BccRequestPage.vue:359`).
- Коды ошибок: **ни одного, при двух обещанных**. `mockAcceptResponse` на неизвестном `eventId` возвращает `null` (`mocks/bcc.ts:349-350`), ветка отдаёт его как `BccRequest` (`mocks/index.ts:925-932`), страница кладёт `null` в ленту (`BccRequestPage.vue:359`) → БАГ-04. Обещанного контрактом «422, если `price <= 0`» (`03-api-contract.md:607`) нет ни во фронте, ни в моке: `mockAcceptResponse` цену не проверяет вовсе (`mocks/bcc.ts:345-370`), клиент проверяет только `NaN` (`BccRequestPage.vue:349`). Кода `VALIDATION_ERROR` ядро объявляет (`backend/app/core/exceptions.py:23-27`), но домен его не бросает: роутов нет.
- Save-режим: quick-action в модалке. Открывается кнопкой «принять» у строки `sent` (`BccRequestPage.vue:931`, `:971` → `openResponseModal`, `:335-342`) и кнопкой «править» у строки `responded` (`:991`); сохранение — `savePrice` (`:348-365`), после успеха модалка закрывается и показывается тост `msg.price_saved` (`:361`). Save bar не участвует.
- Пробел контракта: старый раздел (`03-api-contract.md:603-607`) верен по природе (event-sourcing, старая строка остаётся), но: **(а)** его строка `- **Body:** \`{ price: number; unit: 'kg'|'m- **Response 200:** \`BccRequest\`…\` (`:606`) **порвана** — перечисление единиц оборвано на `'m` и склеилось со следующим пунктом, то есть допустимого набора единиц старый контракт не называет вовсе; **(б)** «Также обновляет `priceHistory` карточки соответствующего супплайера» (`:607`) кодом не поддержано: `grep -c "priceHistory" frontend_vue/src/services/mocks/bcc.ts` → 0, `grep -c "lastBccDate" frontend_vue/src/services/mocks/bcc.ts` → 0 (то же наблюдение с другой стороны — аудит suppliers, графа «Производные значения»); **(в)** обещанное 422 на `price <= 0` не реализовано нигде (см. выше); **(г)** не сказано, что событие рождает уведомление — единственное во всём домене (`mocks/bcc.ts:368`, см. графу «События и уведомления»), — и что «правка ответа» рождает его **повторно** (БАГ-10); **(д)** не сказано, в какой валюте цена: поля валюты у `BccRequest` нет (`src/types/bcc.ts:21-34`), у `bcc_events` — тоже (`backend/app/modules/bcc/shared/models.py:74-75` — только `price` и `unit`), при том что у поставщика валюта своя (аудит suppliers, графа «Значения по умолчанию»).
- Источник истины: мок + клиент. Формы — `src/services/bccService.ts:69-74`, `mocks/bcc.ts:345-370`, `mocks/index.ts:925-932`. Схема — бэкенд: `price` это `Numeric(12, 2)` nullable, `unit` — `String(20)` nullable (`backend/app/modules/bcc/shared/models.py:74-75`), то есть точность цены на сервере два знака, а клиент шлёт любое `Number`.

### POST /api/bcc/log
- Вызывающий: `src/services/bccService.ts:57`
- Бэкенд: **нет**
- Мок: `mocks/index.ts:917`
- Форма запроса: `{ productIds: string[]; recipientIds: string[]; source: TranslatedString }` плюс заголовок `Idempotency-Key` (`src/services/bccService.ts:49-67`). `source` клиент **нормализует**: строка превращается в `TranslatedString` текущей локали через `toTranslatedString(payload.source, locale)` (`:61`), то есть на проводе всегда объект, никогда строка. Значение приходит из константы страницы `SOURCE_OPTIONS = ['BCC Tool', 'Email', 'Phone', 'Messenger', 'Other']` (`BccRequestPage.vue:377`), дефолт `'Email'` (`:379`); переводы — вторая константа `SOURCE_TRANSLATIONS` там же (`:381-387`). Ключ идемпотентности генерируется на каждый вызов (`bccService.ts:64` → `newIdempotencyKey`, `src/services/api.ts:240-245`), мок его чтит: `withIdempotency` кэширует результат по ключу (`mocks/index.ts:262-269`, применение `:919`). Файлов вложений тело **не несёт** — `fileIds` есть только у `send`.
- Форма ответа: `{ requestId: string }` и больше ничего (`src/services/bccService.ts:56`, `mocks/bcc.ts:337-343`). `mockLogBccRequest` — три строки: игнорирует весь payload (имя параметра `_payload`, `:337`) и возвращает `{ requestId: \`req-${Date.now()}\` }` (`:342`). **Ни одной строки истории не создаётся** → находка БАГ-11. Возвращённый `requestId` клиент **выбрасывает**: `await logBccRequest(...)` без присваивания (`BccRequestPage.vue:430-437`), а строки получают номер, посчитанный на клиенте (`nextRequestId`, `:264-275`, вызов `:391`) → находка БАГ-02.
- Коды ошибок: **ни одного** — `mockLogBccRequest` не бросает (`mocks/bcc.ts:337-343`). Валидация целиком клиентская и возвращает не коды, а тосты: `validateSelection` требует непустых наборов товаров и получателей (`BccRequestPage.vue:415-425`). Ошибка любого рода показывается общим тостом `msg.status_error` (`:452`).
- Save-режим: quick-action, уходит сразу по split-кнопке «Log: `<source>`» (`BccRequestPage.vue:554-584` → `onLogClick` `:456-459` и `onLogSourcePick` `:461-465` → `logRequest` `:427-454`). После успеха страница сама сочиняет N×M строк (`createEventRows` `:390-413`), кладёт их в свою ленту (`:439`) **и в мок-стор** (`:440`, БАГ-01), показывает тост и сбрасывает выбор товаров (`:441-450`).
- Пробел контракта: старый раздел (`03-api-contract.md:587-601`) расходится с кодом в трёх местах: **(а)** тело описано с `attachments?: { fileIds: string[] }` (`:597`) — клиент вложений при логировании не шлёт вовсе (`src/services/bccService.ts:49-67`); **(б)** `source` описан как строковый union из пяти значений (`:596`), а на проводе это `TranslatedString` (`bccService.ts:61`); **(в)** ответ описан как `{ requestId: string; events: BccRequest[] }` — «массив созданных строк, чтобы клиент сразу подложил в `history`» (`:600`), а в коде ответ несёт только `requestId`, и строки сочиняет клиент (БАГ-11, БАГ-02). Не сказано и того, что **отправка и логирование обязаны создавать строки одинаково**: у обоих это одна и та же функция страницы `createEventRows` (`BccRequestPage.vue:291`, `:438`), отличающаяся только значением `source`.
- Источник истины: мок + клиент. Формы — `src/services/bccService.ts:49-67`, `mocks/bcc.ts:337-343`, `mocks/index.ts:917-923`. Схема — бэкенд: строка события несёт `source: String(50)` NOT NULL (`backend/app/modules/bcc/shared/models.py:71-73`), то есть одно значение, а не три локали.

### POST /api/bcc/send
- Вызывающий: `src/services/bccService.ts:35`
- Бэкенд: **нет**
- Мок: `mocks/index.ts:910`
- Форма запроса: `{ productIds: string[]; recipientIds: string[]; subject: TranslatedString; body: TranslatedString; fileIds?: string[] }` плюс заголовок `Idempotency-Key` (`src/services/bccService.ts:25-46`). `subject` и `body` клиент **нормализует** в `TranslatedString` текущей локали (`:39-40`), хотя собирает их на всех трёх языках сразу (`src/domain/bccEmail.ts:82-88`, `:96-108`) — то есть две локали письма на провод не попадают. `fileIds` — идентификаторы, уже загруженные общим аплоадом: страница кладёт `u.fileId` из ответа `DropZone` (`BccRequestPage.vue:309-318`, `src/components/admin/ui/DropZone.vue:4`), композабл собирает их из вложений шаблона (`src/composables/useBccRequest.ts:131`). Отдельного `POST /api/bcc/attachments` нет — так и записано в старом контракте (`03-api-contract.md:616`).
- Форма ответа: `{ requestId: string }` (`src/services/bccService.ts:34`, `mocks/bcc.ts:334`). Тот же разрыв, что у `log`: строк истории ответ не несёт, а `requestId` клиент **выбрасывает** — `await send()` без присваивания (`BccRequestPage.vue:290`), номер считается своим `nextRequestId()` (`:264-275`, вызов `:391`) → БАГ-02, БАГ-11. Побочный эффект мока, которого нет у `log`: конверт письма пишется в журнал `MOCK_SENT_EMAILS` (`mocks/bcc.ts:292`, запись `:323-332`), и именно он делает главное правило домена проверяемым (`mocks/bcc-envelope.spec.ts`, 89 строк, пять проверок `:40,48,58,68,75`).
- Коды ошибок: **один во фронте, два на бэкенде.** Мок бросает `MAIL_NOT_CONFIGURED`, когда почта не настроена (`mocks/bcc.ts:317`), условие — общий для проекта `isMailConfigured` (`src/types/settings.ts:167-171` через `mockIsMailConfigured`, `mocks/settings.ts:611-613`); тот же код бросает тест почты в настройках (`mocks/settings.ts:621`), то есть код кросс-доменный (см. аудит settings, `POST /api/settings/mail/test`). Бэкенд объявляет **два**: `MAIL_NOT_CONFIGURED` (`backend/app/modules/bcc/features/send_request/domain.py:23-31`) и `NO_RECIPIENTS` (`:34-38`) — второго фронт не знает вовсе: `grep -rn "NO_RECIPIENTS" frontend_vue/src` → 0. Ни один из двух до человека в BCC-инструменте **не доходит**: `catch` показывает общий тост `msg.status_error` (`BccRequestPage.vue:304-306`), а разбора кода нет ни там, ни в композабле (`src/composables/useBccRequest.ts:145-148` кладёт `e.message` в `error`) → находка БАГ-08. Для сравнения, страница почтовых настроек код читает — хотя и из `e.message`, а не из `code` (`src/views/admin/settings/MailSettings.vue:79-84`; это находка соседа, см. аудит settings, находка 19).
- Save-режим: quick-action по кнопке «Send» (`BccRequestPage.vue:586-590` → `sendRequest`, `:277-307`). Кнопка гаснет, пока почта не настроена или идёт отправка: `:disabled="sending || !mailReady"` (`:588`), где `mailReady = isMailConfigured(settings.mail)` (`src/composables/useBccRequest.ts:79`) — то есть гейт смотрит на **состояние сервера**, ровно как кнопка теста почты (аудит settings, «Правила домена», п. 5). Перед отправкой страница проверяет непустоту наборов (`:278` → `validateSelection` `:415-425`) и непустую тему в текущей локали (`:280-284`), потом переносит `selectedRecipientIds` в флаги `recipients[].selected` (`:286-288`), потому что композабл собирает адресатов именно из них (`useBccRequest.ts:130`). После успеха — те же три шага, что у `log`: свои строки в ленту (`:292`), те же строки в мок-стор (`:293`, БАГ-01), сброс товаров с сохранением преднастроенного получателя (`:296-303`).
- Пробел контракта: старый раздел (`03-api-contract.md:565-585`) — самый подробный в домене и в главном **верен**: одно письмо, все адреса в BCC, отправитель из `GET /api/settings/mail`, отказ `MAIL_NOT_CONFIGURED` вместо молчаливого успеха, ссылки на обе проверки (`:585`). Расходится он в четырёх местах: **(а)** тело описано вложенным — `template: { subject, body, attachments: { fileIds } }` (`:572-577`), а клиент шлёт плоское с `fileIds` на верхнем уровне (`src/services/bccService.ts:35-45`); **(б)** `subject`/`body` описаны строками, на проводе — `TranslatedString` (`:39-40`); **(c)** «Одновременно создаётся N × M row'ов в history со `status: 'sent'`, `source: 'BCC Tool'`» (`:585`) — сервером не создаётся ни одной, это делает клиент (БАГ-01, БАГ-11); **(г)** формат `requestId` объявлен как `req-###` (`:581`), мок отдаёт `req-${Date.now()}` (`mocks/bcc.ts:334`), а клиент подставляет свой `req-NNN` (`BccRequestPage.vue:274`) — три формата на одно поле, при том что колонка `request_id` это `String(50)` (`backend/app/modules/bcc/shared/models.py:54-56`). Не сказано и того, что **вложения нигде не разрешаются в файлы**: мок кладёт `fileIds` в конверт как есть (`mocks/bcc.ts:330`) и в реестр загруженных файлов (`mocks/index.ts:281`) не заглядывает.
- Источник истины: **по форме запроса и ответа** — мок и клиент (`src/services/bccService.ts:25-46`, `mocks/bcc.ts:310-335`); **по правилу отправки** — бэкенд, и он старше: `build_bcc_envelope` кладёт всех получателей в `Bcc`, а в `To` — самого отправителя (`backend/app/modules/bcc/features/send_request/domain.py:101-107`), снимает дубли с сохранением порядка (`:97`), отказывает при пустом списке (`:98-99`) и при ненастроенной почте (`:92-93`); `send_bcc_request` делает **ровно один** `send_message` (`:123-124`). Мок повторяет это же (`mocks/bcc.ts:319-332`), но дубли не снимает и пустой список не отвергает — то есть слабее сервера на два правила. По **схеме хранения** источник тоже бэкенд: `subject`, `body`, `attachment_file_ids`, `sender_user_id` — колонки `bcc_events` (`models.py:76-83`), которых нет ни в типе фронта, ни в моке.

## Обязанности сервера

Заполняется как НАБЛЮДЕНИЕ: что знает мок, что знает бэкенд, где во фронте стоит константа
на месте серверного значения. Ответ «нигде» — это не решение, а строка в
`00-решения-владельца.md` с указанием домена.

- Значения по умолчанию и их владелец: **владельцу принадлежит ровно одно — почтовый сервер, и домен его только читает.** Отправитель, хост, шифрование и признак готовности берутся из настроек: `mailFrom` собирается из `settings.mail.fromName`/`fromEmail` (`src/composables/useBccRequest.ts:64-72`), `mailReady` — общий `isMailConfigured` (`:79`, тип `src/types/settings.ts:167-171`), мок берёт те же поля через `mockGetMail()` (`mocks/bcc.ts:318`, конверт `:324-325`), бэкенд объявляет ту же форму своим `MailServerConfig` (`backend/app/modules/bcc/features/send_request/domain.py:41-55`) с тем же правилом из трёх полей (`:58-65`). Своей копии у BCC нет — так и записано в коде (`useBccRequest.ts:64-67`). **Всё остальное — константы фронта.** Единицы цены: `UNIT_OPTIONS = ['kg','m','piece','ton']` и дефолт `'kg'` (`src/views/admin/suppliers/BccRequestPage.vue:333`, `:331`, `:340`), мимо справочника `AppSettings.uoms` (`src/types/settings.ts:240`), из которого строит свой список склад (`src/views/admin/warehouse/WarehousePage.vue:423-437`) — БАГ-07. Источники запроса: `SOURCE_OPTIONS` из пяти строк и `SOURCE_TRANSLATIONS` на три локали (`BccRequestPage.vue:377`, `:381-387`), дефолт `'Email'` (`:379`); на схеме `source` — свободный `String(50)` без `CHECK` (`backend/app/modules/bcc/shared/models.py:71-73`). Текст письма: словарь `PHRASES` на три локали (`src/domain/bccEmail.ts:34-56`), и это **осознанно** — сервер получает готовый текст и своей константы с названием компании не держит (`03-api-contract.md:585`; причина в коде — `src/domain/bccEmail.ts:10-12`: до 2026-08-28 там стояло имя постороннего юрлица). Размеры страниц: товары 10 из `10/25/50` (`BccRequestPage.vue:97`, `:170-174`), получатели 5 из `5/10/25` (`:194`, `:229-233`), лента 25 литералом (`src/composables/useBccRequest.ts:106`); справочника под них нет ни в настройках, ни на схеме. Кому принадлежат перечень единиц и перечень источников — **нигде**, вынесено в `00-решения-владельца.md`.
- События и уведомления: домен рождает **ровно одно уведомление и только в одной точке** — `notifySupplierResponse` вызывается из `mockAcceptResponse` (`src/services/mocks/bcc.ts:368` → `src/services/mocks/notifications.ts:657-670`). Это один из семи триггеров проекта (перечень и правило «событие — это переход, а не момент» выведены соседом: см. аудит notifications, графа «События и уведомления» и «Правила домена», п. 4). **Расхождение с этим правилом здесь и есть находка:** остальные шесть триггеров защищены условием перехода и проверены спекой (`mocks/notification-triggers.spec.ts:111-119,181-200,232-234,257-266`), а `mockAcceptResponse` зовёт эмиттер безусловно (`mocks/bcc.ts:368`), поэтому «править ответ» у уже отвеченной строки (кнопка `BccRequestPage.vue:991` → тот же `savePrice` `:348-365`) рождает второе уведомление о том же ответе — БАГ-10. Отправка и логирование не рождают ничего: `grep -n "notify" frontend_vue/src/services/mocks/bcc.ts` даёт две строки — импорт (`:4`) и единственный вызов (`:368`). Отсутствие уведомления у `no-response` — **не пропуск, а решение**, и причина записана в коде (`mocks/bcc.ts:365-367`). Уведомление при этом ведёт на карточку поставщика по `entityId: supplier.id` (`notifications.ts:667-669`), а `id` берётся из строки события (`bcc.ts:368`) — то есть попадает в чужое пространство идентификаторов (аудит suppliers, БАГ-02; здесь — БАГ-05). Кому адресовано уведомление и рождает ли отправка запроса событие — **нигде**, вынесено в `00-решения-владельца.md`.
- Запись в аудит-лог: **нигде.** `grep -c "auditLog" frontend_vue/src/services/mocks/bcc.ts` → 0: ни отправка, ни логирование, ни принятие ответа, ни отметка молчания следа не оставляют. В перечне сущностей ленты аудита BCC нет — девять логов, которые читает `audit-feed`, лежат на чужих сущностях (`mocks/index.ts:392-393`), и `bcc` среди них не значится. Формально лента событий домена **и есть** его журнал — тип объявляет это прямо (`src/types/bcc.ts:20`), — но автора у записи нет: `BccRequest` не имеет ни поля пользователя, ни времени точнее суток (`src/types/bcc.ts:21-34`, `date` — `YYYY-MM-DD`, `:24`). На схеме автор частично предусмотрен: колонка `sender_user_id` с `ondelete="SET NULL"` (`backend/app/modules/bcc/shared/models.py:79-83`, миграция `f96e6fb2d5cf_phase_8_bcc.py:50`) и `created_at` с индексом (`models.py:84-89`) — но заполнять их некому: вызывающих у слайса ноль, а мок про пользователя не знает вовсе (`grep -in "tenant\|userId\|user_id" frontend_vue/src/services/mocks/bcc.ts` — пусто). Кто автор принятия ответа и остаётся ли след у отправки — **нигде**, вынесено в `00-решения-владельца.md`.
- Кастомные поля: домен их не имеет ни в каком виде — ни определений, ни значений: `grep -rn "fieldValues\|FieldDefinition\|customField" frontend_vue/src/types/bcc.ts frontend_vue/src/services/mocks/bcc.ts backend/app/modules/bcc` → 0 попаданий. Обе таблицы домена состоят из фиксированных колонок и ни одной ссылки на библиотеку определений (`backend/app/modules/bcc/shared/models.py:11-89`). Единственная точка соприкосновения с библиотекой полей — **чужая**: в карточке поставщика есть поле `f-last-bcc` «Дата последнего BCC» (`frontend_vue/src/services/mocks/config.ts:92-94`, размещение в секции `:162`), то есть определение живёт в домене `config`, значение — у поставщика, а BCC его только должен был бы обновлять (и не обновляет: `grep -c "lastBccDate" frontend_vue/src/services/mocks/bcc.ts` → 0; та же дыра с другой стороны — аудит suppliers, графа «Производные значения»).
- Настройки, которых мок не отслеживает: **пять, и первая — та единственная, которую он как раз отслеживает, но не до конца.** (1) **Почта.** Мок читает хост, отправителя и признак готовности (`mocks/bcc.ts:317-318`) и на ненастроенном сервере отказывает — это правило доказано спекой (`mocks/bcc-envelope.spec.ts:68-73`). Чего он не делает: не снимает дубли адресов и не отвергает пустой список получателей, тогда как сервер делает и то, и другое (`backend/app/modules/bcc/features/send_request/domain.py:97-99`, тесты `backend/tests/modules/bcc/test_send_request.py:97-103`, `:161-165`) — то есть мок **слабее** сервера на два правила, и оба пути ошибки под моками не воспроизводятся. (2) **Вложения.** `fileIds` записываются в конверт как есть (`mocks/bcc.ts:330`) и в реестр загруженных файлов (`mocks/index.ts:281`) не разрешаются; колонка под них на схеме есть — `attachment_file_ids: JSON` (`backend/app/modules/bcc/shared/models.py:78`), и это одна из двух ссылок на файлы во всём бэкенде (см. аудит uploads, строка «События и уведомления» в `00-решения-владельца.md`). (3) **Срок жизни ленты.** Удаления в домене нет: `DELETE` среди семи путей отсутствует, функции удаления в `mocks/bcc.ts` нет (`grep -n "^export function" frontend_vue/src/services/mocks/bcc.ts` даёт семь имён, ни одного `delete`), при том что старый контракт сам предупреждает про «50k+ event-rows за год» (`03-api-contract.md:563`). (4) **Обратная связь с поставщиком.** Ни `lastBccDate`, ни `priceHistory` карточки поставщика домен не трогает (оба грепа → 0), хотя старый контракт этого требует (`:607`). (5) **Каталог товаров.** Дерево BCC — константа мока (`mocks/bcc.ts:7-118`), связанная с товарами только захардкоженной картой имён категорий (`:122-138`); ни `products`, ни `categories` в моке BCC не импортируются (`grep -n "^import" frontend_vue/src/services/mocks/bcc.ts` — четыре импорта: типы, `suppliers`, `notifications`, `settings`). Все пять вынесены в `00-решения-владельца.md`, кроме первой (она — находка про мок).
- Мультиарендность: во фронте не выражена **никак** — ни `tenantId`, ни `userId`, ни заголовка: `grep -in "tenant\|userId\|user_id" frontend_vue/src/services/mocks/bcc.ts` пусто, а `bccService.ts` (78 строк) шлёт заголовки только двух видов и только у двух вызовов — `Idempotency-Key` у `send` (`:43`) и у `log` (`:64`); остальные пять вызовов идут без `options` вовсе (`:7`, `:11`, `:21`, `:73`, `:77`), то есть без единого заголовка (`options?.headers` — единственный их источник, `src/services/api.ts:144-158`, `:163-175`) → находка БАГ-09. На сервере выражена схемой, и на обеих таблицах: `bcc_categories.tenant_id` — FK на `tenants.id`, `ondelete="CASCADE"`, `nullable=False, index=True` (`backend/app/modules/bcc/shared/models.py:16-21`) и `bcc_events.tenant_id` теми же условиями (`:48-53`); миграция `f96e6fb2d5cf_phase_8_bcc.py:28,39`. Плюс адресность автора — `sender_user_id` FK на `users.id` (`models.py:79-83`). Как сервер узнаёт арендатора и автора — в домене не выражено ничем; соседи с живым бэкендом шлют `Authorization: Bearer` + `X-CSRF-Token` (`src/services/settingsService.ts:18,27`, `src/services/auditFeedService.ts:20,41`), и сервер достаёт из токена `user_id` (`backend/app/modules/settings/features/crud/action.py:97-128`). Вынесено в `00-решения-владельца.md`.
- Права — в какой функции проверяются: **нигде на уровне действия.** Доступ гейтится только фича-флагами, и их два: страница целиком — `meta.featureFlag: 'bccRequest'` на роуте (`src/router/index.ts:199-202`), панель истории — `useFeatureFlag('bccHistory')` (`src/views/admin/suppliers/BccRequestPage.vue:34`, применение `:869`). Оба объявлены `true` константой (`src/config/featureFlags.ts:18`, `:34`) и типизированы (`src/types/features.ts:15`, `:30`); на бэкенде оба заведены записями каталога фич — `bccRequest` уровня `page`, `bccHistory` уровня `section` (`backend/alembic/versions/8cf3bfa380dd_phase_12_plans_multi_role.py:63-65`, `:101-103`; колонки — `:256-262`), то есть это тариф, а не право. В матрице прав BCC-инструмента нет: `grep -in "bcc" frontend_vue/src/services/mocks/config.ts frontend_vue/src/types/config.ts` даёт три попадания, и все три — про **поле карточки поставщика** `f-last-bcc` (`mocks/config.ts:92-94`, `:162`), а не про отправку. Функции, которая проверяет право отправить письмо от лица компании или принять цену поставщика, нет ни одной; ни во фронте, ни в моке, ни на сервере (роутов ноль). Вынесено в `00-решения-владельца.md`.
- Транзакционность и идемпотентность: **разделено пополам, и обе половины неполны.** `Idempotency-Key` шлют два вызова из семи — `send` (`src/services/bccService.ts:43`) и `log` (`:64`), генератор общий (`src/services/api.ts:240-245`), мок ключ чтит и возвращает закэшированный результат (`mocks/index.ts:262-269`, применение `:912`, `:919`). Не шлют его `POST /api/bcc/events/:id/response` (`bccService.ts:73`) и `.../no-response` (`:77`) — **при том, что каждый из двух создаёт новую строку события** (`mocks/bcc.ts:364`, `:386`), то есть двойной клик даёт две записи → БАГ-03. Оптимистичной блокировки нет нигде: `grep -c "If-Match\|version" frontend_vue/src/services/bccService.ts` → 0. Атомарность **письма** гарантирована и доказана с обеих сторон: один `send_message` на отправку (`backend/app/modules/bcc/features/send_request/domain.py:123-124`, тест «a loop would leave len(SUPPLIERS) messages here» — `backend/tests/modules/bcc/test_send_request.py:81-85`), один конверт в журнале мока (`mocks/bcc.ts:323`, спека `mocks/bcc-envelope.spec.ts:40-46`). Атомарность **записи** не выражена нигде и это главное наблюдение графы: «письмо ушло» и «в ленте появились N × M строк» — сегодня два разных места, причём второе живёт **в браузере**: мок строк не создаёт вовсе (`mocks/bcc.ts:310-335`, `:337-343` — БАГ-11), а страница сочиняет их сама и пишет прямо в мок-стор (`BccRequestPage.vue:291-293`, `:438-440` — БАГ-01). Что делать, если письмо ушло, а строки не записались, — **нигде**, вынесено в `00-решения-владельца.md`.
- Производные значения (считать, не хранить): пять. (1) `totalPages` — `Math.max(1, Math.ceil(total / pageSize))` при чтении (`mocks/bcc.ts:265`), колонки под него нет. (2) `total` — длина всей ленты, фильтров у эндпоинта нет (`:258`). (3) **«Текущее состояние пары (запрос, поставщик, товар)» — производное от порядка событий, и считает его сегодня клиент:** `isLatestEvent` берёт первое совпадение ключа `requestId::supplierId::productId` в массиве (`BccRequestPage.vue:492-496`), потому что новая строка всегда кладётся в начало (`mocks/bcc.ts:364`, `:386`). Сервер обязан вычислять то же самое, но по `created_at` (`backend/app/modules/bcc/shared/models.py:84-89`), а не по порядку выборки, — и у него для этого есть индекс, а у клиента нет даже времени точнее суток (`src/types/bcc.ts:24`). (4) `requestId` — **выводимое значение, которое сегодня считают оба и по-разному:** сервер возвращает своё (`mocks/bcc.ts:334`, `:342`), клиент его выбрасывает и считает `max(req-NNN) + 1` по загруженной странице ленты (`BccRequestPage.vue:264-275`) → БАГ-02. (5) `productCount` категории — на схеме **хранимая** колонка с дефолтом `0` (`backend/app/modules/bcc/shared/models.py:29-31`), в моке — константа у корней и ноль у всех пятнадцати листьев (`mocks/bcc.ts:11,38,55,77,99` против `:16` и далее); выводится ли она из каталога товаров или обновляется событием — на сегодня не сказано нигде, и это часть строки про природу дерева, вынесенной владельцу.

## Правила домена, которых нет в контракте

Самое ценное содержимое аудита: эндпоинты машина перечислит и без человека, а правило,
живущее только в моке или доменном слое, — нет.

1. **Одно письмо, все адреса в BCC — и это единственное правило проекта, реализованное на бэкенде раньше, чем эндпоинт.** `build_bcc_envelope` кладёт получателей только в `Bcc`, а в `To` — самого отправителя (`backend/app/modules/bcc/features/send_request/domain.py:101-107`), и причина названа в докстроке дословно: снаружи рассылка циклом выглядит идентично, поэтому гарантия выражена кодом с тестом, а не фразой в ревью (`:1-12`). Проверок три класса: одна отправка на многих (`backend/tests/modules/bcc/test_send_request.py:77-85`), ни одного адреса в видимых заголовках (`:106-113`) и — главное — **в передаваемых байтах нет ни списка получателей, ни заголовка `Bcc`** (`:115-133`, проверка на настоящем `smtplib.SMTP` с отобранным сокетом, `:56-73`). Мок повторяет то же со своей стороны (`frontend_vue/src/services/mocks/bcc.ts:319-332`, спека `mocks/bcc-envelope.spec.ts:39-56`).

2. **Отказ вместо тихого успеха — тоже правило, и оно одно на проект.** Ненастроенная почта отвергает отправку и на сервере (`domain.py:92-93`, тест по каждому из трёх обязательных полей — `test_send_request.py:143-153`), и в моке (`mocks/bcc.ts:317`, спека `bcc-envelope.spec.ts:68-73`). Условие — общий `isMailConfigured`, сужённый до трёх полей нарочно (см. аудит settings, «Правила домена», п. 2); бэкенд повторяет его теми же тремя (`domain.py:58-65`) и явно разрешает пустые логин и имя отправителя (`test_send_request.py:155-159`).

3. **Сервер сильнее мока на два правила отправки.** Дубли адресов снимаются с сохранением порядка — «тот же поставщик дважды получил бы запрос дважды из одной отправки» (`domain.py:95-97`, тест `:93-103`); пустой список получателей отвергается кодом `NO_RECIPIENTS` (`domain.py:98-99`, `:34-38`, тест `:161-165`). Мок не делает ни того, ни другого (`mocks/bcc.ts:319-321`), и кода `NO_RECIPIENTS` фронт не знает вовсе. Значит два пути ошибки под моками не воспроизводятся — и первый из них не «ошибка интерфейса», а двойное письмо живому поставщику.

4. **Отправка не бывает частичной: конверт один, поэтому «ушло половине» невозможно по построению.** Транспорт открывает **одну** SMTP-сессию на одну отправку и закрывает её в `finally` (`backend/app/modules/bcc/features/send_request/transport.py:20-43`), а `send_message` без `to_addrs` — именно то, что превращает `Bcc` в конвертных получателей, не вынося заголовок на провод (`:46-54`, комментарий `:48-51`). Неизвестное значение `encryption` трактуется как `none`, а не роняет отправку, и причина названа (`:24-27`).

5. **Событие — строка, а не состояние: ни одна запись ленты никогда не правится.** «Ответ пришёл» и «не ответил» создают **новые** строки, копируя у исходной шесть полей и получая свои `id` и `date` (`mocks/bcc.ts:351-363`, `:375-385`). Исходная остаётся как след. Отсюда следствие для сервера: `UPDATE` по `bcc_events` не нужен ни одному сценарию домена, а «текущее состояние» — это выборка последней строки по ключу `(request_id, supplier_id, product_id)`.

6. **Тип фронта и схема таблицы расходятся по пяти полям, и схема старше — она уже в БД** (миграция `backend/alembic/versions/f96e6fb2d5cf_phase_8_bcc.py`):
   - `id`: фронт — строка вида `evt-001` / `evt-${Date.now()}` (`frontend_vue/src/types/bcc.ts:22`, значения `mocks/bcc.ts:143`, `:352`), схема — `UUID` из `UUIDMixin` (`backend/app/modules/bcc/shared/models.py:43`, миграция `:38`);
   - `date`: фронт — строка `YYYY-MM-DD` (`types/bcc.ts:24`, `mocks/bcc.ts:354`), схема — `created_at: DateTime(timezone=True)` с индексом и `server_default=func.now()` (`models.py:84-89`); поля `date` на схеме нет вовсе;
   - `supplierName` и `productName`: фронт хранит подписи прямо в строке события, обе `TranslatedString` (`types/bcc.ts:26,28`), схема хранит только внешние ключи `supplier_id` и `product_id` (`models.py:57-67`) — то есть сервер обязан подмешивать имена при чтении, а не хранить их;
   - `source`: фронт — `TranslatedString` (`types/bcc.ts:30`, нормализация `src/services/bccService.ts:61`), схема — `String(50)` NOT NULL (`models.py:71-73`);
   - `subject`, `body`, `attachment_file_ids`, `sender_user_id`: четыре колонки схемы (`models.py:76-83`), которых нет ни в типе, ни в моке — то есть содержимое письма сервер хранит, а фронт после отправки не видит никогда.

7. **Идентификатор «товара» BCC не существует больше нигде, а схема ссылается на `products.id`.** Листья дерева — `sheet-2mm`, `beam-i20`, `pipe-100` и ещё двенадцать (`mocks/bcc.ts:7-118`), и они встречаются только в моке домена и его спеке (`grep -rn "sheet-2mm" frontend_vue/src` → `mocks/bcc.ts`, `mocks/bcc-envelope.spec.ts:32,77`); товары мока — `prod-001`… (`mocks/products.ts:31`), категории — `cat-1`… (`mocks/categories.ts:11`). При этом `bcc_events.product_id` — FK на `products.id` с `ondelete="SET NULL"` (`models.py:63-67`; та же политика замечена соседом: см. строку `products · Транзакционность` в `00-решения-владельца.md`). Пока не решено, что такое `bcc_categories` — отдельный каталог или проекция `products`, серверная реализация не сможет ни принять `productIds` клиента, ни отдать своё дерево.

8. **Подпись письма собирается из настроек, а не из константы, и это следствие реального инцидента.** Тема — название компании и дата (`src/domain/bccEmail.ts:82-88`), тело — приветствие, список позиций и подпись менеджера (`:96-108`); источник полей — `AppSettings.company` и `AppSettings.profile` (`src/composables/useBccRequest.ts:43-49`). Причина записана в коде: до 2026-08-28 на их месте стояла константа с именем постороннего юрлица, и письмо уходило живому поставщику от лица чужой компании (`src/domain/bccEmail.ts:10-12`). Закреплено e2e — тема обязана называть **нашу** компанию и не содержать прежнего имени (`frontend_vue/tests/e2e/admin/suppliers/bcc-request.spec.ts:321-329`), тело — подписываться текущим менеджером (`:331-336`). Пустые поля выпадают вместе с «С уважением,», чтобы не оставлять висящее прощание (`bccEmail.ts:96-104`).

9. **Дата в теме и дата в ленте — один формат и одна функция.** `formatBccDate` живёт в доменном слое (`src/domain/bccEmail.ts:64-69`) и импортируется страницей под своим именем (`BccRequestPage.vue:26`), чтобы у инструмента не завелось двух форматов даты (причина — `bccEmail.ts:58-63`). На проводе при этом дата события — ISO (`src/types/bcc.ts:24`), а `dd.mm.yyyy` только для показа.

10. **Список получателей не сокращается никогда — совпадение по категории лишь всплывает наверх.** Мок возвращает всех поставщиков и помечает флагом `selected` тех, чьи категории покрывают выбранные товары (`mocks/bcc.ts:229-246`, причина — `:230-233`); страница сортирует выбранных вверх, но не прячет остальных (`BccRequestPage.vue:204-213`). Для сервера это значит, что `products` в запросе — параметр **подсказки**, а не фильтра, и пустой ответ по нему невозможен.

11. **Мок отдаёт ссылки на свои константы, а не копии.** `mockGetBccCategories` возвращает сам массив (`mocks/bcc.ts:226`), `mockGetBccHistory` — `slice` исходного (`:261`). Соседний домен на этом месте ставит `structuredClone` (аудит notifications, `GET /api/notifications`). Отсюда и возможность страницы дописывать «серверную» ленту у себя (БАГ-01), и то, что мутация ответа меняет «сервер».

12. **Тема и тело собираются на трёх языках, а уходит один.** `buildBccSubject`/`buildBccBody` строят `TranslatedString` целиком (`src/domain/bccEmail.ts:82-88`, `:96-108`), пересборка — на `watchEffect`, а не на `watch(..., { deep: true })`, и причина названа питфоллами #36/#37 (`src/composables/useBccRequest.ts:51-62`). Но на провод клиент кладёт `toTranslatedString(tf(...), locale)` — текущую локаль в объекте из одной заполненной ветки (`src/services/bccService.ts:39-40`, вход — `useBccRequest.ts:136-137`). Две другие локали письма не покидают браузер, а колонки под них на схеме и нет: `subject`/`body` — `Text` (`backend/app/modules/bcc/shared/models.py:76-77`).

## Находки про код → contract-sync-bcc-bugs.md

Одиннадцать, все записаны в `roo_code/plans/bugs/contract-sync-bcc-bugs.md`, код не тронут:

| | Суть | Файл |
|---|---|---|
| БАГ-01 | продакшн-страница импортирует мок-стор и пишет в него: «серверная» лента заполняется браузером | `src/views/admin/suppliers/BccRequestPage.vue:19,293,440` |
| БАГ-02 | `requestId`, который вернул сервер, выбрасывается — номер считается на клиенте по загруженной странице | `src/views/admin/suppliers/BccRequestPage.vue:264-275,290,430` |
| БАГ-03 | `response` и `no-response` создают строки без `Idempotency-Key`, хотя `send`/`log` его шлют | `src/services/bccService.ts:73,77` |
| БАГ-04 | мок возвращает `null` на неизвестный `eventId`, и клиент кладёт `null` в ленту | `src/services/mocks/bcc.ts:350,374`, `src/views/admin/suppliers/BccRequestPage.vue:359,370` |
| БАГ-05 | в одной ленте два пространства id поставщика: сиды `sup-001`, новые строки `'1'`…`'6'` | `src/services/mocks/bcc.ts:146,240` |
| БАГ-06 | идентификаторы «товаров» BCC не существуют ни в одном другом домене, а схема ссылается на `products.id` | `src/services/mocks/bcc.ts:7-118`, `backend/app/modules/bcc/shared/models.py:63-67` |
| БАГ-07 | единицы цены — константа из четырёх строк мимо справочника единиц настроек | `src/views/admin/suppliers/BccRequestPage.vue:333` |
| БАГ-08 | `MAIL_NOT_CONFIGURED` в BCC-инструменте до человека не доходит — общий тост вместо кода | `src/views/admin/suppliers/BccRequestPage.vue:304-306` |
| БАГ-09 | пять вызовов из семи идут без единого заголовка, а обе таблицы домена — `tenant_id NOT NULL` | `src/services/bccService.ts:7,11,21,73,77` |
| БАГ-10 | правка уже принятого ответа рождает второе уведомление о том же ответе | `src/services/mocks/bcc.ts:368` |
| БАГ-11 | `mockSendBccRequest` и `mockLogBccRequest` не создают ни одной строки события | `src/services/mocks/bcc.ts:310-335,337-343` |

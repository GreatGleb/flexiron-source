# Баги и уязвимости — страница 1.1 Товары

Найдены в ходе реализации Промптов 0–14 и финального самопроверочного прохода.

---

## БАГ-01 — Неиспользуемые импорты типов вызывали TypeScript TS6196

**Файлы:**
- `src/services/mocks/products.ts` — `LinkedSupplier` импортирован, не используется
- `src/services/productsService.ts` — `LinkedSupplier` импортирован, не используется
- `src/composables/useProductCard.ts` — `PriceUnit` импортирован, не используется
- `src/views/admin/products/ProductCardPage.vue` — `SvgIcon` импортирован, не используется

**Серьёзность:** Средняя — `npm run typecheck` падал

### Проблема

`LinkedSupplier` использовался только внутри `Product.linkedSuppliers` — тип массива элементов. TypeScript выводит его автоматически из интерфейса `Product`, явный импорт не нужен. Аналогично `PriceUnit` в `useProductCard.ts` — используется только как часть `Product`, не как самостоятельный тип. `SvgIcon` был добавлен в imports ProductCardPage.vue, но потом не использован в шаблоне (нет `<SvgIcon>` тегов в карточке).

### Исправление

Удалены из списков импортов:
- `LinkedSupplier` из `products.ts` и `productsService.ts`
- `PriceUnit` из `useProductCard.ts`
- `SvgIcon` из `ProductCardPage.vue`

### Правило на будущее

Перед написанием импортов — убедиться что тип используется явно в подписях или `ref<T>()`, а не только косвенно через другой интерфейс. Запускать `typecheck` после каждого Промпта, не только в конце.

---

## БАГ-02 — CustomSelect не принимает `string | null` в v-model

**Файл:** `src/views/admin/products/ProductsPage.vue`  
**Серьёзность:** Высокая — TypeScript ошибка + runtime несоответствие

### Проблема

`CustomSelect` ожидает `modelValue: string` (SelectOption.value всегда string). Поля `filters.categoryId: string | null` и `newProduct.categoryId: string | null` несовместимы — TypeScript-ошибка при прямой привязке `v-model="filters.categoryId"`.

Кроме того, `categoryOptions` первым элементом имел `{ value: null }` — `CustomSelect` сравнивает `value === modelValue`, и null никогда не совпадёт со строкой.

### Исправление

Computed-адаптеры для каждого поля `string | null`:

```ts
const categoryFilterStr = computed({
  get: () => filters.categoryId ?? '',
  set: (v: string) => { filters.categoryId = v || null },
})

const newProductCategoryStr = computed({
  get: () => newProduct.value.categoryId ?? '',
  set: (v: string) => { newProduct.value.categoryId = v || null },
})
```

`categoryOptions` первым элементом: `{ value: '', label: t('products.filter_category_all') }` (пустая строка, не null).

### Правило на будущее

`CustomSelect` принимает **только `string`** в v-model и в `SelectOption.value`. Для любого поля `string | null`:
1. Создать computed-адаптер `null ↔ ''`
2. Опция «Все/пусто» → `value: ''`, не `value: null`

---

## БАГ-03 — Новые feature flags не добавлены в FeatureFlagKey

**Файлы:**
- `src/types/features.ts`
- `src/config/featureFlags.ts`
- `tests/e2e/helpers/flags.ts`

**Серьёзность:** Высокая — `useFeatureFlag('adminServices')` и `useFeatureFlag('productSupplierLinks')` падали с TypeScript ошибкой «Argument of type ... is not assignable»

### Проблема

Два новых флага (`adminServices` — страница Услуги 1.3, placeholder; `productSupplierLinks` — секция поставщиков в карточке товара) были использованы в компонентах, но не были добавлены в union-тип `FeatureFlagKey` и объект `featureFlags`. TypeScript не мог проверить переданный ключ.

### Исправление

`src/types/features.ts`:
```ts
adminServices: boolean        // page-level, Services 1.3 placeholder
productSupplierLinks: boolean // section-level, supplier links in product card
```

`src/config/featureFlags.ts`:
```ts
adminServices: false,       // hidden until 1.3 Services page is built
productSupplierLinks: true, // supplier section visible in product card
```

`tests/e2e/helpers/flags.ts` — добавлены в `ALL_FLAGS_ENABLED`:
```ts
adminServices: true,
productSupplierLinks: true,
```

### Правило на будущее

Каждый новый `useFeatureFlag('...')` требует три изменения в одном коммите:
1. `types/features.ts` — добавить в интерфейс
2. `config/featureFlags.ts` — добавить с дефолтным значением
3. `tests/e2e/helpers/flags.ts` — добавить в `ALL_FLAGS_ENABLED: true`

---

## БАГ-04 — Кнопка «Назад» использовала несуществующий CSS класс `btn-ghost`

**Файл:** `src/views/admin/products/ProductCardPage.vue`  
**Серьёзность:** Высокая — кнопка рендерилась без стилей (прозрачная, без hover, без border)

### Проблема

Back-кнопка в header карточки была написана как:

```html
<button class="btn btn-ghost" @click="router.back()">
  ← {{ t('products.btn_back') }}
</button>
```

Класс `btn-ghost` не определён ни в одном CSS файле проекта. Также `router.back()` — ненадёжная навигация: если пользователь открыл карточку по прямой ссылке, `router.back()` уходит из приложения.

### Исправление

```html
<router-link :to="{ name: 'admin-products' }" class="btn btn-secondary">
  ← {{ t('products.btn_back') }}
</router-link>
```

- `btn btn-secondary` — существующий стиль из `_buttons.css`
- `router-link` с именованным маршрутом — всегда ведёт на список товаров, независимо от истории

### Правило на будущее

- Проверять имена CSS классов у кнопок в `_buttons.css`: только `btn-primary`, `btn-secondary`, `btn-danger` — `btn-ghost` не существует
- Кнопка «Назад» в карточке → `<router-link>` на список, не `router.back()` — пользователь мог зайти по прямой ссылке

---

## БАГ-05 — `v-model.number` ставит NaN при очистке поля — null не восстанавливается

**Файл:** `src/composables/useProductCard.ts`  
**Серьёзность:** Средняя — очистка поля цены/миностатка оставляла `NaN` в данных вместо `null`

### Проблема

`v-model.number` в Vue 3 вызывает `parseFloat("")` при пустом input → результат `NaN`. Далее:

- `JSON.stringify(NaN) === "null"` — `useDirtyCheck` **не замечает** изменение, если исходное значение тоже было `null`. Это корректно.
- Но если исходное значение было, например, `15.5` и пользователь очистил поле → dirty.diff() возвращает `{ price: NaN }` → `mockPatchProduct` сохраняет `NaN` в STORE → после reload поле показывает `NaN`.

```ts
// до исправления — NaN попадал в patchProduct:
form.value.price = NaN   // ← после очистки поля
await patchProduct(id, { price: NaN })  // ← STORE.price = NaN
```

### Исправление

Добавлен watcher в `useProductCard.ts` после `useDirtyCheck(form)`:

```ts
watch(
  form,
  (val) => {
    if (Number.isNaN(val.price as unknown)) form.value.price = null
    if (Number.isNaN(val.minStock as unknown)) form.value.minStock = null
  },
  { deep: true },
)
```

Как только `v-model.number` ставит NaN — watcher немедленно заменяет на `null`. `useDirtyCheck` видит `null` (валидное значение), diff корректен.

### Правило на будущее

Для **nullable number полей** (`number | null`) с `v-model.number` — обязателен watcher-нормализатор NaN → null. Альтернатива: использовать `v-model` (без `.number`) + computed с явным `parseFloat` и fallback на `null`.

---

## БАГ-06 — `DropZone` — не v-model компонент, требует `hint: string`

**Файл:** `src/views/admin/products/ProductCardPage.vue`  
**Серьёзность:** Высокая — компонент DropZone при неверном использовании не рендерил hint и не вызывал upload

### Проблема

Первоначальный черновик использовал `v-model` на `DropZone`:

```html
<!-- неверно: -->
<DropZone v-model="fieldValues[fv.fieldId]" />
```

`DropZone` не имеет `modelValue` prop — `v-model` молча игнорировался. Также `hint` — required prop — был пропущен, что давало runtime warning.

### Исправление

```html
<DropZone
  hint="PDF / image"
  @uploaded="(files) => ((fieldValues as Record<string, unknown>)[fv.fieldId] = files[0]?.fileId ?? null)"
/>
```

`@uploaded` эмитирует `UploadedFile[]`. Берём первый файл, сохраняем `fileId` в `fieldValues`.

### Правило на будущее

`DropZone` — **не v-model**. Контракт:
- `hint: string` — обязательный prop, текст подсказки
- `@uploaded="(files: UploadedFile[]) => ..."` — событие после загрузки
- Нет `modelValue`, нет `v-model`

---

## PAT-01 — `useDirtyCheck` использует `capture()`, а не `track()`

**Файл:** `src/composables/useProductCard.ts` (и любые будущие карточки)  
**Серьёзность:** Средняя — вызов несуществующего метода → runtime error «is not a function»

### Проблема

В плане (`1.1-products-plan.md`) и при первом написании composable был использован метод `track()`:

```ts
// план изначально написал:
dirty.track()  // ← не существует
```

Реальный API `useDirtyCheck`:

```ts
const { isDirty, capture, reset, diff } = useDirtyCheck(form)
// capture() — зафиксировать текущее состояние как baseline
// diff()    — shallow diff: только изменённые ключи
// reset()   — алиас capture()
// isDirty   — computed<boolean>
```

### Исправление

`capture()` вызывается сразу после заполнения `form.value` данными из API:

```ts
form.value = { name: data.name, sku: data.sku, ... }
dirty.capture()  // ← правильный метод
```

### Правило на будущее

`useDirtyCheck` публичный API:
| Метод/prop | Описание |
|---|---|
| `isDirty` | `Ref<boolean>` — есть ли изменения |
| `capture()` | зафиксировать baseline (вызывать после load) |
| `diff()` | возвращает `Partial<T>` только изменённых ключей |
| `reset()` | алиас `capture()` — сброс к baseline |

Метода `track()` не существует.

---

## БАГ-07 — `mockCreateProduct` не резолвит `categoryName`

**Файл:** `src/services/mocks/products.ts`  
**Функция:** `mockCreateProduct`  
**Серьёзность:** Средняя — созданный товар всегда показывает `—` в поле «Категория» на карточке

### Проблема

После создания товара с выбранной категорией и перехода на карточку — поле `categoryName` было `null`, потому что `mockCreateProduct` всегда устанавливал его в `null`:

```ts
categoryName: null,  // ← всегда null, categoryId игнорируется
```

При GET карточки `mockGetProduct` возвращает объект из STORE, где `categoryName: null`. Поле «Категория» в карточке показывало `—`.

### Исправление

```ts
categoryName: data.categoryId ? (mockGetCategory(data.categoryId)?.name ?? null) : null,
```

Импортирован `mockGetCategory` из `./categories`. Резолвит имя из categories STORE на момент создания. Без циклических зависимостей — `categories.ts` не импортирует `products.ts`.

---

## БАГ-08 — `linkedSuppliers` использует несуществующие ID `sup-1`, `sup-2`, `sup-3`

**Файл:** `src/services/mocks/products.ts`  
**Серьёзность:** Высокая — клик по строке поставщика в карточке товара навигирует на несуществующий маршрут → supplier card падает с 404

### Проблема

Данные `linkedSuppliers` в products STORE содержали выдуманные ID:
```ts
{ id: 'sup-1', name: 'Metinvest', ... }
{ id: 'sup-2', name: 'ArcelorMittal', ... }
{ id: 'sup-3', name: 'INTERPIPE', ... }
```

Suppliers STORE использует числовые строковые ID: `'1'`…`'6'`. Клик `router.push({ name: 'admin-supplier-card', params: { id: 'sup-1' } })` вёл на несуществующий `/admin/suppliers/sup-1`, карточка поставщика падала.

### Исправление

Заменены на реальные ID и имена из suppliers STORE:
```ts
// prod-001:
{ id: '1', name: 'Steel Plus OÜ', ... }
{ id: '2', name: 'Metal Trade LT', ... }

// prod-003:
{ id: '3', name: 'Nordic Steel AB', ... }
```

### Правило на будущее

При добавлении linked references в mock STORE — всегда использовать реальные ID из соответствующего STORE. Перед написанием mock данных — grep ID в целевом STORE.

---

## БАГ-09 — `<span class="glass-input">` для read-only поля — width:100% не работает

**Файл:** `src/views/admin/products/ProductCardPage.vue`  
**Серьёзность:** Средняя — поле «Категория» уже других полей (span inline, не block)

### Проблема

`.glass-input` задаёт `width: 100%`, но не задаёт `display`. На `<input>` и `<textarea>` это работает (они block/inline-block по умолчанию). На `<span>` — `display: inline`, поэтому `width: 100%` не применяется: поле отображается только по ширине текста.

```html
<!-- неверно: -->
<span class="glass-input">{{ product?.categoryName ?? '—' }}</span>
```

### Исправление

```html
<input
  :value="product?.categoryName ?? '—'"
  class="glass-input"
  type="text"
  readonly
  data-test="field-category"
/>
```

`<input readonly>` визуально идентичен редактируемому полю, но не принимает ввод.

### Правило на будущее

Read-only отображение значения в форме → `<input :value="..." readonly class="glass-input">`, не `<span class="glass-input">`. Span — только для текстовых меток вне формы.

---

## БАГ-10 — `form.sku` / `form.description` сохранялись как `''` вместо `null`

**Файл:** `src/composables/useProductCard.ts`  
**Серьёзность:** Средняя — после очистки поля список товаров показывает пустую ячейку вместо `—`

### Проблема

`<input v-model="form.sku">` при очистке поля ставит `form.sku = ''`. `useDirtyCheck.diff()` возвращает `{ sku: '' }`. Это уходит в `patchProduct` → STORE хранит `sku: ''`. В списке: `item.sku ?? '—'` — `??` не срабатывает на `''`, ячейка пуста.

### Исправление

В `save()` после `Object.assign(delta, dirty.diff())` добавлена нормализация:

```ts
if (delta.sku === '') delta.sku = null
if (delta.description === '') delta.description = null
```

Нормализация только при сохранении — не мешает вводу (пустая строка — допустимое промежуточное состояние).

### Правило на будущее

После `dirty.diff()` нормализовать nullable string поля: `if (delta.field === '') delta.field = null`. Поля с `?? '—'` в шаблоне не различают `null` и `''` — нужно гарантировать `null` из save.

---

## БАГ-11 — HTML-комментарии внутри `<template>` (pitfall #9)

**Файлы:** `src/views/admin/products/ProductCardPage.vue`, `src/views/admin/products/ProductsPage.vue`  
**Серьёзность:** Низкая — нарушение правила #9 из vue-rules.md

### Проблема

В шаблонах остались HTML-комментарии: `<!-- Header -->`, `<!-- Основное -->`, `<!-- Цена -->`, `<!-- Динамические поля -->`, `<!-- Поставщики -->`, `<!-- Create modal -->`, `<!-- Delete modal -->`. Правило проекта (pitfall #9) запрещает комментарии внутри `<template>`.

### Исправление

Все 7 комментариев удалены из обоих файлов.

---

## БАГ-12 — Заголовки секций через `<h2 class="section-title">` вместо `:title` prop у GlassPanel

**Файл:** `src/views/admin/products/ProductCardPage.vue`  
**Серьёзность:** Средняя — нарушение визуальной консистентности с CategoryCardPage и SupplierCardPage

### Проблема

Промпт 12 написал `<h2 class="section-title">` внутри `panel-body`. Существующий паттерн (SupplierCardPage, CategoryCardPage) — передавать заголовок через `:title` prop GlassPanel, который рендерит его в `.panel-header` (`span.panel-title`, font-size 14px, font-weight 700). `<h2>` внутри — отдельный блок в теле панели с другим стилем.

### Исправление

Все четыре секции переведены на `:title` prop:
```html
<GlassPanel :title="t('products.section_info')" ...>
<GlassPanel :title="t('products.section_price')" ...>
<GlassPanel :title="t('products.section_fields')" ...>
<GlassPanel :title="t('products.section_suppliers')" ...>
```

`.section-title` удалено из `products_card.css` (стало неиспользуемым).

### Правило на будущее

Заголовки секций в карточках — всегда `:title` prop на GlassPanel, не `<h2>` внутри slot.

---

## PAT-02 — Компонент обязан сам импортировать свои стили

**Файл:** `src/views/admin/products/ProductCardPage.vue`, `src/styles/admin/components/_entity-card-layout.css`  
**Серьёзность:** Высокая — save bar рендерится без стилей если забыт импорт

### Проблема

`entity-card-layout.css` определяет классы `entity-action-bar`, `no-margin`, `pos-static` — используются в header карточки для save bar и кнопок. Этот файл **не входит** в `admin-core.scss`. Каждая карточка-страница должна импортировать его явно.

При первом черновике карточки товара импорт был пропущен — save bar выглядел как блок без flex, кнопки без gap.

### Исправление

```ts
import '@styles/admin/components/_entity-card-layout.css'
import '@styles/admin/products_card.css'
```

Оба импорта — обязательны для страницы-карточки.

### Правило на будущее

Страницы-карточки (card pages) **всегда** импортируют два файла:
1. `@styles/admin/components/_entity-card-layout.css` — layout save bar + кнопки
2. `@styles/admin/[domain]_card.css` — page-specific стили

Страницы-списки (list pages) — только `[domain]_list.css`. `_entity-card-layout.css` им не нужен.

---

## БАГ-13 — `.empty-state` недоступен в ProductCardPage при прямой навигации

**Файл:** `src/styles/admin/products_card.css`  
**Серьёзность:** Средняя — секция поставщиков (пустой список) рендерится без стилей при заходе напрямую на URL карточки

### Проблема

`ProductCardPage.vue` использует `class="empty-state"` в секции поставщиков:
```html
<div v-if="!product || product.linkedSuppliers.length === 0" class="empty-state">
```

Этот класс определён только в `products_list.css`, `categories_list.css`, `suppliers_list.css`. Ни один из них не является глобальным. `admin-core.scss` не импортирует ни один из них. `ProductCardPage.vue` импортирует только `products_card.css` и `_entity-card-layout.css`.

При заходе напрямую на `/admin/products/prod-002` (у которого нет linked suppliers) — `empty-state` не получает стилей: нет `flex`, нет `padding 48px`, нет `text-align: center`.

### Исправление

Добавлено в `products_card.css`:
```css
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12px;
  padding: 48px 24px;
  color: var(--text-secondary, #888);
  text-align: center;
}
```

### Правило на будущее

Каждая страница, использующая `.empty-state`, должна иметь его определение в своём собственном CSS файле. Класс не является глобальным — не входит в `admin-core.scss`.

---

## БАГ-14 — Mock-данные cat-2 (Sheets) не содержат унаследованных полей f-1-1 и f-1-2

**Файл:** `src/services/mocks/products.ts`  
**Серьёзность:** Средняя — карточки товаров категории Sheets не показывают секцию «Динамические поля» для наследованных полей

### Проблема

В `categories.ts` STORE у `cat-2` (Sheets) есть `inheritedFields: [f-1-1 'Steel grade', f-1-2 'Standard / GOST']` — наследование от `cat-1` (Metal). Однако в `products.ts` STORE продукты `prod-001`, `prod-002`, `prod-008` (все в `cat-2`) не содержат этих полей в `fieldValues`. Комментарий в начале файла тоже не упоминал inherited fields для cat-2.

Следствие: секция «Динамические поля» для cat-2 товаров показывала только 3 поля (f-2-1, f-2-2, f-2-3) вместо 5 (f-1-1, f-1-2, f-2-1, f-2-2, f-2-3).

### Исправление

Добавлены `inherited: true` поля в начало `fieldValues` для всех cat-2 продуктов:
- `prod-001`: Steel grade = 'S235JR', Standard = 'EN 10051'
- `prod-002`: Steel grade = 'AMg2', Standard = 'GOST 21631'
- `prod-008`: Steel grade = 'S320GD', Standard = 'EN 10346'

Также исправлен комментарий в начале файла:
```
// cat-2 Sheets: f-2-1 (number), f-2-2 (enum), f-2-3 (text) + inherited: f-1-1 (text), f-1-2 (text)
```

### Правило на будущее

При написании mock-данных для продуктов — обязательно сверяться с `inheritedFields` их категории в `categories.ts`. Если у категории есть родитель — у всех её продуктов должны быть `inherited: true` поля в `fieldValues`.

---

## Итог

| # | Тип | Файл | Суть |
|---|---|---|---|
| БАГ-01 | TypeScript | `mocks/products.ts`, `productsService.ts`, `useProductCard.ts`, `ProductCardPage.vue` | Неиспользуемые импорты типов → TS6196 |
| БАГ-02 | TypeScript + Runtime | `ProductsPage.vue` | `CustomSelect` не принимает `string \| null` → computed-адаптеры `null ↔ ''` |
| БАГ-03 | TypeScript | `types/features.ts`, `featureFlags.ts`, `flags.ts` | Новые флаги не добавлены в FeatureFlagKey → TypeScript ошибка |
| БАГ-04 | CSS + UX | `ProductCardPage.vue` | `btn-ghost` не существует → `btn btn-secondary` + `router-link` |
| БАГ-05 | Runtime | `useProductCard.ts` | `v-model.number` ставит NaN при очистке → нормализатор NaN → null (форма + динамические поля) |
| БАГ-06 | Runtime | `ProductCardPage.vue` | `DropZone` — не v-model, требует `hint` prop и `@uploaded` event |
| БАГ-07 | Runtime | `mocks/products.ts` | `mockCreateProduct` не резолвил `categoryName` → карточка показывала `—` |
| БАГ-08 | Runtime | `mocks/products.ts` | `linkedSuppliers` ID `sup-1/2/3` не существуют в suppliers STORE → навигация 404 |
| БАГ-09 | CSS | `ProductCardPage.vue` | `<span class="glass-input">` — span inline, width:100% не работает → `<input readonly>` |
| БАГ-10 | Runtime | `useProductCard.ts` | `form.sku/description` сохранялись как `''` вместо `null` → список показывал пустую ячейку |
| БАГ-11 | Code style | `ProductCardPage.vue`, `ProductsPage.vue` | HTML-комментарии в `<template>` → pitfall #9 |
| БАГ-12 | Design | `ProductCardPage.vue` | `<h2 class="section-title">` вместо `:title` prop GlassPanel → нарушение паттерна |
| БАГ-13 | CSS | `products_card.css` | `.empty-state` отсутствовал в ProductCardPage → секция поставщиков без стилей |
| БАГ-14 | Mock data | `mocks/products.ts` | cat-2 продукты не имели inherited полей f-1-1, f-1-2 → секция полей неполная |
| PAT-01 | Паттерн | `useProductCard.ts` | `useDirtyCheck.track()` не существует → правильный метод `capture()` |
| PAT-02 | CSS | `ProductCardPage.vue` | карточки обязаны импортировать `_entity-card-layout.css` |

Проект Flexiron, ветка fix/contract-critical-2026-09-07 от main, дерево чистое.
Сведение API-контракта закончено: 17 доменов, 175 эндпоинтов из 175 (гейт печатает
строку «[контракт] сведено доменов: …»). Читай roo_code/plans/api/repair-after-run-2026-09-04.md
— там история и три правила, которые она дала.

ЗАДАЧА: провести меня через решения владельца и превратить ответы в спецификацию.

Файл: roo_code/plans/api/audit/00-решения-владельца.md — 189 строк.
Формат строки: <домен> · <графа> · что именно неизвестно · где смотрел.
Это вопросы к человеку, а не задачи: туда снесено то, у чего источник «нигде» —
обязанность сервера, которой нет ни в моке, ни на бэкенде, ни константой во фронте.

Спрашивай КУСТАМИ, а не по доменам: вопросы повторяются, и один ответ закрывает
несколько строк. Четыре куста закрывают 89 строк из 189:

  права            21 строка, задана во ВСЕХ 17 доменах — начни с него
  умолчания        31 строка (кто владеет ставкой НДС, валютой, размерами страниц)
  аудит-лог        19 строк (что пишем, кто автор — имя или id)
  транзакционность 18 строк (что из одной кнопки Save обязано быть атомарным)

Порядок работы на каждый куст — три звена, третье не пропускать:

  1. собери строки куста в один читаемый список: вопрос + что сейчас в коде + чем
     это измерено. Я отвечаю в чате;
  2. занеси ответ в 00-решения-владельца.md — рядом со строкой, а не вместо неё:
     видно должно быть и что спрашивали, и что решили, и когда;
  3. перепиши соответствующие «осталось» в разделах roo_code/roo-context/api/<домен>.md
     на нормальные требования к серверу. Всего таких строк 221; без этого шага
     ответ останется только в переписке, а контракт продолжит писать «осталось».

Формат раздела контракта и линзы К1–К7 — скил roo_code/skills/api-contract.md.

ПРИЁМКА после каждого куста:
  cd frontend_vue && npx vitest run          — 794 теста, гейт контракта внутри
  cd frontend_vue && CONTRACT_REFS=roo_code/roo-context/api/<домен>.md \
    npx vitest run src/services/contractRefs.spec.ts   — ссылки файл:строка
  cd backend && python3 -m unittest discover -s tests -t .   — 14 тестов
Про резолвер знай заранее: его «нет токена в диапазоне» почти всегда ложное
срабатывание (токен относится к соседнему утверждению). Ценны только жёсткие
вердикты — «нет файла» и «вне границ».

НЕЛЬЗЯ:
  — трогать мок-режим. Гвард frontend_vue/src/router/index.ts:422-424 намеренно
    пускает без входа при VITE_USE_MOCKS !== 'false' — это свойство, а не дефект;
  — git push и мерж: мержит человек;
  — git restore и git checkout -- <файл> (в deny-листе). Откат — git show HEAD:<path>;
  — писать утверждение о коде без замера рядом. Счётное утверждение без команды,
    которая его печатает, — самый частый дефект этого проекта: он убил шесть доменов
    из тринадцати на прошлом прогоне.

СОСТОЯНИЕ: все три Critical закрыты. Незакрытым осознанно оставлено:
  — bcc БАГ-02 и БАГ-11: починена только клиентская половина, форма ответа
    оставлена владельцу. Это ВХОДИТ в твою работу — два вопроса про форму
    ответа, задай их вместе с кустом, к которому они относятся;
  — alembic БАГ-02 (alembic check краснеет на девяти таблицах) и bcc БАГ-12
    (log в композабле никто не зовёт) — заведены, не тронуты, к решениям
    владельца не относятся. Не бери.
Баг-файлы держат открытые находки, в архив не уезжают.

Начни с куста «права»: собери 21 строку в список и задай мне вопросы.

1.

на странице http://localhost:5173/admin/settings/finance
там и настройки по валюте - оттуда она и берется, при этом будет хранится в бд на сервере


2.

exchangeRate точно не обязательно и его даже можно удалить, так как пока было решено от него отказаться, возможно позже вернем

3.

б) несколько чисел, по одному на валюту, и интерфейс показывает их списком;

4.

Валют, единиц измерения, правил пересчёта и статусов заказа это всё что нужно для работы с самого начала создается миграцией. те же статусы заказов - у нас в моках они обозначены системными, которые создаются системой и не могут быть удалены.

5.

я думаю пока что всё это целесообразнее хранить в коде. только вместо копий может сделать так чтобы был один источник для каждого из подобных значений в коде, сделать так как правильно.
но для Условия оплаты поставщика возможно стоит занести в настройки.

6.

нужно брать текущий месяц начиная от 1 числа до текущего момента

в моке тоже исправить чтоб всегда писало текущий месяц и год

7.

а. так и надо пусть удаляются

б. по умолчанию сессия живёт

Пока вкладка открыта и есть активность: сессия продлевается (скользящий тайм-аут / sliding expiration).

Если активность прекратилась: запускается тайм-аут бездействия (например, через 15–30 минут отсутствия кликов/запросов сессия завершается на сервере, даже если вкладка осталась открытой).

а если с галочкой запомнить меня - живёт 30 дней, с момента последней активности, то есть юзер может пользоваться всегда, если он этой сессией пользуется не реже раза в месяц без релогина, сессия сама себя продлевает

в. ок пусть сервер это контролирует

г. Механизм нужен — тогда чинить целиком: рождать черновиком, снимать пометку на Save, завести уборщика.

д. добавить отсрочку платежа в настройки, по умолчанию также 0 дней, но чтобы это можно было бы гибко настроить

е. новый элемент по умолчанию видит только админ.
но обычно ролям доступен весь домен и тогда если фича в этом домене, то таким ролям он также доступен. там правило гибкое. если роль или отдельный пользователь имеет полные права на не меньше чем 90% от домена - то ему и новая фича в домене доступна. если он имеет также не меньше чем на 90% домена часть прав, эти права распространяются и на новую фичу.

ж. ширина реза по умолчанию она тоже пусть будет в настройках, но она также может гибко настраиваться в каждом случае отдельно

8. Сквозная нумерация: #1, #2, #3…
Читается, совпадает с тем, что уже на экране. Сервер держит счётчик на арендатора и выдаёт следующий. Единственное требование — выдавать по одному за раз, чтобы двое одновременных не получили один номер; 
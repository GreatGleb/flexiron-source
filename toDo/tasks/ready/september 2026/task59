1.1 
в) Выборочно. Атомарен там, где полумера опасна — заказ, склад, — а справочники и настройки остаются как есть.

1.2

Я бы советовал (б) для товаров и услуг — они попадают в исторические документы, и заказ двухлетней давности должен читаться как был, — и (а) для справочников (единицы, валюты): там отказ понятнее.

удаленный товар больше не видел в таблице товаров, не предлагается нигде где он может использоваться - в заказах, запросах к поставщику и тд, но доступен по ссылке из заказов и в других таких же старых сущностях

1.3

а) Сервер отдаёт всё на одну отметку времени и присылает её в ответе,
но уточнять и показывать юзеру время отчёта не надо

1.4
срок хранения сутки, ключ считается использованным для конкретной операции, а не вообще

И надо для всех подобных операций, особенно для сохранения, добавления, и вообще везде на всех страницах на фронтенде добавить следующую обработку, если этого ещё нет: 
сначала когда фронтенд делает отправку - кнопка становится замороженной и юзер не может отправить второй раз пока не придёт ответ от сервера. поэтому например при возврате он сначала увидит, что операция завершилась и возврат отправлен, окно добавления возврата закрылась и только затем кнопка становится размороженной. (хотя она тут же станет замороженной из-за того, что окно возврата обновится и станет пустым, а для отправки нужно сначала заполнить все данные там)
Это спасёт везде от подобных ошибок. Это отдельная задача по перепроверки всех эндпойнтов отправки данных на бекенд из фронтенда. Возможно придётся обновить и бекенд, чтобы он возвращал нужный ответ для фронтенда.

1.5 

а) Сначала записать, потом отправить. Если отправка упала — запись остаётся, помеченная как неотправленная, и её можно повторить. Обратный порядок хуже: письмо не вернёшь.

возвращает ли сервер созданные строки прямо в ответе, или клиент после отправки перечитывает историю заново?
возвращать — на один запрос меньше, список обновляется мгновенно;

--- добор 2026-09-09, две строки вне пяти вопросов ---

карточка клиента, атомарен ли Save:
ну пусть как есть

финансовая запись, атомарна ли связка аплоада и Save:
Финансовая запись тоже как есть

повторная загрузка того же файла:
Повторная загрузка того же файла  - просто фильтром на фронте - вроде фронт умеет различать если ему в одной форме сразу выслать 2 одинаковых файла, а на бэке не надо
